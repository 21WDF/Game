# 大招框架扩展：支持指定格子瞄准 · Trae 提示词

## 问题

现有大招只能对「敌人」或「自身」释放，无法对「某个格子」释放。后续多个棋子的大招需要指定一个位置作为目标（可能是空格），例如在指定位置投放召唤物、在指定位置放置领域、对指定位置及其周围造成范围伤害等。

## 根源

现有大招的目标参数是「棋子」，大招瞄准流程也只高亮「敌人」。因此无法表达「指定一个格子（尤其是空格）」这种目标，大招效果也拿不到「目标位置坐标」。

## 修复目标

让大招支持「指定格子」作为目标，同时保留现有的「对敌人」「对自身」两种模式：

- 大招瞄准时，玩家可以点选一个格子（包括空格）作为目标
- 大招效果执行时，能拿到目标格子的坐标
- 不同大招的瞄准范围可自行定义（由后续各棋子的大招配置决定，本次只搭能力框架）

## 涉及文件

- `Assets/Game/Piece/Model/PieceData.cs`（大招配置）
- `Assets/Game/Piece/Effects/IUltimateEffect.cs`（大招效果接口）
- `Assets/Game/Piece/Controller/PieceManager.cs`（大招执行）
- `Assets/Game/Battle/Controller/BattleController.cs`（大招瞄准流程）

## 不要破坏

1. 现有两个大招（对敌人 / 对自身）的行为
2. 大招的 AP 消耗、能量消耗逻辑
3. 普攻选目标的逻辑（这是普攻，不是大招）

## 验收要点

- 现有大招行为不变
- 新大招能指定一个格子为目标，点空格也能作为目标
- 大招效果执行时能拿到目标格子的坐标
