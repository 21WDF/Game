# 阶段② C 批（死亡之蔑 / 中娅悖论 / 军团圣盾）+ 唯一被动框架 · Trae 提示词

## 总览

本批实现 4 个新被动类 + 1 个通用「唯一被动」框架 + 伤害/死亡介入 + 光环查询。涉及 3 种新机制：

1. **伤害介入**（首伤=1、死亡之蔑拆分）——在扣血前修改伤害值，不走事后钩子
2. **死亡介入**（免死）——在扣血后、死亡判定前恢复 1 HP
3. **光环查询**（军团圣盾）——属性查询时遍历全场，动态累加范围加成

---

## 一、唯一被动框架（通用，先做）

### 目标
唯一被动按「**唯一被动 id（uniqueId）**」判断约束：同一棋子身上，`uniqueId` 相同的唯一被动只生效**最后装备的那件**，先装备的失效（先进先出）。不同棋子各持相同 `uniqueId` 的被动可同时生效（光环重叠处可叠加）。**不同装备可共享同一 `uniqueId`**（此时它们互相冲突）——这是关键：约束键是「唯一被动 id」，不是装备名、不是装备身份。

### 字段（PassiveConfig 级别，非装备级别）

`EquipmentData.PassiveConfig` 加 3 个字段：

| 字段 | 类型 | 默认 | 说明 |
|---|---|---|---|
| passiveName | string | "" | 被动名字（纯展示用，后续 UI 装备介绍开头显示） |
| isUnique | bool | false | 是否唯一被动 |
| uniqueId | string | "" | 唯一被动 id（isUnique=true 时必填；不同装备填相同 uniqueId 即互相冲突） |

> `isUnique=false` 时只填 `passiveName`（展示名），`uniqueId` 留空；`isUnique=true` 时 `passiveName` + `uniqueId` 都要填。

### 改动
1. **`EquipmentData.PassiveConfig`**：加 `passiveName` / `isUnique` / `uniqueId` 三字段（带 Tooltip 说明）。
2. **`IPassiveEffect`**：加默认接口属性 `string UniqueId => null;`（现有被动类零改动；返回 null = 非唯一）。
3. **`PassiveFactory.Create`**：加第三个参数 `string uniqueId = null`，透传给唯一被动类构造（如 LegionAegisPassive 存字段并覆写 `UniqueId` 属性）。
4. **`EquipmentManager.InstantiatePassives`**：调用 Create 时传 `cfg.uniqueId`（isUnique 时非空）。
5. **`EquipmentModel.cs`**：加字段 `public int EquipOrder;`（装备时间戳，0=未装备，**仅用于判断「先进先出」的先后**，不是唯一性标识）。
6. **`EquipmentManager.cs`**：加 `private int _equipOrderCounter;`；`EquipToPiece` 时 `equipment.EquipOrder = ++_equipOrderCounter;`；`UnequipInternal` 时 `equipped.EquipOrder = 0;`。
7. **`PieceModel.GetAllPassives()`**：遍历 `EquippedItems` 时对**被动级**做唯一去重——对 `UniqueId` 非空的被动，按 `UniqueId` 分组，每组只保留 `EquipOrder` 最大（最后装备）那件装备里的该被动；`UniqueId` 为 null 的被动（普通被动）全部加入；`BuiltInPassives` 不变照旧加入。

### 约束
- **唯一性分组键 = `uniqueId`（唯一被动 id）**，不是装备名、不是 EquipOrder。`EquipOrder` 只用于「先进先出」的先后取舍。
- 去重逻辑只影响 `GetAllPassives()` 的遍历结果，**不改 `EquippedItems` 数组本身**（被压制的装备仍在槽位里，可正常卸下）。
- `passiveName` 本次只落字段，UI 展示后续再做。

---

## 二、死亡之蔑（DeathDancePassive）——延迟伤害

### 参数（jsonParams）
| 字段 | 类型 | 默认 | 说明 |
|---|---|---|---|
| delayPercent | int | 50 | 延迟比例（受击伤害的 X% 延迟，1~99） |
| delayTurns | int | 2 | 延迟回合数（受击后 N 回合内平分结算） |
| realTimeDamage | bool | false | 实时伤害开关（false=固定；true=随防御变化微调） |

### 机制

**① 受击拆分（伤害介入，扣血前）**
- 受击时把本次伤害拆成「立即结算」+「延迟结算」：
  - 延迟部分 = `floor(伤害 × delayPercent / 100)`
  - 立即部分 = `伤害 - 延迟部分`
- 立即部分正常扣血；延迟部分累加进被动实例的 `_pendingDamage`，`_pendingTurns = delayTurns`，并记录受击时防御力 `_baseDefense`（实时开关用）。

**② 每回合结算（OnTurnStart）**
- 若 `_pendingTurns > 0`：
  - 每回合基础伤害 = `floor(_pendingDamage / _pendingTurns)`
  - 若 `realTimeDamage == false`：本回合结算伤害 = 基础伤害（固定，防御变化不影响）
  - 若 `realTimeDamage == true`：
    - 系数 `k = floor(delayPercent / delayTurns)`
    - 防御变化量 `Δ = 当前有效防御 - _baseDefense`
    - 修正量 `n = floor(abs(Δ) × k / 100)`
    - 本回合结算伤害 = 基础伤害 - `sign(Δ) × n`（防御上涨→伤害下降；防御下降→伤害上涨）
  - 结算伤害最低 1（若 >0 则至少扣 1）
  - 扣血：`owner.TakeDamage(伤害)` + `owner.TurnsSinceDamaged = 0` + 触发 `owner` 全部被动 `OnDamageReceived(owner, 伤害, DamageSource.Dot)`（打断计时；**不触发 OnAttacked / OnDamageDealt / 元素反应 / 金币 / 能量**）
  - `_pendingDamage -= 已结算伤害`；`_pendingTurns--`；`_pendingTurns == 0` 时清空 `_pendingDamage`
- 延迟伤害是物理伤害，**不享受防御减伤**（结算值就是上面算出的值，直接扣）。

**③ 禁止卸下**
- `_pendingTurns > 0`（有未结算延迟伤害）期间禁止卸下该装备。

### 改动文件
1. **新建 `Assets/Game/Equipment/Effects/DeathDancePassive.cs`**：实现 `IPassiveEffect`，覆写 `OnTurnStart`；暴露方法供伤害介入调用（如 `void SplitIncomingDamage(ref int damage, int currentDefense)`，把 delayPercent% 拆进实例 `_pendingDamage`）；暴露 `bool HasPendingDamage => _pendingTurns > 0`。
2. **`PassiveFactory.cs`**：加 `case "DeathDancePassive"` + 参数类 `DeathDanceParams`（`[System.Serializable]`）。
3. **`PieceManager.cs`**：
   - 加私有聚合方法 `ApplyDeathDelay(PieceModel target, ref int damage)`：遍历 target 全部被动，对 `DeathDancePassive` 调用拆分。
   - 在 `AttackPiece` 和 `ApplyIncomingDamage` 两处的 `TakeDamage` 前调用它（顺序见下文「介入顺序」）。
4. **`EquipmentManager.cs`**：`UnequipInternal` 开头加检查——若该装备 `ActivePassives` 中有 `DeathDancePassive` 且 `HasPendingDamage`，则 `return` 拒绝卸下（打印日志提示）。

---

## 三、中娅悖论（两个独立被动）

> 中娅悖论 = 「首伤=1」+「免死」两个独立被动，装备的 `passives` 数组里配两条 `PassiveConfig` 分别指向它们，各自有独立冷却。

### 3.1 首伤=1（FirstDamageToOnePassive）

**参数**：`cooldownTurns`（int，默认 4）。

**机制**：
- 受击扣血前（伤害介入）：若冷却完毕（`_cooldown == 0`）且 `damage > 1`，则 `damage = 1`，`_cooldown = cooldownTurns`。
- `OnTurnStart`：`_cooldown > 0` 时递减。
- 冷却记在被动实例上；卸下暂停（`OnUnequip` 空实现，卸下后不再收到 OnTurnStart 自然暂停，重装继续递减）。

### 3.2 免死（DeathDefyPassive）

**参数**：`cooldownTurns`（int，默认 16）。

**机制**：
- 扣血后、死亡判定前（死亡介入）：若目标已死（`IsDead`）且冷却完毕（`_cooldown == 0`），则 `SetHP(1)`（免死一次），`_cooldown = cooldownTurns`。
- `OnTurnStart`：`_cooldown > 0` 时递减。
- 冷却记在被动实例上；卸下暂停（同上）。

### 改动文件
1. **新建 `FirstDamageToOnePassive.cs`**、**新建 `DeathDefyPassive.cs`**。
2. **`PassiveFactory.cs`**：加 `case "FirstDamageToOnePassive"`、`case "DeathDefyPassive"` + 两个参数类（`[System.Serializable]`）。
3. **`PieceManager.cs`**：
   - 加私有聚合方法 `ApplyFirstDamageProtection(PieceModel target, ref int damage)`：遍历 target 全部被动，对 `FirstDamageToOnePassive` 调用「首伤降为 1」。
   - 加私有聚合方法 `TryDefyDeath(PieceModel target)`：遍历 target 全部被动，对 `DeathDefyPassive` 调用「免死」。
   - 在 `AttackPiece` 和 `ApplyIncomingDamage` 两处按「介入顺序」接入。

### 介入顺序（AttackPiece 与 ApplyIncomingDamage 均遵循）

```
伤害计算（含元素反应）
  → 附加/减伤介入（max(1, 基础+附加-减伤)）
  → ApplyFirstDamageProtection（首伤=1，伤害降为 1）
  → ApplyDeathDelay（死亡之蔑拆分：立即 + 延迟）
  → TakeDamage（扣立即部分）
  → TryDefyDeath（免死：将死则回 1 HP）
  → （AttackPiece 后续）死亡判定 DestroyPiece
```

---

## 四、军团圣盾（LegionAegisPassive）——光环 + 进化

### 参数（jsonParams）

| 字段 | 类型 | 默认 | 说明 |
|---|---|---|---|
| affectDirection | string | "Ally" | 作用方向：Ally=己方 / Enemy=敌方 |
| affectRange | int | 2 | 作用范围（HexCoord 距离 ≤ 该值） |
| bonusStat | string | "Defense" | 加成属性：Attack / Defense |
| bonusAmount | int | 5 | 加成数值（可为负值） |
| evolveTurns | int | 4 | 进化回合：-1=不进化；0=装备立即进化；>0=连续该回合数未受伤后进化 |
| evolvedDirection | string | "Ally" | 进化后作用方向 |
| evolvedRange | int | 3 | 进化后作用范围 |
| evolvedStat | string | "Defense" | 进化后加成属性 |
| evolvedAmount | int | 10 | 进化后加成数值（可为负值） |
| evolveOverrides | bool | true | 新光环是否覆盖旧光环（true=覆盖；false=新旧叠加） |

> `affectDirection` / `bonusStat` / `evolvedDirection` / `evolvedStat` 用 **string 承接 + 手动解析**（JsonUtility 枚举字段只认数值不认名字，同 FreeAPType 的坑）。定义两个枚举 `AuraDirection { Ally, Enemy }`、`AuraStat { Attack, Defense }`，解析不区分大小写。

### 机制

**① 光环（查询型，动态遍历全场）**
- 当任意棋子的 `EffectiveAttack` / `EffectiveDefense` 被查询时，遍历**全场所有棋子**（`TurnManager.Instance.Model` 的 P1+P2 列表），对每个棋子的 `LegionAegisPassive` 判断：
  - 携带者坐标与目标棋子坐标的 `HexCoord.Distance ≤ 作用范围`
  - 目标棋子阵营与 `作用方向` 匹配（Ally=同阵营 / Enemy=敌阵营）
  - 命中则累加 `加成数值`
- 进化状态决定用哪套光环参数：
  - 未进化：用 `affectDirection/affectRange/bonusStat/bonusAmount`
  - 已进化且 `evolveOverrides=true`：只用 `evolved*` 一套
  - 已进化且 `evolveOverrides=false`：新旧两套都算（分别判断范围/方向/属性，命中都累加）

**② 进化计时（装备实例）**
- 实例字段 `_turnsWithoutDamage`：`OnTurnStart` +1 并判断 `≥ evolveTurns` 则进化（一次性，进化后不再退化）；`OnDamageReceived` 归零；`OnEquip` 清零（卸下重装重新计时）。
- `evolveTurns == 0`：`OnEquip` 时立即进化。
- `evolveTurns == -1`：永不进化（始终用基础光环）。

**③ 唯一**
- 走「唯一被动框架」：军团圣盾的 `PassiveConfig` 里 `isUnique=true` + 填 `uniqueId`（如 `"LegionAegis"`）+ `passiveName`（如 `"圣盾光环"`）。约束按 `uniqueId` 生效，与其他同名唯一被动冲突。

### 改动文件
1. **新建 `LegionAegisPassive.cs`**：含 `AuraDirection` / `AuraStat` 枚举；实现 `IPassiveEffect`，覆写 `OnTurnStart` / `OnDamageReceived` / `OnEquip`；**构造接收 `uniqueId` 参数并覆写 `UniqueId` 属性**（唯一被动框架用）；暴露方法供属性查询调用（如 `int GetAuraAmount(string statType, HexCoord targetCoord, PlayerSide targetOwner)`，返回对指定目标该属性的加成，0=不在范围内/属性不匹配）。
2. **`PassiveFactory.cs`**：加 `case "LegionAegisPassive"` + `LegionAegisParams` 参数类（`[System.Serializable]`，string 字段承接枚举）；case 内把 `Create` 收到的 `uniqueId` 透传给 LegionAegisPassive 构造。
3. **`PieceModel.cs`**：
   - 加私有方法 `GetAuraBonus(string statType)`：遍历全场棋子收集 `LegionAegisPassive.GetAuraAmount` 累加（`TurnManager.Instance == null || Model == null` 时返回 0）。
   - `EffectiveAttack` 追加 `+ GetAuraBonus("Attack")`；`EffectiveDefense` 追加 `+ GetAuraBonus("Defense")`。

---

## 五、约束（务必遵守）

1. **不要改 `HasAttackedThisTurn` 字段语义、不要改 `HandleAttack` 内部连射逻辑、不要改元素反应/金币/能量结算**。
2. **介入顺序严格按上面「介入顺序」**：首伤=1 → 死亡之蔑拆分 → 扣血 → 免死。
3. 延迟伤害、免死、首伤=1 的冷却/状态**都记在被动实例上**（跟装备走，卸下暂停），不要借用棋子全局字段。
4. 新增参数类**全部 `[System.Serializable]`**；枚举字段用 string 承接 + 手动解析。
5. 光环遍历注意空安全（`TurnManager.Instance` 可能为 null）。
6. 诊断无报错后收尾。
