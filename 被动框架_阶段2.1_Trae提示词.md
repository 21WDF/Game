# 被动框架（阶段2.1）Trae 提示词

```
在 Chaotic Chess 中实现被动系统框架（阶段②第一步）：把 IPassiveEffect 从
"OnEquip/OnUnequip 生命周期"扩展为"事件驱动"，支撑事件触发型被动。

本阶段只做框架，不实现具体被动效果（具体被动在 2.2 分批做）。

## 改动 1：IPassiveEffect 扩展事件钩子

给 IPassiveEffect 接口加事件钩子方法。用 C# 默认接口方法（default interface method），
现有 ExtraDamagePassive 不用改，新被动按需覆写：

public interface IPassiveEffect
{
    void OnEquip(PieceModel owner);
    void OnUnequip(PieceModel owner);

    // 新增事件钩子（默认空实现）
    void OnTurnStart(PieceModel owner) { }
    void OnDamageReceived(PieceModel owner, int damage) { }
    void OnDamageDealt(PieceModel owner, int damage) { }
    void OnKill(PieceModel owner, PieceModel victim) { }
    void OnAllyDeath(PieceModel owner, PieceModel ally) { }
}

## 改动 2：PieceModel 状态字段（被动运行时状态）

给 PieceModel 加被动需要的通用状态字段（默认值，具体被动在 2.2 用）：

- 临时属性 buff 列表（如"攻击+10 持续10回合"）：List<TempBuff> TempBuffs
  （TempBuff 结构：属性类型 + 数值 + 剩余回合，定义在 PieceModel 或独立文件）
- 未受伤害连续回合计数：int TurnsSinceDamaged（默认 0，受伤害时归零，回合开始 +1）
- 上次受伤害来源：PieceModel LastDamageSource（默认 null，石像鬼板甲用）
- 延迟伤害池：int PendingDamage（默认 0，死亡之蔑用）
- 冷却计时器：Dictionary<string,int> Cooldowns（中娅悖论/探索者护臂/诸葛连弩用）

（这些是通用字段，具体被动 2.2 再用；不确定的字段可以留到 2.2 再加，本阶段先搭 TempBuffs + TurnsSinceDamaged + PendingDamage + Cooldowns）

## 改动 3：事件触发点（在游戏逻辑里调用钩子）

### 3.1 回合开始（TurnManager）
在回合结束回血回蓝逻辑附近（或回合开始处），遍历双方所有棋子的所有被动
（EquippedItems 的 ActivePassives + Data.builtInPassives），调 OnTurnStart(owner)。

### 3.2 造成伤害 / 受到伤害（PieceManager.AttackPiece）
- 造成伤害后：遍历攻击方的所有被动，调 OnDamageDealt(attacker, damage)
- 受到伤害后：遍历目标的所有被动，调 OnDamageReceived(target, damage)

### 3.3 击杀 / 己方死亡（PieceManager.DestroyPiece 或死亡处理处）
- 棋子死亡时：遍历击杀者（若有）的所有被动，调 OnKill(killer, victim)
- 遍历死亡方其他存活棋子的所有被动，调 OnAllyDeath(ally, victim)

### 辅助方法
在 PieceModel 或一个静态工具里加一个方法，方便统一遍历棋子的所有被动：
List<IPassiveEffect> GetAllPassives()
  → EquippedItems 的 ActivePassives + Data.builtInPassives 的实例

（builtInPassives 如果目前没有实例化逻辑，本阶段先加一个：棋子创建时把
 Data.builtInPassives 的 PassiveConfig 实例化成 IPassiveEffect 存到 PieceModel。
 具体工厂复用 EquipmentManager.CreatePassive 或新建一个 PassiveFactory。）

## 约束

- 不改现有 ExtraDamagePassive（默认接口方法兼容）
- 不改伤害计算 / 元素反应 / 金币 / 能量逻辑
- 事件钩子是"通知"，不改变主流程的伤害值或返回值
- 本阶段不实现具体被动效果（钩子先空跑）
- 不改 MovePiece / 攻击范围 / 移动模板

## 验收

- [ ] IPassiveEffect 有 5 个新事件钩子（默认空实现）
- [ ] PieceModel 有 TempBuffs / TurnsSinceDamaged / PendingDamage / Cooldowns 字段
- [ ] 回合开始时，所有棋子的被动 OnTurnStart 被调用（可加 Debug.Log 验证）
- [ ] 攻击时，攻击方 OnDamageDealt、目标 OnDamageReceived 被调用
- [ ] 棋子死亡时，OnKill / OnAllyDeath 被调用
- [ ] 现有 ExtraDamagePassive 编译通过（没被迫改）
- [ ] 游戏运行无报错（钩子空跑不影响主流程）
```
