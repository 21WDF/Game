# 城邦选择 UI — Trae 提示词

## 现状（框架事实）

- 城邦选择的后端管线**已经完整、且已按联机模型预留**：提供「双方各自独立提交心仪城邦 → 双方都提交后自动『求交集 + 随机』定 1 个」的完整能力；随机入口收敛到单一方法（将来网络化只替换这一处为同步 seed）。这条管线天然就是联机的「同时选择、提交后确定」模型。
- 当前 `GameFlowController` 在战前流程最前用的是**临时自动随机占位**（双方各随机挑 4 个提交），这次替换成真实 UI。
- 战前流程顺序：城邦选择 → P1 选棋子 → P1 部署 → P2 选棋子 → P2 部署 → 开战。
- 可选城邦共 6 个，已有统一城邦列表数组。

## 改动方向

新增「城邦选择」交互 UI，替换自动随机占位。**核心原则：选择/提交/结算逻辑必须传输无关（不依赖本地热座的「同屏 / 顺序 / 信息可见」假设），将来联机直接复用。**

1. 选择流程建模为「双方各自独立提交，双方都提交后自动结算」——这天然就是联机模型。
2. 本地热座阶段，由战前流程控制器驱动「当前谁在选择」（P1 先选、提交后切 P2）；UI 只负责三件事：展示可选城邦、采集当前玩家的选择、调提交接口。UI 不假设「对方是否在同一屏幕看到我的选择」。
3. **界面布局**：城邦以「卡片」形式铺开——每行 3 张、纵向排布（当前 6 城邦 = 3 列 × 2 行）。卡片式观感参考金铲铲「海克斯强化」选择的布局，但注意：金铲铲海克斯是「横排 3 选 1 单选」，这里是「6 城邦多选、选 4 个」，交互不同，只借鉴其卡片铺开的美术观感。
4. **交互**：点击卡片切换「选中/取消」（多选）；选满数量后点「提交」按钮直接提交，未选满时提交不可用或给出提示。
5. 每位玩家选择数量 = floor(城邦总数/2)+1（当前 6 城邦 = 4），由城邦总数动态计算，不写死。
6. 双方都提交后，走现有「求交集 + 随机」管线定出本局城邦并揭晓，随后进入 P1 选棋子。

## 效果目标（验收标准）

1. 开局进入城邦选择，P1 勾选 4 个并确认。
2. P1 确认后切 P2 勾选 4 个并确认。
3. 双方确认后，从重合城邦随机定 1 个并揭晓。
4. 揭晓后进入 P1 选棋子，后续部署、对战流程顺序不变。
5. 每方可选数量 = floor(城邦总数/2)+1，动态算出（将来增减城邦不用改）。
6. **传输无关**：选择、提交、结算的逻辑不写死「P1 先、P2 后」或「同屏可见」等热座假设；「谁在选择」只是本地热座的驱动方式，将来联机换成「各自屏幕 + 网络同步」即可复用同一套逻辑。
7. **界面表现**：城邦卡片以「每行 3 张、纵向铺开」布局，选中态清晰可辨，选满后可点提交直接提交。

## 红线（绝不能破坏）

1. **后端选择管线零改动**：「各自提交 + 双方提交后自动结算」的模型、求交集纯函数、随机入口收敛单一方法，全部保持原样，UI 只调用。
2. **逻辑层不引入热座假设**：不要在提交/结算逻辑里依赖「顺序」「同一屏幕」「对方能看到我的选择」；本地热座的「轮流选择」仅是驱动层的事，不得写进逻辑层。
3. **不破坏 P1 选棋子之后的流程顺序**：城邦揭晓后进入 P1Select，其后 P1Deploy → P2Select → P2Deploy → Playing 顺序不能乱。
4. 城邦确定后必须真正写入当前城邦状态（后续机制过滤依赖它），不能只显示结果不落地。
5. 临时随机占位相关代码随之移除，不留死代码。
