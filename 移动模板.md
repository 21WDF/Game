# 移动模板 设计规范

> 体系：`IMoveRangeProvider` + `MoveRangeConfig`（接口 + 工厂 + ScriptableObject）
> 目录：`Assets/Game/Piece/Range/`

---

## 一、体系定位

移动模板是「Range Provider」体系的一部分，与攻击范围（`IAttackRangeProvider`）并列。它只回答一个问题：**一个棋子移动时，哪些格子可达？**

```
PieceData.moveConfig（SO）
  └── providerClassName + jsonParams
        └── RangeProviderFactory 创建 IMoveRangeProvider
              └── GetReachable(from, effectiveRange, isBlocked, model) → List<HexCoord>
```

关键原则：**移动规则 ≠ 移动表现**。provider 管"规则"（可达性），动画管"表现"（走/跑/飞/闪现/跳跃弧线）。

---

## 二、现有模板（2 种）

| 模板 | 规则 | 状态 |
|------|------|:---:|
| **FreeMove** | BFS 六方向，不能穿过被棋子占据的格子 | ✅ 已有 |
| **StraightMove** | 6 方向射线，遇阻（棋子/边界）停止 | ✅ 已有 |

---

## 三、待新增模板（3 种）

### 核心洞察：飞行和瞬移是两条规则，不是两种表现

**飞行会经过中途格子（吃领域技能/地刺/光环），瞬移点到点、不吃任何途中效果。** 这是实质玩法差异，必须分成两个模板。

| 模板 | 可达性 | 穿过棋子 | 经过中途格（触发领域/地刺） | 表现 |
|------|--------|:---:|:---:|------|
| **FlyMove** | BFS 六方向 | ✅ 是 | ✅ **是**（领域能打到） | 飞行 |
| **BlinkMove** | BFS 六方向 | ✅ 是 | ❌ **否**（点对点） | 闪现 |
| **JumpMove** | 跳过 `jumpOver` 格 | 跳过 | ❌ 否（跳过格不触发） | 跳跃弧线 |

- **FlyMove**：BFS 无视棋子阻挡（可飞越），落点必须空格，但逐格经过、触发 `onTileTraversed`
- **BlinkMove**：BFS 无视阻挡，点到点瞬移，不触发任何经过效果
- **JumpMove**：跳过 `jumpOver` 格障碍（马式跳跃的泛化），落点空格，中间跳过格不触发经过效果

---

## 四、参数设计

| 模板 | 参数 | 默认值 |
|------|------|--------|
| Free / Fly / Blink | `maxSteps` | 3~5 |
| Straight | `maxSteps` | 4 |
| Jump | `maxSteps` + `jumpOver` | `jumpOver = 1` [PLACEHOLDER] |

---

## 五、唯一的架构改动

`IMoveRangeProvider` 增加一个只读属性：

```
MoveTraversalMode TraversalMode { get; }
// Traverse（Free/Straight/Fly）→ 逐格动画 + onTileTraversed 回调
// Instant（Blink/Jump）→ 直接瞬移，跳过中途格
```

**`onTileTraversed` 回调是之前做移动动画时预留的**，现在终于有了真正的用途——Fly 经过中途格时触发领域效果。这是当初设计的前瞻性兑现。

---

## 六、表现映射（动画层）

移动配置增加 `moveVisual` 字段：`{ Walk, Run, Fly, Blink, Jump }`。

- 规则模板决定"可达性"，`moveVisual` 决定"播什么动画"
- 瞬移棋子 = `BlinkMove + Blink` 表现，不新写 provider
- 飞行棋子 = `FlyMove + Fly` 表现

---

## 七、9 棋子落地

| 棋子 | 移动模板 |
|------|---------|
| 骑士 Knight | **Straight_4**（直线冲锋） |
| 刺客 Assassin | **Blink_5**（瞬移背刺） |
| 游侠 Ranger | **Jump_4**（跳跃游击） |
| 其余 6 棋子 | Free（标准） |

**Fly 模板纯预留**——现 9 棋子均人形，飞行不合身。未来加飞行/漂浮棋子时直接套 `FlyMove`。

---

## 八、待确认项

1. Fly 现在无棋子用，是否临时给某棋子配 Fly 来实测"飞行吃领域 / 瞬移不吃"的交互？还是纯预留？
2. Jump 的 `jumpOver` 默认值 1 格，是否合适？
