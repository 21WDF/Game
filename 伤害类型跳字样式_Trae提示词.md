# 伤害类型跳字样式（纯色/渐变/边框）—— Trae 提示词

## 目标

让伤害数字跳字的**样式**表达「伤害类型」，让玩家不依赖元素也能一眼分辨伤害类型：

| 伤害类型 | 跳字样式 |
|---------|---------|
| 物理 | 纯色块（纯色填充） |
| 魔法 | 颜色渐变（如上下明暗渐变） |
| 真实 | 深色边框（描边） |

而**颜色**继续表达「元素」（火/水/雷/冰），两者正交：

| 场景 | 颜色 | 样式 |
|------|------|------|
| 带元素物理 | 元素色 | 纯色块 |
| 带元素魔法 | 元素色 | 渐变 |
| 带元素真实 | 元素色 | 深色边框 |
| 无元素物理 | 物理红 | 纯色块 |
| 无元素魔法 | 魔法紫 | 渐变 |
| 无元素真实 | 真实白 | 深色边框 |

## 现状（已核对，供你定位）

- 跳字组件是 **TextMeshPro（TMP）**（`FloatingText.text` 是 `TextMeshProUGUI`）。
- 样式参数当前是 `FloatStyle`（color / duration / floatDistance / fontSize / shake），只有「纯色」一种形态，`FloatingText.Show` 里 `text.color = style.color`。
- 颜色来源**上一轮已修对**：普攻路径与统一入口都已按「带元素→元素色、无元素→伤害类型色」取色（`ElementColorMapper.GetElementDamageColor` / `GetDamageKindColor`）。本轮**只加样式维度，颜色逻辑保持现状**。
- 伤害数字跳字目前有个 `isReaction` 参数，作用是「触发了反应就把伤害数字染成紫色」。这个语义本轮要调整：**伤害数字的颜色/样式不再被「是否反应」影响**，「触发反应」改由单独的反应名提示跳字（「蒸发！×2」）承担（这段保持现状，不用动）。

## 改动方向

1. 给跳字样式增加「伤害类型」维度（纯色 / 渐变 / 边框 三种形态），伤害数字跳字时根据**伤害类型**（物理/魔法/真实）选择形态。
2. 伤害数字跳字的调用点需要把「伤害类型」传进去（各调用点都已持有 damageKind，普攻/溅射=物理，附加伤害段与统一入口已有 kind 参数）。
3. `FloatingText` 渲染层按样式切换 TMP 表现：纯色=直接着色；渐变=TMP 渐变；边框=TMP 描边。

## 实现注意（容易踩的坑）

- **淡出动画与渐变的交互**：现在淡出靠颜色 alpha 渐减。切到渐变后要保证淡出仍正常（不「漏光」、不闪烁）。
- **边框是「组件」不是「属性」**：对象池复用时，从边框/渐变切回纯色，要清理上一次残留的渐变/描边状态，避免串到下一次跳字。
- **渐变对比度要够**：跳字只有 0.8 秒上漂+淡出，渐变若「浅色→浅色」玩家分辨不出。魔法渐变建议用明暗跨度明显的两色，一眼区别于纯色块。

## 红线（不要破坏）

- 伤害数值、元素附着、元素反应、减伤/易伤、金币、能量——**全部是逻辑层，一律不动**，本轮是纯 View 层改动。
- 反应名提示跳字（「蒸发！×2」等紫色提示）保持现状。
- 颜色映射（`ElementColorMapper` 两套取色）保持现状，本轮不新增颜色来源。
- 非元素伤害（反伤/感电 DoT/超载爆炸）的颜色仍是伤害类型色（物理红/魔法紫/真实白），只是样式按类型走。

## 验收要点

- 物理伤害数字 → 纯色块（颜色=元素色或物理红）
- 魔法伤害数字 → 渐变（颜色=元素色或魔法紫）
- 真实伤害数字 → 深色边框（颜色=元素色或真实白）
- 触发反应时，伤害数字不再变紫，反应名提示（「蒸发！」）照常显示
- 伤害数值、反应、附着与修复前一致（仅跳字样式变）
