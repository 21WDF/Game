# P0 修补方案：扩展 UI_StatElement 实时跟随面板

## 前提

**现有系统不动**：`UI_PieceStatsDisplay.LateUpdate` → `SyncPieces` → `UpdateElement` 的对象池+屏幕跟随机制完全保留。只做两件事：

1. `UI_StatElement` 加字段
2. `UpdateElement` 里填充这些字段

---

## 1. 修改 UI_StatElement.cs

在现有 `attackText` 和 `hpText` 基础上追加以下字段。**不动现有字段，不改 `lastAttack/lastHP/lastSeenFrame`。**

```csharp
// 现有字段保留不动：
// public TextMeshProUGUI attackText;
// public TextMeshProUGUI hpText;
// public int lastAttack = -1;
// public int lastHP = -1;
// public int lastSeenFrame;

// === 新增字段 ===

[Header("新增：属性数值")]
public TextMeshProUGUI defText;
[System.NonSerialized] public int lastDef = -1;

public TextMeshProUGUI moveText;
[System.NonSerialized] public int lastMove = -1;

public TextMeshProUGUI rangeText;
[System.NonSerialized] public int lastRange = -1;

[Header("新增：能量")]
public Slider energyBar;
public Image energyBarFill;
[System.NonSerialized] public int lastEnergy = -1;
[System.NonSerialized] public bool lastEnergyFull;

[Header("新增：元素图标")]
public Image innateElementIcon;    // 所属元素
public Image affixedElementIcon;   // 当前附着元素
[System.NonSerialized] public ElementType lastInnateElement = (ElementType)(-1);
[System.NonSerialized] public ElementType lastAffixedElement = (ElementType)(-1);

[Header("新增：装备图标")]
public Image[] equipIcons; // 长度=3
[System.NonSerialized] public int lastEquipCount = -1;

[Header("新增：状态图标容器")]
public Transform statusContainer;  // 子物体由代码动态创建，HorizontalLayoutGroup
public GameObject statusIconPrefab; // 单个状态图标 Prefab（只含 Image）
[System.NonSerialized] public int lastStatusHash; // 用于判断状态是否变化
```

新增一个统一的 `ResetCache()` 方法供对象池 Get 时调用（在 `UI_PieceStatsDisplay.actionOnGet` 里调）：

```csharp
public void ResetCache()
{
    lastAttack = -1;
    lastHP = -1;
    lastDef = -1;
    lastMove = -1;
    lastRange = -1;
    lastEnergy = -1;
    lastEnergyFull = false;
    lastInnateElement = (ElementType)(-1);
    lastAffixedElement = (ElementType)(-1);
    lastEquipCount = -1;
    lastStatusHash = 0;
}
```

---

## 2. 修改 UI_PieceStatsDisplay.cs

### 2.1 加两个字段

```csharp
[Header("图标映射")]
[SerializeField] private ElementIconMap elementIconMap;
[SerializeField] private StatusIconMap statusIconMap;
```

Awake 中加载：
```csharp
if (elementIconMap == null)
    elementIconMap = Resources.Load<ElementIconMap>("ElementIconMap");
if (statusIconMap == null)
    statusIconMap = Resources.Load<StatusIconMap>("StatusIconMap");
```

### 2.2 actionOnGet 中加 ResetCache

```csharp
actionOnGet: el =>
{
    el.gameObject.SetActive(true);
    el.transform.SetAsLastSibling();
    el.ResetCache(); // ← 加这一行，替换旧的 lastAttack/lastHP 手动重置
},
```

### 2.3 修改 UpdateElement 方法

把现有方法末尾的文本更新块替换为完整的新逻辑：

```csharp
private void UpdateElement(UI_StatElement el, PieceModel piece)
{
    // ===== 下面这段：世界坐标→屏幕定位 =====
    // （现有的定位逻辑完全不动，照原样保留）
    Vector3 screenPos = _cam.WorldToScreenPoint(piece.View.transform.position);
    if (screenPos.z <= 0f)
    {
        if (el.gameObject.activeSelf) el.gameObject.SetActive(false);
        return;
    }
    if (!el.gameObject.activeSelf) el.gameObject.SetActive(true);

    var canvasCam = _canvas != null ? _canvas.worldCamera : null;
    RectTransformUtility.ScreenPointToLocalPointInRectangle(container, screenPos, canvasCam, out Vector2 localPos);
    localPos += screenOffset;
    el.Rect.anchoredPosition = localPos;

    // ===== 下面是新加的：填充所有字段 =====

    // 攻击（现有，保留）
    int atk = piece.EffectiveAttack;
    if (el.lastAttack != atk)
    {
        el.attackText.text = atk.ToString();
        el.lastAttack = atk;
    }

    // HP（现有，保留）
    int hp = piece.CurrentHP;
    if (el.lastHP != hp)
    {
        el.hpText.text = hp.ToString();
        el.lastHP = hp;
    }

    // 防御（新增）
    if (el.defText != null)
    {
        int def = piece.EffectiveDefense;
        if (el.lastDef != def)
        {
            el.defText.text = def.ToString();
            el.lastDef = def;
        }
    }

    // 移动范围（新增）
    if (el.moveText != null)
    {
        int mv = piece.MoveRange;
        if (el.lastMove != mv)
        {
            el.moveText.text = mv.ToString();
            el.lastMove = mv;
        }
    }

    // 攻击范围（新增）
    if (el.rangeText != null)
    {
        int range = piece.AttackRange;
        if (el.lastRange != range)
        {
            el.rangeText.text = range.ToString();
            el.lastRange = range;
        }
    }

    // 能量条（新增）
    if (el.energyBar != null && piece.Energy != null)
    {
        if (!el.energyBar.gameObject.activeSelf)
            el.energyBar.gameObject.SetActive(true);

        int cur = piece.Energy.CurrentEnergy;
        int max = piece.Energy.MaxEnergy;
        if (el.lastEnergy != cur)
        {
            el.energyBar.maxValue = max;
            el.energyBar.value = cur;
            el.lastEnergy = cur;
        }
        bool full = piece.Energy.IsFull;
        if (el.lastEnergyFull != full)
        {
            el.energyBarFill.color = full
                ? new Color(1f, 0.84f, 0f)   // 满能金色
                : new Color(0.27f, 0.53f, 1f); // 未满蓝色
            el.lastEnergyFull = full;
        }
    }
    else if (el.energyBar != null)
    {
        if (el.energyBar.gameObject.activeSelf)
            el.energyBar.gameObject.SetActive(false);
    }

    // 元素图标（新增）
    if (el.innateElementIcon != null && elementIconMap != null)
    {
        var elem = piece.Data?.innateElement ?? ElementType.None;
        if (el.lastInnateElement != elem)
        {
            var spr = elementIconMap.GetIcon(elem);
            el.innateElementIcon.sprite = spr;
            el.innateElementIcon.gameObject.SetActive(spr != null);
            el.lastInnateElement = elem;
        }
    }

    if (el.affixedElementIcon != null && elementIconMap != null)
    {
        bool has = piece.AffixedElement != ElementType.None && piece.AffixedElementDuration > 0;
        var aff = has ? piece.AffixedElement : ElementType.None;
        if (el.lastAffixedElement != aff)
        {
            var spr = has ? elementIconMap.GetIcon(aff) : null;
            el.affixedElementIcon.sprite = spr;
            el.affixedElementIcon.gameObject.SetActive(spr != null);
            el.lastAffixedElement = aff;
        }
    }

    // 装备图标（新增）
    if (el.equipIcons != null && el.equipIcons.Length == 3)
    {
        int equipCount = 0;
        for (int i = 0; i < 3; i++)
        {
            bool hasEquip = i < piece.EquippedItems.Length && piece.EquippedItems[i] != null;
            if (hasEquip) equipCount++;
            if (el.equipIcons[i] == null) continue;

            if (hasEquip)
            {
                var icon = piece.EquippedItems[i].Data?.icon;
                el.equipIcons[i].sprite = icon;
                el.equipIcons[i].color = Color.white;
            }
            else
            {
                el.equipIcons[i].sprite = null;
                el.equipIcons[i].color = new Color(0.5f, 0.5f, 0.5f, 0.3f);
            }
        }
        // 装备数量变了才重建状态图标（因为可能会影响状态），这里只做轻量更新
        el.lastEquipCount = equipCount;
    }

    // 状态图标（新增）
    if (el.statusContainer != null && el.statusIconPrefab != null && statusIconMap != null)
    {
        int hash = HashStatus(piece);
        if (el.lastStatusHash != hash)
        {
            // 清除旧图标
            for (int i = el.statusContainer.childCount - 1; i >= 0; i--)
                Destroy(el.statusContainer.GetChild(i).gameObject);

            // 按条件创建新图标
            ShowStatusIf(el, "DefDown", piece.CurrentDefenseReduction > 0);
            ShowStatusIf(el, "DefUp",   piece.TemporaryDefenseBonus > 0);
            ShowStatusIf(el, "Frozen",  piece.IsFrozen);
            el.lastStatusHash = hash;
        }
    }
}

// 辅助方法（放在类内）
private void ShowStatusIf(UI_StatElement el, string key, bool condition)
{
    if (!condition) return;
    var spr = statusIconMap.GetIcon(key);
    if (spr == null) return;
    var go = Instantiate(el.statusIconPrefab, el.statusContainer);
    go.GetComponent<Image>().sprite = spr;
    go.SetActive(true);
}

private static int HashStatus(PieceModel p)
{
    int h = 0;
    if (p.CurrentDefenseReduction > 0) h |= 1;
    if (p.TemporaryDefenseBonus > 0) h |= 2;
    if (p.IsFrozen) h |= 4;
    return h;
}
```

---

## 3. 修改 UI_PieceStatsDisplay.actionOnGet（补充）

```csharp
actionOnGet: el =>
{
    el.gameObject.SetActive(true);
    el.transform.SetAsLastSibling();
    el.ResetCache(); // 替换原来的 lastAttack=-1/lastHP=-1
},
```

---

## 4. 修改 UI_StatElement Prefab（你在 Editor 中做）

选中有 `UI_StatElement` 脚本的 Prefab，在它的子物体中追加：

```
StatElement (现有结构保持不变)
├── attackText (现有)
├── hpText (现有)
├── defText (新增 TMP，字号同 hpText) ← 右键 → UI → Text - TMP
├── moveText (新增 TMP) ← 同上
├── rangeText (新增 TMP) ← 同上
├── energyBar (新增 Slider) ← Hierarchy 右键 → UI → Slider
│   (删掉 Handle Slide Area，Fill 色蓝色，Background 深蓝，高 4px 宽 60px)
├── innateElementIcon (新增 Image，16×16)
├── affixedElementIcon (新增 Image，16×16)
├── equipSlot0/1/2 (新增 3 个 Image，14×14)
└── statusContainer (新增 Empty + HorizontalLayoutGroup)
```

Prefab 改完后，回到 `UI_PieceStatsDisplay` Inspector，把所有新增字段拖好。

---

## 5. 关于前面错误方案的清理

之前提到的 `PieceInfoPanel` Prefab、`PieceView.pieceInfoPanelPrefab` 字段、`BattleView` 中 ShowInfoPanel/HideInfoPanel 调用——**如果 Trae 已经写入了代码，全部回退**。如果还没写，就不要加。只按本修补方案改 `UI_StatElement` + `UI_PieceStatsDisplay.UpdateElement`。
