# 阶段②被动框架 2.1 修复提示词（发给 Trae）

## 唯一需要修的点

`Assets/Game/Equipment/Effects/PassiveFactory.cs` 里的参数类 `ExtraDamageParams` 缺少 `[System.Serializable]` 特性。

**问题**：Unity 的 `JsonUtility.FromJson<T>()` 只反序列化标记了 `[Serializable]` 的类型。现在 `ExtraDamageParams` 没标，会导致 `amount` 反序列化失败、永远拿到默认值 0（甚至运行时异常）。这是 2.2 阶段所有靠 `jsonParams` 传参的被动（死亡之刃 +10 攻、荆棘刺甲反伤、死亡之蔑延迟伤害 50% 等）都会踩的坑，必须在 2.2 开工前修掉。

**要求**：
1. 给 `ExtraDamageParams` 加上 `[System.Serializable]`（对标 `PieceManager.cs` 里 `FireSlashParams` 的既有写法）。
2. 只改这一处，其他任何文件都不要动。

**验证**：改完后临时在 `ExtraDamagePassive` 构造函数里打一行日志，确认从 JSON 读到的 `amount` 不是 0。
