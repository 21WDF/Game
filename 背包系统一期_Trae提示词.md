# 背包系统一期 — Trae 提示词

> 铁律：本提示词只写「改动方向 + 效果目标 + 红线」，不写任何实现细节（不加方法名建议、不写算法步骤、不给伪代码）。实现方式由你决定，我只验收结果。

## 〇、背景与目标

当前装备有背包但交互是「按钮点击弹窗」，道具是「购买即用、无库存」。本期的目标是：给道具引入库存，把「装备 + 道具」统一进一个**常驻侧栏**（金铲铲式，屏幕一侧），用点击式交互跑通完整数据链路。**拖拽与双击交互是二期，本期不做。**

## 一、现状（框架事实，帮你理解问题本质）

1. **道具无库存**：`GridItemManager.BuyAndUse(side, data)` 是「扣金币 → 立即进瞄准 → 命中执行 / 取消退金币」。没有「持有」概念。
2. **商店道具购买调用点**：`UI_ShopPanel` 的道具 Tab 购买按钮 → `GridItemManager.BuyAndUse`。这是本期要改的入口。
3. **现有瞄准模式**：`BuildTargetingVisuals`（生成幽灵格/高亮）→ `BattleController.HandleTileClick` 委托 → `HandleTargetClick` 命中执行 / `CancelTargeting` 取消（**退金币**）。这套瞄准/命中/执行逻辑**应原样复用**，只换「来源」。
4. **装备已有背包**：`EquipmentManager._backpack`（双方各一个）+ `EquipToPiece` / `UnequipFromPiece` / `GetBackpack(side)` + `OnEquipmentChanged` 事件。侧栏直接复用。
5. **「需要配合棋子的道具」已有现成判据**：`GridItemData.requiresSelectedPiece`（传送石=true，扩展石/删除石=false）。一期点击式下，它语义 = 「使用前需先选中己方棋子」。
6. **现有装备面板 `UI_EquipmentPanel`**：E 键开合的弹窗，按钮式「装备/卸下」。本期用常驻侧栏**替换**它。
7. **常驻侧栏与现有「面板开闭计数」的冲突（关键坑）**：`InputHandler` 有 `RegisterPanelOpen/Close` 计数机制，任何面板打开都会让 `IsAnyPanelOpen` 为 true，从而**全局屏蔽棋盘点击/悬停/大招键**。常驻侧栏如果走这套计数，棋盘会被永久锁死。所以侧栏**必须不调用** `RegisterPanelOpen`，但仍需能挡住「鼠标在侧栏上时点击穿透到棋盘」。

## 二、改动方向

### 1. 道具库存（数据层）

- 给道具引入「持有列表」（按玩家分组），并给出「进背包」和「从背包消耗」两个方向的能力。
- 商店购买道具：从「购买即用」改为「购买进背包」——扣金币后入背包，**不再立即进入瞄准模式**。
- 「使用」动作：从背包移除一个道具实例 → 进入瞄准模式（复用现有瞄准/命中/执行逻辑）→ 命中消耗 + 扣 AP → **取消时退回背包**（不消耗，等价于现在「取消退金币」的语义）。
- 需要能查询某方当前持有哪些道具（供侧栏展示）。

### 2. 商店道具购买改造

- 商店道具 Tab 的购买按钮，从「买即用」改成「买进背包」。
- 购买后商店 Tab 与金币显示照常刷新；道具本身不出现在商店里变少（商店列表是固定资产，不进背包减库存），只是玩家的持有变多。

### 3. 背包侧栏 UI（常驻，替换旧装备面板）

- 屏幕一侧常驻一个侧栏，展示**当前活动玩家**的：装备背包 + 道具背包。
- 装备项：点击「装备」→ 装备到**当前选中的己方棋子**（复用现有装备到棋子的能力；未选中棋子时按钮置灰）。侧栏也应能看到已装备状态或至少能触发装备动作。
- 道具项：点击「使用」→ 从背包消耗并进入瞄准；`requiresSelectedPiece` 的道具在未选中己方棋子时置灰/给出提示。
- 侧栏内容随「当前活动玩家」切换（装备/道具都按 `TurnManager.ActivePlayer` 取），回合切换时刷新。
- 侧栏**对战阶段（Playing）显示、战前（选棋子/部署等）隐藏**（战前尚无背包内容）。
- 旧 `UI_EquipmentPanel` 的「E 键开合弹窗」入口不再需要；侧栏取代它的职能。

## 三、效果目标（验收标准）

1. 商店买道具 → 金币扣除、道具进背包，**不立即进入瞄准模式**。
2. 常驻侧栏显示当前活动玩家的装备与道具（每件道具独立占一格，不堆叠）。
3. 侧栏点装备「装备」→ 选中己方棋子时装备成功（进棋子槽位）；未选中时按钮置灰。
4. 侧栏点道具「使用」→ 道具从背包移除 → 进入瞄准 → 命中目标执行效果 + 扣 AP → 道具被消耗。
5. 道具瞄准中途取消（点非目标/右键等）→ 道具**退回背包**，不消耗、不扣 AP。
6. 回合切换后，侧栏内容自动切换为新的活动玩家。
7. 侧栏常驻可见，但**不屏蔽棋盘**——鼠标在棋盘区域时点击/悬停/大招照常，仅在鼠标位于侧栏上时挡穿透。
8. `requiresSelectedPiece` 的道具：未选中己方棋子时「使用」按钮置灰或提示。
9. 对战阶段外（战前）侧栏隐藏。

## 四、红线（绝不能破坏）

1. **常驻侧栏不得走 `RegisterPanelOpen` 面板开闭计数**——否则棋盘被全局锁死（这是本期最容易翻车的点）。
2. **装备的 `EquipToPiece` / `UnequipFromPiece` 及被动 OnEquip/OnUnequip 逻辑零改动**——侧栏只是调用，不改装备核心。
3. **道具瞄准模式（生成瞄准视觉 / 命中执行 / 取消清理）零改动**——只换「来源」，不动瞄准/执行本身。
4. **商店装备购买（`BuyEquipment`）零改动**——本期只改道具购买。
5. **拍卖、地下交易（目前只卖装备）零改动**。
6. **伤害管道、元素系统、护盾、城邦过滤、回合/轮结算零改动**——本期纯 UI + 道具库存数据层。
7. 道具「取消退回背包」与「命中消耗」的语义要等价于现状的「取消退金币 / 命中扣 AP」，不要出现道具凭空消失或凭空多出的情况。

## 五、明确不做（二期及以后）

- 拖拽（装备拖到棋子装备、道具拖到棋子使用）。
- 双击直接使用。
- 道具堆叠（本期每件占一格）。
- 道具库存的持久化 / 存档。
