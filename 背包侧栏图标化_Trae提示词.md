# 背包侧栏图标化_Trae提示词.md

> 铁律：本提示词只写「改动方向 + 效果目标 + 红线」，不写任何实现细节（不加方法名建议、不写算法步骤、不给伪代码）。实现方式由你决定，我只验收结果。

## 〇、背景与目标

背包侧栏（一期已做：道具库存 + 商店买道具进背包 + 常驻侧栏）当前是「文字列表项 + 按钮」式。本期把它改造成**云顶之弈式的「图标格子」**：装备和道具都直接摆图标，单击选中高亮、悬停显示 tooltip、双击道具使用。**拖拽给棋子装备/使用是下一步，本期不做。**

## 一、现状（框架事实）

1. **侧栏 `UI_BackpackSidebar` 当前用 `UI_EquipmentItemRefs`**（文字列表项：nameText / statsText / iconImage / actionButton / buttonText），「装备/卸下/使用」都是按钮点击。本期要换成「图标格子」。
2. **装备有 icon、道具没有**：`EquipmentData` 有 `icon`（Sprite）；`GridItemData` 目前没有 icon 字段，需要加（否则道具没法图标化）。
3. **项目零 tooltip 组件、零双击代码**——悬停浮层和双击检测都要从零做（Unity 双击可用 `IPointerClickHandler` 的 `clickCount` 判断）。
4. **道具「使用」已就绪**：`GridItemManager.UseFromInventory(side, data)`（移出背包 → 进瞄准 → 命中消耗 / 取消退回），双击只需调用它。
5. **装备「装备/卸下」已就绪**：`EquipmentManager.EquipToPiece` / `UnequipFromPiece`。但本期**不调用**（装备动作等拖拽下一步）。
6. **「需指定棋子的道具」已有现成判据**：`GridItemData.requiresSelectedPiece`（传送石=true）。
7. **侧栏的常驻约定（延续一期红线）**：不调用 `InputHandler.RegisterPanelOpen/Close`（否则常驻面板锁死棋盘），穿透阻挡靠 `UIPanelRaycastBlocker`；内容随活动玩家切换（订阅 `TurnModel.OnTurnStarted`），战前隐藏、游戏结束隐藏。

## 二、改动方向

### 1. 道具图标字段

- 给道具数据补一个图标字段（供侧栏图标格子显示；asset 图标由用户后续配置）。

### 2. 侧栏图标格子化

- 侧栏的「装备背包区」和「道具背包区」从文字列表项改成**图标格子**（TFT 式，图标横向/网格排布）。
- 「选中棋子的装备槽位区」保留**图标展示**（显示已装备的图标 + tooltip），但「卸下」动作本期移除（卸下归拖拽下一步）。
- 装备图标：**只展示 + tooltip，无装备动作**（等拖拽）。
- 道具图标：展示 + tooltip + **双击使用**。

### 3. 图标格子交互（单击选中 + 悬停 tooltip + 双击）

- **单击图标**：高亮选中（视觉反馈，如选中框）。
- **悬停图标**：显示 tooltip——装备显示「名称 + 属性」；道具显示「名称 + 描述 + AP 消耗」。（属性/描述文案可复用 `UI_ShopPanel` 里现成的构建方法。）
- **双击道具图标**：使用该道具（进入瞄准模式）。需要指定棋子的道具（`requiresSelectedPiece`），在未选中己方棋子时双击应给出提示/不触发。
- **双击装备图标**：本期无动作（装备只能拖拽，下一步做）。
- 单击与双击的时序：单击=高亮（无害）、双击=使用，双击过程中先触发一次单击高亮再触发双击使用是可接受的，不必为此引入复杂延迟判定。

### 4. 移除本期不做的交互

- 装备的「装备」按钮、槽位的「卸下」按钮，本期一并移除（由拖拽下一步接管）。

## 三、效果目标（验收标准）

1. 侧栏装备、道具均以**图标格子**展示（不是文字列表项），道具图标读取新加的 icon 字段。
2. 单击图标 → 高亮选中。
3. 悬停图标 → 出现 tooltip：装备显示名称 + 属性，道具显示名称 + 描述 + AP 消耗；移开隐藏。
4. 双击道具图标 → 道具从背包移除进入瞄准（命中消耗+扣 AP / 取消退回），行为与一期「使用按钮」一致。
5. 双击装备图标 → 无动作。
6. 装备图标、已装备槽位图标 → 只展示 + tooltip，无可点击的装备/卸下动作。
7. `requiresSelectedPiece` 的道具在未选中己方棋子时双击 → 提示或不触发（不崩溃、不误用）。
8. 侧栏仍：常驻可见、不屏蔽棋盘（不调面板开闭计数）、随活动玩家切换、战前隐藏、游戏结束隐藏。

## 四、红线（绝不能破坏）

1. **侧栏仍不得调用 `RegisterPanelOpen/Close`**——棋盘点击/悬停/大招键不能被锁死（延续一期红线）。
2. **道具使用 `UseFromInventory` 零改动**——双击只是调用，不改瞄准/命中/取消逻辑。
3. **装备 `EquipToPiece` / `UnequipFromPiece` 及被动 OnEquip/OnUnequip 零改动**——本期不调用，仅不删除这些方法。
4. **商店、拍卖、地下交易、伤害管道、元素系统、护盾、城邦过滤零改动**。
5. **道具 icon 字段是纯数据字段**——不影响道具购买/使用/库存的任何现有逻辑。
6. 侧栏内容随活动玩家切换、战前隐藏、游戏结束隐藏的既有行为保持不变。

## 五、明确不做（下一步及以后）

- **拖拽**（装备拖到棋子装备、道具拖到棋子使用）——这是下一步，本期不做。
- 装备/卸下动作（由拖拽接管，本期移除）。
- tooltip 的拖拽跟随、图标拖拽幽灵等与拖拽相关的东西。
- 道具堆叠（仍每件占一格）、存档。
