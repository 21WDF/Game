# 季风之城三期A（配置结构重构）— Trae 提示词

> 铁律：只写「改动方向 + 效果目标 + 红线」，不写实现细节。

## 〇、背景（一句话）

三期气候复杂组（飓风/雷暴）的前置重构：把二期 `ClimateEntry.value` 这个「一字段多用」的权宜字段拆掉，改成**各气候专属参数区块**，消除「简单气候背一堆无用参数」的问题。本期**只重构、不新增任何气候**，二期暴雨/暴雪/高温行为零变化。

## 一、现状（框架事实）

1. `MonsoonConfig.ClimateEntry` 现有字段：`climateName` / `probability` / `restrictedSeasonIndex` / `effectKind` / `value` / `exclusive`。其中 **`value` 一字段多用**：暴雨=攻击距离减量、暴雪=击退格数、高温未用。
2. `MonsoonManager` 里 value 的两个消费点：
   - `GetAttackRange`（暴雨）：累加 `c.entry.value` 作为攻击距离减量，`Mathf.Max(0, ...)`；
   - `OnAttackLanded`（暴雪）：累加 `c.entry.value` 作为击退格数，`Mathf.Max(1, ...)`。
3. `ClimateEffectKind` 现有枚举：`None / Rain / Blizzard / Heat`（本期**不追加**飓风/雷暴，留三期B/C）。
4. `MonsoonConfig` 已有 `seasons` / `daySlot` / `nightSlot` / `climates` 等区块，以及 `EffectSlot` 带参+无参构造的先例。
5. 常驻红线：气候「是否生效」内聚在 `MonsoonManager`，判断不散落到调用点。

## 二、改动方向

1. **`ClimateEntry` 通用化**：删除 `value` 字段，只保留 `climateName` / `probability` / `restrictedSeasonIndex` / `effectKind` / `exclusive` 这五个通用字段。
2. **新增各气候专属参数区块（MonsoonConfig 独立字段）**：
   - 暴雨参数：攻击距离减量（默认 1）。
   - 暴雪参数：击退格数（默认 1）。
   - 高温参数：无额外数值（高温语义是「行动后锁定」，本就不需要数值）。
   - （飓风/雷暴参数留三期B/C 再加，本期不建。）
3. **迁移 value 消费点**：`GetAttackRange` 改从「暴雨参数」读攻击距离减量、`OnAttackLanded` 改从「暴雪参数」读击退格数；语义与数值完全不变（只是数据来源从 `entry.value` 换成专属区块）。
4. **保持数据驱动**：专属参数区块的字段默认值仅作初始值，Inspector 可改，代码零硬编码。

## 三、效果目标（验收标准）

1. 二期暴雨/暴雪/高温**行为完全不变**：暴雨仍是「上回合受击者攻击距离 -1（下限 1）」、暴雪仍是「攻击后击退 1 格」、高温仍是「行动后锁定」——数值与语义零变化。
2. `ClimateEntry` 不再有 `value` 字段；暴雨/暴雪的数值从各自专属区块读取。
3. 专属参数区块字段在 Inspector 可调，改数值后运行时生效。
4. 非季风城邦下仍全部不生效；气候判定/持续/叠加/独占逻辑不变。
5. 四季昼夜、伤害管道、元素、护盾、贸易、城邦选择/过滤、背包系统等零改动。

## 四、红线（绝不能破坏）

1. **二期行为不变**：本次是「搬位置」重构，暴雨/暴雪/高温的效果数值、叠加/独占语义、时间单位（按轮）——全部零变化。
2. **内聚过滤不变**：气候「是否生效」仍内聚在 `MonsoonManager`，不散落到调用点。
3. **数据驱动**：专属参数区块字段默认值仅作初始值，代码零硬编码。
4. **不新增气候**：本期只重构，不追加飓风/雷暴枚举、不实现任何新气候效果（留三期B/C）。
5. 四季昼夜、伤害管道、元素反应、护盾、贸易、城邦选择/过滤、背包系统零改动。

## 五、明确不做

- 飓风（幸运方块）、雷暴（雷电残留/永久加成）的任何实现（三期B/C）。
- ClimateEffectKind 枚举追加（留三期B/C）。
- 任何新棋盘实体、新属性系统。
