# 项目交接报告 01 — Chaotic Chess

> 用途：交给下一个 AI 助手，快速接手本项目继续开发。
> 交接日期：2026-08-20
> 项目路径：`D:/unity/Project/Chaotic Chess`
> 协作工作区（文档/脚本）：`C:/Users/MECHREVO/WorkBuddy/2026-07-27-23-05-25`

---

## 一、项目概览

| 项 | 内容 |
|---|---|
| 项目类型 | Unity 6 Hex 战棋（六边形棋盘，1v1，每方 7 棋子） |
| 技术栈 | MVC 分层 + URP/Lit + ScriptableObject 配置化 + DOTween v1.3.030 |
| 棋子模型 | 菲林斯 VRoid 模型（pivot 在脚底） |
| 规模 | 约 83+ `.cs` / 64 `.asset` / 18 `.prefab` / 14+ MVC 模块 |
| 当前状态 | 单机可玩；装备系统（22 件）完整落地；进入 playtest 数值调优阶段 |

**核心玩法循环**：选棋子 → 部署 → 移动/攻击（消耗 AP）→ 触发四元素反应（火/水/雷/冰）→ 装备被动与城邦效果生效 → 金币经济 → 回合结算 → 一方全灭判负。

---

## 二、项目演进历程

| 阶段 | 内容 |
|---|---|
| Phase 1 | 原型：基础棋盘、棋子移动/攻击 |
| Phase 2 | 完整系统（5 个 Sprint）：元素城邦、装备、大招、棋盘道具、7 棋子对战 |
| Phase 2 框架优化 | 视觉/交互闭环：移动动画、伤害跳字、选中发光、已行动变灰、Range Provider、元素附着量、浮动文字参数化 |
| 装备系统阶段① | 装备数据层 + 回蓝回血 + 商店 Tab 分类（初级/中级/高级/城邦/道具） |
| 装备系统阶段② | 被动框架：事件驱动钩子 + PassiveFactory 工厂 + 22 件装备被动（分 A/B/C 三批） |
| 装备系统阶段③ | 元素反应修饰层：IReactionModifier + 光界之力 + 四元素之力 |
| 伤害类型系统 | DamageKind 三分类（物理/魔法/真实）+ DamageSource 来源位掩码 |
| 伤害跳字补全 | 所有伤害路径（DoT/延迟/附加/反伤/爆炸/溅射）都有跳字，颜色按类型 |

---

## 三、技术架构

### 3.1 MVC 分层
- **Model 层全部纯 C#**（无 UnityEngine 引用）：`TurnModel`/`BattleModel`/`GoldModel`/`APModel`/`EnergyModel`/`PieceModel`/`EquipmentModel`/`ChessBoardModel`，可在 NUnit 直接单测。
- **Controller 层**：`PieceManager`（伤害/死亡结算核心）、`TurnManager`（回合/DoT/减防递减）、`BattleController`（交互/AP/溅射/大招瞄准）、`EquipmentManager`（购买/装备/卸下）、`GoldManager`、`GridItemManager`。
- **View 层**：`PieceView`、UI 面板（`UI_ShopPanel`/`UI_EquipmentPanel`/`UI_EnergyBar` 等）、`FloatingTextPool`（跳字对象池）。

### 3.2 三种效果工厂（统一模式）
`IPassiveEffect`（被动）、`IUltimateEffect`（大招）、`IGridItemEffect`（道具）都遵循同一套模式：
**接口 + className 字符串 + 工厂 switch + ScriptableObject 配置**。新增效果只需：1 个实现类 + 1 个 switch case + 1 行 `jsonParams`。

### 3.3 被动框架（阶段②核心成果）
- `IPassiveEffect` 用 **C# 默认接口方法**提供事件钩子，具体被动按需覆写：
  - `OnEquip` / `OnUnequip`（生命周期）
  - `OnTurnStart`（回合开始，双方棋子每次回合开始都触发）
  - `OnAttacked`（被攻击，无论是否减血）
  - `OnDamageReceived`（实际减血，带 source + kind 参数）
  - `OnDamageDealt`（造成伤害，带 target）
  - `OnKill`（击杀）/ `OnAllyDeath`（己方棋子死亡）
  - `UniqueId` 默认属性（唯一被动 id，null = 非唯一）
- `PassiveFactory.Create(className, jsonParams, uniqueId)` 静态工厂，装备被动与棋子内建被动共用。
- `IReactionModifier.Modify(reaction)` 元素反应修饰接口（光界之力/四元素之力实现）。

### 3.4 伤害类型三分类（重要，新设计）
```
enum DamageKind { Physical, Magical, True }
[Flags] enum DamageSource { Attack=1, Splash=2, Ultimate=4, Dot=8, Reflect=16 }
```
两个维度独立并存：
- **DamageKind（类型）** 决定「受不受防御减伤、反甲反不反」：
  - Physical 物理（受防御减伤、触发反甲）：普攻/大招/溅射/延迟伤害
  - Magical 魔法（不受减伤、不触发反甲）：DoT/反伤/超载爆炸
  - True 真实（不受减伤、不触发反甲，未来无视护盾）：真实附加伤害
- **DamageSource（来源）** 位掩码，标记伤害从哪条路径来。

**反甲**：只反 Physical，反出 Magical（天然防递归）。
**减伤被动**：默认全类型，可加 `filter` 参数筛选（All/Physical/Magical/True）。
**护盾机制**：尚未实现，魔法 vs 真实的差异靠未来护盾区分（和城邦机制挂钩）。

---

## 四、伤害结算链（最关键，接手必读）

### 4.1 伤害介入顺序（普攻路径 AttackPiece + 统一入口 ApplyIncomingDamage 都遵循）
```
伤害计算（元素反应 CalculateWithReaction）
→ 附加/减伤（物理减伤被动；附加伤害已独立成段，不在此混入）
→ 首伤=1（中娅悖论 FirstDamageToOnePassive）
→ 死亡之蔑拆分（DeathDancePassive.SplitIncomingDamage，拆延迟部分）
→ 扣血 TakeDamage
→ 免死（中娅悖论 DeathDefyPassive.TryDefyDeath）
→ 死亡判定 / DestroyPiece
```

### 4.2 统一伤害入口 `ApplyIncomingDamage(target, damage, source, damageType, damageKind)`
所有「非普攻」伤害路径统一走这里：DoT、反伤、超载爆炸、非元素溅射、大招。内部：OnAttacked 通知 → 减伤被动 → 首伤=1 → 延迟拆分 → 扣血 → 免死 → OnDamageReceived 通知 → 跳字。

### 4.3 附加伤害独立成段 `ApplyExtraDamageSegments`
附加伤害（ExtraDamagePassive）**不再混入基础物理伤害**，而是在基础伤害扣血后，按 kind 分物理/魔法/真实三份独立结算：
- 物理附加：先吃防御减伤（`max(1, extra - EffectiveDefense)`）再吃物理减伤
- 魔法/真实附加：不吃防御，吃各自类型减伤
- 每份独立扣血 + 免死 + OnDamageReceived（正确 kind）
- 不触发：OnAttacked / OnDamageDealt（吸血）/ 元素反应 / 金币 / 能量 / 跳字
- 附加段致死，击杀者仍是攻击者（末尾统一 DestroyPiece）

### 4.4 其他关键结算点
- **元素附着**：`AttackPiece` 中 `triggerGauge = 1 + GetExtraElementGauge(attacker, elem)`（元素之力加成）。
- **超载爆炸**：`ApplyOverloadExplosion`，爆炸中心在击退前坐标（已修正），快照收集周围敌方，`ApplyIncomingDamage(Splash, Magical)`。
- **超导减防**：`DefenseReductionTurnsRemaining` 按剩余回合递减（`TickElementDuration`），基础 1 回合、元素之力 +2 回合。
- **回合开始**：`TurnManager` 依次做 DoT 结算 → 减防递减 → 临时防御重置 → 变灰重置 → OnTurnStart 通知。

---

## 五、22 件装备清单（16 个被动类）

### 5.1 通用装备（A 批 6 件）
| 被动类 | 装备 | 参数（jsonParams） | 机制 |
|---|---|---|---|
| ExtraDamagePassive | 附加伤害 | `amount`, `kind`(默认 True) | 攻击追加独立伤害，按类型结算 |
| LifestealPassive | 饮血剑 | `baseHeal`, `percent` | 吸血 = baseHeal + floor(伤害×percent%) |
| DamageReductionPassive | 减伤 | `amount`, `filter`(默认 All) | 受击伤害 -amount，按类型筛选 |
| RegenPassive | 生命铠甲 | `baseAmount`, `upgradedAmount`, `upgradeThreshold` | 每回合回血，连续未受伤 N 回合进化（计时记实例） |
| ThornsPassive | 荆棘刺甲 | `amount`, `percent`, `affectedByDefense`, `reflectRadius`, `alwaysReflectAttacker` | 只反物理，反出魔法，可配置范围/防御减伤 |
| KillAttackPassive | 死亡之刃 | `amount` | 击杀后攻击永久 +amount（叠层记实例 Accumulated） |

### 5.2 通用装备（B 批 5 件）
| 被动类 | 装备 | 参数 | 机制 |
|---|---|---|---|
| StoneSkinPassive | 石像鬼板甲 | `defenseBonus`, `timerTurns`, `triggerOnNoDamage` | 被「不同敌人」攻击才叠防，计时归零/卸下清空 |
| WitherPassive | 枯萎宝珠 | `defenseReduction`, `duration` | 攻击命中给目标减防 debuff（TempBuff） |
| SpiritBannerPassive | 英灵幡 | `instantEnergy`, `regenEnergyPerTurn`, `durationTurns` | 己方死亡立刻回蓝 + 持续回蓝 |
| FreeAPPassive | 探索者护臂 | `freeType:"Move"`, `cooldownTurns`, `gainEnergy` | 移动完全免 AP，4 回合冷却 |
| FreeAPPassive | 诸葛连弩 | `freeType:"Attack"`, `cooldownTurns`, `gainEnergy` | 攻击连射（解除每回合一次限制）+ 免 AP 冷却 |

### 5.3 通用装备（C 批 3 件 = 4 个被动类）
| 被动类 | 装备 | 参数 | 机制 |
|---|---|---|---|
| DeathDancePassive | 死亡之蔑 | `delayPercent`, `delayTurns`, `realTimeDamage` | 受击伤害延迟到后续回合平分结算，延迟期间禁止卸下 |
| FirstDamageToOnePassive | 中娅悖论（首伤=1） | `cooldownTurns` | 冷却内受击伤害降为 1 |
| DeathDefyPassive | 中娅悖论（免死） | `cooldownTurns` | 致命伤回 1 HP 免死一次（响应一切致死） |
| LegionAegisPassive | 军团圣盾 | `affectDirection`, `affectRange`, `bonusStat`, `bonusAmount`, `evolveTurns`, `evolved*`(4), `evolveOverrides` | 光环（查询型）+ 连续未受伤进化 + 唯一被动 |

> 中娅悖论是 **1 件装备配 2 条 PassiveConfig**（首伤=1 + 免死，各自独立冷却）。

### 5.4 元素装备（5 件 = 2 个被动类，放 `Equipment/Effects/Elemental/`）
| 被动类 | 装备 | 参数 | 机制 |
|---|---|---|---|
| ReactionBoostPassive | 光界之力 | `percent` | 元素反应倍率 +percent%（加法叠加，非唯一） |
| ElementalCorePassive | 潮涌/灼火/陨雷/凝冰之力 | `element` + 9 数值参数 | 四件共用一个类，按 element 区分；反应增强 + 元素量加成 + 超载爆炸；四件 `uniqueId` 都填 `ElementalCore`（同名唯一覆盖） |

### 5.5 唯一被动框架
- 字段在 `PassiveConfig`（被动级）：`passiveName`（展示）+ `isUnique` + `uniqueId`。
- 约束键 = `uniqueId`（不是装备名/装备序号）：同棋子同 `uniqueId` 只生效最后装备那件（先进先出，用 `EquipOrder` 时间戳判先后）。
- 不同棋子各持一件可同时生效（光环可叠加）。
- 唯一被动后续会大量引入（装备平衡手段）。

---

## 六、元素反应系统

12 条反应规则（6 种 × 2 方向，`ReactionDatabase.asset`）：
蒸发（×1.5/2）、融化（×2/1.5）、超载（×1 + 击退 1 格 + 爆炸）、感电（×0.5 + DoT 5×2 回合）、冻结（×1 + 冻结 1 回合）、超导（×0.8 + 减防 5，持续 1 回合）。

- `ElementReactionTable.GetReaction` 查反应（含 `IReactionModifier` 修饰链）。
- `CollectReactionModifiers` 收集攻击方反应修饰器。
- 元素附着量 Gauge 机制：普攻 +1、大招 +2、每回合 -1、克制方 ×2、等量消耗。

---

## 七、关键文件结构

```
Assets/Game/
├── Core/           # GameConfig、GameEnums（ElementType/ReactionType 枚举）
├── Piece/
│   ├── Model/      # PieceModel（属性聚合 Effective*、状态字段）、PieceData（SO）
│   ├── Controller/ # PieceManager（伤害/死亡结算核心，最大文件）
│   ├── View/       # PieceView
│   └── Effects/    # FireSlashUltimate、IceBarrierUltimate（大招）
├── Equipment/
│   ├── Model/      # EquipmentData（含 PassiveConfig）、EquipmentModel
│   ├── Controller/ # EquipmentManager（购买/装备/卸下 + 工厂）
│   └── Effects/    # 15 个通用被动类 + PassiveFactory + IPassiveEffect
│       └── Elemental/  # IReactionModifier + ReactionBoostPassive + ElementalCorePassive
├── Battle/Controller/ # BattleController（交互/AP/溅射/大招瞄准，第二核心文件）
├── Turn/Controller/   # TurnManager（回合/DoT/减防递减/OnTurnStart 通知）
├── Systems/       # ElementReactionTable、RuntimeReactionTable、ReactionDatabaseSO
├── Client/        # FloatingTextPool、FloatStyles、ElementColorMapper
├── Gold/ ActionPoint/ GridItem/ ChessBoard/ PieceLayout/  # 其他模块
└── Resources/     # GameConfig.asset、ReactionDatabase.asset
```

---

## 八、工作约定（重要：与原开发者协作模式）

1. **用户用 Trae（另一个 AI）执行代码修改**；本 AI 负责：设计规格 → 生成 Trae 提示词 → 审查 Trae 输出 → 生成修复提示词。
2. **提示词要求（务必遵守）**：
   - 不改项目代码，只写提示词文件让 Trae 改。
   - 只给「改动方向 + 约束」，**不写详细代码逻辑**（不写完整代码块/伪代码）。
   - 做好约束（不乱调整、不改指定文件之外的东西）。
3. **审查 Trae 输出按复杂度取舍**：简单/低风险（纯 View、机械增删）信任汇报 + 抽查关键点；复杂（多文件逻辑联动、伤害/结算/时序介入）才全量读码详查。
4. 用户偏好「一步步来」，大改动要拆分阶段。

---

## 九、残留问题 / 待办（按优先级）

### 9.1 非阻塞瑕疵（已记录，未修）
1. **超载击退 + 附加补刀致死时序**：附加伤害段放在元素副作用（击退）之后，若附加段致死且触发超载击退，目标会先被击退再死（死亡位置在击退后格子，而非原地）。罕见场景，影响轻微。
2. **延迟伤害未更新 LastDamageSource**：死亡之蔑延迟结算触发 `OnDamageReceived(Physical)` 但没更新 `owner.LastDamageSource`，若同时带反甲，反伤会打给「过时的上次攻击来源」。
3. **反伤致死攻击者仍充能**：同归于尽极端场景，已死攻击者仍 `Energy.Gain`，无害（能量作废）。

### 9.2 Phase 2 遗留（框架优化审查报告）
1. 死代码 `PieceManager.MovePiece`（唯一入口是 `MovePieceAlongPath`，建议删）。
2. `GameConfig.cs:46` Header 命名错误（金币字段挂在「战术叠加层颜色」标题下）。
3. `isRunning` 判定写法不一致（`MovePiece` 硬编码，随删死代码自然消失）。
4. 高频 `Debug.Log` 噪音（InputHandler 每次点击、PieceManager 移动/攻击、KnockbackResolver 击退循环）。

### 9.3 后续方向
1. **22 件装备实际 .asset 参数配置**（用配置生成器表格生成 jsonParams 填入）。
2. **playtest 数值平衡**（建议先打 20-30 局单机，再考虑联网）。
3. **护盾机制**（魔法 vs 真实的差异靠护盾，和城邦机制挂钩）。
4. **唯一被动 UI 联动**（Inspector 级：勾选 isUnique 才显示 uniqueId 输入框；当前只落了字段 + Tooltip）。
5. **passiveName UI 展示**（装备介绍开头显示被动名，未做）。
6. **道具/城邦/棋子内建被动填充**（模板已建，待填）。
7. **Phase 3 在线对战**（MVC 已为网络层留好注入点，但建议先调平衡）。

---

## 十、技术陷阱（避坑清单，接手必读）

1. **JsonUtility 枚举字段只认数值、不认名字**：`{"freeType":"Attack"}` 会静默落回默认值。所有枚举字段用 **string 承接 + 手动 Parse 解析**（对标 `ParseFreeAPType`/`ParseElement`/`ParseReductionFilter`）。
2. **参数类必须 `[System.Serializable]`**：否则 `JsonUtility.FromJson` 读不到字段，值恒为默认（`ExtraDamageParams` 踩过）。
3. **C# 默认接口方法 + 接口加参**：接口加参数后，旧签名实现类会**静默落到空默认实现**（反甲/再生/石像鬼会失效，编译器不报错）。改接口签名必须同步所有显式实现类（用 grep 查 `void OnDamageReceived` 等）。
4. **static 方法调实例方法**：需 `Instance.` 前缀（`ApplyOverloadExplosion` 踩过，编译报错；Unity 无实时语言服务，Trae 的"诊断无报错"不可全信）。
5. **反甲防递归**：反甲只反 Physical、反出 Magical，天然防递归（反伤不会反出反伤）。
6. **遍历活列表 + 钩子内致死/销毁**：必须先快照（`new List(pieces)` 或先收集 targets 再结算），否则 `InvalidOperationException`（DoT 致死、反伤致死、延迟伤害致死都涉及）。
7. **装备独立效果记装备实例（被动对象）**，不借棋子全局字段：生命铠甲计时、死亡之刃叠层、免 AP 冷却、石像鬼叠层等。清零语义要分清「惩罚」（损失进度，正确）vs「收益」（白嫖，bug）——只有免 AP 冷却清零是收益，已修。
8. **唯一被动按 uniqueId 判断**，先进先出（后装覆盖先装），不是装备名/装备序号。
9. **伤害介入顺序固定**：`伤害计算 → 附加/减伤 → 首伤=1 → 延迟拆分 → 扣血 → 免死 → 死亡判定`，新增伤害介入必须遵守此顺序。
10. **跳字是纯 View 层**：不改数值/结算/死亡/免死，位置取受击方 `View.transform.position`，池 null 静默跳过。
11. **Excel 公式跨平台兼容**：生成 JSON 引号用 `CHAR(34)` 函数，不用 `""` 转义，不用 IF 嵌套（WPS/腾讯文档预览不支持）。

---

## 十一、配置工具

### 被动效果配置生成器 `被动效果配置生成器.xlsx`
- 4 张表：通用被动配置生成器 / 元素被动配置生成器 / 通用被动速查 / 元素被动速查。
- 配置生成器表：黄色【填写值】列改数值 → 公式自动生成 `className` + `jsonParams`，复制到 Unity `PassiveConfig`。
- 生成脚本：工作区 `gen_passive_config.py`（managed Python venv：`C:/Users/MECHREVO/.workbuddy/binaries/python/envs/default/Scripts/python.exe`）。
- 唯一被动三字段（`isUnique`/`uniqueId`/`passiveName`）**不在 jsonParams 里**，需在 Unity Inspector 的 PassiveConfig 单独填。

### 调参入口
- `GameConfig.asset`：棋盘大小、AP、金币倍率、Overlay 颜色、浮动文字颜色/动画、移动动画时长、走跑阈值等。
- 被动数值：各装备 `.asset` 的 `PassiveConfig.jsonParams`。
- 元素反应数值：`ReactionDatabase.asset`（12 条规则）。
- 伤害类型颜色：`GameConfig.floatDamageMagical`（魔法紫）/ `floatDamageTrue`（真实金）。

---

## 十二、快速上手建议（给下一个 AI）

1. **先读**：本报告 + 项目长期记忆 `MEMORY.md` + `Assets/Game/Piece/Controller/PieceManager.cs`（伤害结算核心）+ `Equipment/Effects/IPassiveEffect.cs`（被动框架接口）。
2. **改数值**：优先走 GameConfig.asset / 被动 jsonParams / ReactionDatabase.asset，不要动代码。
3. **加新被动**：1 个实现类（覆写 IPassiveEffect 钩子）+ PassiveFactory 加 case + 参数类 `[Serializable]` + 表格加块（改 gen_passive_config.py 的 PASSIVES/LOOKUP）。
4. **加新伤害**：只需声明 `DamageKind`，反甲/减伤/跳字颜色自动按类型约束。
5. **继续开发前**：先和用户确认下一步方向（playtest 平衡 / 装备 .asset 配置 / 护盾机制 / 道具城邦），并按「用户用 Trae 执行、你出提示词」的约定协作。

---

## 附：全部提示词文件（工作区 + 项目目录）

工作区 `C:/Users/MECHREVO/WorkBuddy/2026-07-27-23-05-25/` 下存有大量历史提示词与设计文档（被动模板/装备模板/技能模板/移动模板/道具模板、Phase1/Phase2 方案与审查报告、各阶段 Trae 提示词等），需要回溯某个功能的原始设计时可直接查阅。
