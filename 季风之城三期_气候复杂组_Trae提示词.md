# 季风之城三期（气候·复杂组：飓风 + 雷暴）— Trae 提示词

> 铁律：只写「改动方向 + 效果目标 + 红线」，不写实现细节。

## 〇、背景（一句话）

季风之城气候最后一期：**飓风（幸运方块掉落物拾取）** 与 **雷暴（雷电残留 + 雷劈永久加成）**。二者都是全新「附着在格子上的棋盘实体」系统（区别于二期纯数值修正），跨系统依赖背包（已就绪）。同时借本期**重构气候配置结构**（消除「一字段多用」的无用参数问题）。

## 一、现状（框架事实）

1. **气候框架已就绪（二期）**：`MonsoonManager` 有 `TickAndRollClimates`（每轮判定 + 持续递减）、`IsClimateActive(kind)`（内聚过滤）、`RollNewClimates`（独立概率 + 独占）。气候持续轮数走全局 `climateDurationMinRounds/MaxRounds`（默认 1~3 随机）。
2. **二期 ClimateEntry 的 value 是「一字段多用」**：暴雨=攻击距离减量、暴雪=击退格数、高温未用。`MonsoonManager.GetAttackRange` / `OnAttackLanded` 里直接读 `c.entry.value`。本期要拆掉这个 value，改各气候专属参数区块。
3. **背包系统已就绪**：`GridItemManager`（道具库存 + `AddToInventory` + `OnInventoryChanged` + 商店列表）、`EquipmentManager`（装备背包 + `GrantEquipment` + `OnEquipmentChanged` + 商店列表）、`GoldManager.AddGold`、侧栏（显示/拖拽）。
4. **装备/道具字段**：`EquipmentData.tier`（Basic/Intermediate/Advanced/CityState）、`EquipmentData.price`、`GridItemData.price`。商店装备列表 = `EquipmentManager.GetShopItems()`、商店道具列表 = `GridItemManager.GetShopGridItems()`。
5. **移动路径遍历有先例**：`PieceManager.MovePieceAlongPath(piece, path)` 拿到完整 `path`（含起终点），移动开始时遍历 path 触发路径伤害（`VoltPathPassive`）。掉落物拾取 / 残留「经过」复用同一 path 遍历点。
6. **格子叠加层渲染**：`ChessBoardController.GetTile(coord).overlay.Show(color, alpha)`；`AllCoords` 遍历；`PieceLayoutModel.GetPieceAt(coord)` 查占据。
7. **真实伤害**：`DamageKind.True` + `PieceManager.ApplyIncomingDamage(piece, dmg, source=null, DamageSource.Dot, DamageKind.True)`（季风冬扣血同款，不统计地下交易、不附着元素）。
8. **属性聚合点**：`PieceModel` 的 `EffectiveAttack/EffectiveDefense/MaxHP/MoveRange/AttackRange` 五处（永久加成在此接入）。
9. **商店开关**：`InputHandler` 按 B 键触发 `OnShopToggle` → `UI_ShopPanel.Toggle`。飓风「商店不可开」在此拦截（判断内聚 MonsoonManager）。
10. 常驻红线：气候「是否生效」内聚在 `MonsoonManager`，不散落到移动/攻击/回合调用点。

## 二、改动方向

### 1. 配置结构重构（消除无用参数）

**A. `ClimateEffectKind` 枚举追加**：`Hurricane`（飓风）、`Lightning`（雷暴）。

**B. `ClimateEntry` 通用化（删 value）**：只保留通用字段——`climateName` / `probability` / `restrictedSeasonIndex` / `effectKind` / `exclusive`；**删除 `value` 字段**。二期 value（暴雨距离减量、暴雪击退格数）迁移到对应专属参数区块，`GetAttackRange`/`OnAttackLanded` 改从区块读。

**C. 各气候专属参数区块（MonsoonConfig 新增字段，每气候一个、参数内聚不冗余）**：
| 区块 | 字段 | 默认 |
|------|------|------|
| 暴雨参数 | 攻击距离减量 | 1 |
| 暴雪参数 | 击退格数 | 1 |
| 高温参数 | （无额外参数） | — |
| 飓风参数 | 掉落物数量下限 / 上限 | 2 / 4 |
| 飓风参数 | 幸运方块价值档位列表（见 D） | 4 档 |
| 雷暴参数 | 雷劈格子数下限 / 上限 | 2 / 4 |
| 雷暴参数 | 劈中棋子真实伤害 | 20 |
| 雷暴参数 | 残留初始强度 | 8 |
| 雷暴参数 | 残留每轮衰减 | 2 |
| 雷暴参数 | 永久加成概率 | 0.4 |
| 雷暴参数 | 永久加成属性池（属性类型 + 数值，随机抽一条） | HP+2 / 攻击+2 / 防御+2 / 移动+1 / 射程+1 |

**D. 幸运方块价值档位（`LootBoxTier` 列表）——每一格都是可配字段，代码零硬编码**：

每个档位条目含以下**可配置字段**：档位名、方块贴图、方块颜色、概率权重、普通装备 tier 列表、城邦装备价格范围（min/max，tier=CityState）、道具价格范围（min/max）、金币范围（min/max）。任何一项都可空/可调，档位数量也不固定（默认 4 档，可增删）。

默认 4 档（仅作字段初始值，Inspector 可改）：

| 档位 | 颜色 | 概率权重 | 普通装备 | 城邦装备(价格) | 道具(价格) | 金币 |
|------|------|---------|---------|--------------|-----------|------|
| 超高 | 红 | 0.05 | Advanced | 无 | 无 | 100~150 |
| 高 | 金 | 0.15 | Intermediate | ≥50 | ≥50 | 20~60 |
| 中 | 蓝 | 0.30 | Basic | <50 | <50 | 10~20 |
| 低 | 灰 | 0.50 | 无 | 无 | 无 | 1~6 |

> **分界口径**：城邦装备/道具「≥50 进金、<50 进蓝」。这个「50」本身也是字段值（金色档城邦装备/道具 `min=50`、蓝色档 `max=49`），不是代码写死的常量，可随时调。概率权重、tier、价格范围、金币范围同理——全是字段。

**E. `GameConfig` 新增渲染色**：幸运方块格高亮色、雷电残留格高亮色（各一色，可调）。

### 2. 飓风（幸运方块掉落物 + 商店关闭）

- 飓风持续**随机轮数**（走气候全局 1~3 轮随机，不特殊固定）。
- 飓风期间：**商店面板不可打开**（B 键无效，判断内聚 MonsoonManager）。
- 飓风出现时：随机在棋盘**空格**生成若干「幸运方块」（数量可配）；每个方块按概率权重取一个**价值档位**（红/金/蓝/灰）。
- 方块外观 = 该档位贴图/颜色（玩家拾取前**看不到具体内容**，只看到价值档位）。
- **拾取**：棋子移动**经过或停留**方块格 → 自动拾取 → 按该档位内容规则随机开出内容：装备进装备背包、道具进道具背包、金币直接加，方块消失、侧栏/金币刷新。
- **时效**：飓风结束 → 未拾取的方块**全部消失**，商店恢复可开。
- 装备/道具来源 = 商店完整列表（不限专属/通用），按档位的 tier/价格规则筛选后随机一件。

### 3. 雷暴（雷电残留 + 雷劈永久加成）

- 雷暴出现时：随机若干格子**雷劈**（数量可配）。
- 劈中的格子**若站着棋子**：棋子扣真实伤害（默认 20）；且该棋子**若仍存活**，有概率（可配）获得一个**对局内永久**属性加成——从属性池**随机一个属性** +N（可配；多次雷劈可叠加累积）。
- 劈过的每个格子留下**雷电残留**实体：初始强度（默认 8），**每轮 -2 衰减，归零消失**（残留是独立实体，按自身强度衰减，不受雷暴本身持续轮数影响）。
- 雷电残留伤害：棋子**位于或经过**该格 → 受「当前强度」的真实伤害。
- 永久属性加成接入 `EffectiveAttack/EffectiveDefense/MaxHP/MoveRange/AttackRange` 等聚合点（对局内永久，随棋子存续生效，无回合递减）。

### 4. 内聚过滤（关键）

- 幸运方块 / 雷电残留 / 商店关闭 / 雷劈加成的「是否生效」判断全部内聚在 `MonsoonManager`，调用点（移动流程 / 商店开关 / 属性聚合）只调一个入口方法，不做「当前城邦 == 季风」判断。

## 三、效果目标（验收标准）

1. 选到季风城邦 → 气候判定照常；飓风/雷暴按概率出现、持续 1~3 轮随机、可叠加（无上限）、有清晰日志。
2. **飓风期间**：B 键打不开商店；棋盘随机空格出现若干幸运方块（红/金/蓝/灰，按权重，贴图区分价值、看不到具体内容）；棋子移动经过/停留方块格 → 自动拾取并按档位开出内容（装备/道具进对应背包 + 侧栏刷新、金币直接加）；飓风结束未拾取方块消失、商店恢复。
3. **雷暴出现时**：随机格子雷劈；劈中棋子扣真实伤害（无视防御/护盾、不附着元素）；劈中且存活的棋子有概率获得随机一个属性的对局内永久加成；劈过的格子留雷电残留。
4. **雷电残留**：初始强度 8，每轮 -2 归零消失；棋子经过/停留该格受当前强度真实伤害。
5. 幸运方块档位（贴图/颜色/概率/内容规则）、掉落物数量、雷劈格子数、劈中伤害、残留强度/衰减、永久加成概率/属性池/数值——全部从配置资产读取，代码零硬编码。
6. 二期暴雨/暴雪/高温行为不变（仅 value 迁移到专属区块，效果数值与语义不变）。
7. 其他城邦 → 飓风/雷暴完全无效果，无方块、无残留、商店正常开。
8. 四季昼夜、暴雨/暴雪/高温、伤害类型、元素、护盾、贸易机制、城邦选择/过滤、背包系统等已完成功能行为不变。

## 四、红线（绝不能破坏）

1. **内聚过滤**：新机制「是否生效」判断内聚在 MonsoonManager 内部，不散落到移动/攻击/回合流转调用点。
2. **复用现有落点，不另起炉灶**：幸运方块拾取走既有背包/金币入口；雷电残留走真实伤害 + `ApplyIncomingDamage`；路径经过走既有 path 遍历点；永久加成接入既有属性聚合点；商店关闭走既有开关入口。
3. **时间单位铁律**：残留衰减、飓风/雷暴持续递减按「轮」计（轮号守卫，每轮一次），不按「回合」。
4. **数据驱动（最大化可配置）**：所有数值、概率（气候概率、档位权重、永久加成概率）、数量（掉落物/雷劈格子）、价格分界值（如城邦装备/道具的 50 分界）、价格范围、金币范围、tier 列表、属性池、衰减量——**全部是 MonsoonConfig/GameConfig 字段，代码零硬编码**；默认值仅作字段初始值，Inspector 可随时改。
5. **二期行为不变**：value 迁移到专属区块只是「搬位置」，暴雨/暴雪/高温的效果数值与语义零变化。
6. **不破坏既有功能**：四季昼夜、伤害管道、元素反应、护盾、贸易（拍卖/利息/地下交易）、城邦选择与过滤、背包系统——零改动（背包/金币/伤害只调用、不改逻辑）。
7. 幸运方块拾取 / 残留伤害失败或棋子死亡等边界不得产生副作用（不凭空加道具、不重复扣血、不残留脏数据）。

## 五、明确不做

- 幸运方块的「内容预览/透视」（拾取前只显示价值档位，不显示具体物品）。
- 雷电残留的「连锁传导/扩散」。
- 永久属性加成的跨局持久化（仅对局内永久）。
- 方块/残留的手动拾取或拖拽交互（移动自动拾取即可）。
- AP 上限 / 回能类属性的永久加成（本期属性池默认 HP/攻击/防御/移动/射程）。
