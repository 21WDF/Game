# 阶段③.1（超导减防持续回合 + 光界之力）· Trae 提示词

## 总览

两个内容：
1. **超导减防「持续回合」扩展**——给减防加持续回合概念（当前是"每回合开始无条件重置"），为四元素之力的"超导延长2回合"铺路。
2. **光界之力**——纯 `IReactionModifier` 实现（反应倍率 +25%，可叠加），先验证反应修饰全链路。

---

## 一、超导减防持续回合

### 现状
- `ReactionConfig` 有 `DefenseReduction`（减防值），**没有持续回合字段**。
- `AttackPiece` 超导时 `target.CurrentDefenseReduction = reaction.DefenseReduction`（当次攻击即享受）。
- `TickElementDuration` 每回合开始 `piece.CurrentDefenseReduction = 0`（无条件重置）。

### 改动
1. **`ElementReactionTable.ReactionConfig`**：加字段 `public int DefenseReductionTurns;`（减防持续回合数，默认 0；0 = 当次生效后下一回合开始重置，行为与现状一致）。
2. **`PieceModel`**：加字段 `public int DefenseReductionTurnsRemaining { get; set; }`（减防剩余持续回合）。
3. **`PieceManager.AttackPiece`** 超导减防处（约 196-197 行）：设置减防值的同时，`target.DefenseReductionTurnsRemaining = reaction.DefenseReductionTurns;`。
4. **`TurnManager.TickElementDuration`**：把"每回合无条件 `CurrentDefenseReduction = 0`"改为按剩余回合递减——`DefenseReductionTurnsRemaining > 0` 时递减 1，`≤ 0` 时才 `CurrentDefenseReduction = 0`（其余逻辑不变）。
5. **`ReactionDatabase.asset`**：给超导两条（`baseElement:3 triggerElement:4` 和 `baseElement:4 triggerElement:3`，即雷+冰、冰+雷）的 config 加 `DefenseReductionTurns: 1`（基础持续 1 回合）。其余条目不动。

### 语义
- `DefenseReductionTurns = N` 表示减防持续 N 个「回合开始」才重置。
- 基础 1 回合 = 触发后到下一个回合开始重置（≈ 现状）；元素之力"延长2回合"（③.2 通过 Modify 给该字段 +2）→ 3 回合。

---

## 二、光界之力（ReactionBoostPassive）

### 参数（jsonParams）
| 字段 | 类型 | 默认 | 说明 |
|---|---|---|---|
| percent | int | 25 | 反应倍率加成百分比（加法叠加） |

### 机制
- **实现 `IReactionModifier`**：`Modify` 里若 `config.Type != ReactionType.None`，则 `config.DamageMultiplier += percent / 100f`。
  - 只有"发生元素反应"才增强（无反应 `ReactionType.None` 不加，避免普攻无反应也 +25%）。
  - 加法叠加：多件光界之力被 `CollectReactionModifiers` 依次收集，`RuntimeReactionTable.Resolve` 逐个 Modify，累加（2 件 = +50%）。
- 非唯一被动（可叠加）。

### 改动
1. **新建 `Assets/Game/Equipment/Effects/ReactionBoostPassive.cs`**：同时实现 `IPassiveEffect` + `IReactionModifier`。
   - `IReactionModifier.Modify` 按上面逻辑返回修改后的 config（`ReactionConfig` 是 struct，传参即副本，直接改后返回）。
   - `IPassiveEffect.OnEquip/OnUnequip` 空实现（其余钩子走默认）。
2. **`PassiveFactory.cs`**：加 `case "ReactionBoostPassive"` + 参数类 `ReactionBoostParams`（`[System.Serializable]`，`percent` 默认 25）。

---

## 约束（务必遵守）

1. **只做本阶段两件事**，不要提前做四元素之力的 Modify、元素量加成、超载爆炸（那些是 ③.2 / ③.3）。
2. `DefenseReductionTurns` 默认 0，行为向后兼容（0 时 ≈ 现状"当次生效 + 回合开始重置"）。
3. 光界之力的 `Modify` 必须判断 `Type != None`，否则无反应普攻也会被增强。
4. 改 `ReactionDatabase.asset` 时**只加超导两条的 `DefenseReductionTurns: 1`**，其余条目字段原样保留（YAML 文本，注意格式）。
5. 诊断无报错后收尾。
