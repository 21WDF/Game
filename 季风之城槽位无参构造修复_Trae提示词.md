# 季风之城配置槽位无参构造修复 — Trae 提示词

## 问题本质（现状）

`MonsoonConfig` 里的效果槽位类 `EffectSlot` 目前**只有带参构造函数**（`EffectSlot(string, EffectType, int)`），**没有无参构造函数**。C# 里一旦定义了带参构造，无参构造就不会再自动生成。

这会带来一个隐患：`seasons` / `climates` 这类 `List<EffectSlot>` 在 Inspector 里点「+」添加元素时，Unity 需要无参构造来创建新元素实例，缺失时会报错、无法添加。一期因为四季槽位默认已填好 4 个、昼夜槽位是单字段所以不影响，但二期往 `climates` 里加气候条目时会踩中。

## 改动方向

给 `EffectSlot` 补一个无参构造函数，使它在 Inspector 里可作为列表元素正常添加，同时保留现有带参构造函数（四季/昼夜的默认值初始化不受影响）。

## 效果目标（验收标准）

1. `EffectSlot` 有无参构造，且带参构造仍保留。
2. `seasons` / `climates` 列表在 Inspector 里可正常「+」添加元素、编辑字段。

## 红线（绝不能破坏）

1. 现有带参构造、字段、四季/昼夜默认值初始化逻辑不变。
2. 不改变任何运行时的效果结算语义（本次纯补构造函数，零逻辑改动）。
