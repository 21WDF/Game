# 贸易之城三期（地下交易）· Trae 提示词

## 现状（框架事实，帮你理解问题本质）

贸易之城分三期，一期（透支 + 拍卖）、二期（利息 + 信誉）已完成。本期做三期「地下交易」，是最后一个城邦机制。

与本期直接相关的现状：

1. **信誉系统已存在**：`TradeCityManager.GetReputation(side)` 返回 0~10 信誉；`CanBidAuction(side)` 判信誉 ≥ 6。地下交易的「信誉 < 6」条件直接查它。
2. **时间单位「回合」≠「轮」**（铁律）：
   - 回合 = 单方行动一次（`CurrentTurn`），后手方结束只代表后手那一个回合结束。
   - 轮 = 双方各行动一次（`CurrentRound`），后手方结束才代表本轮结束。
   - 地下交易的概率触发按「轮」（每轮结束），关闭检查按「回合」（每回合结束）。
3. **伤害来源标记**（`DamageSource` 枚举，在 `IPassiveEffect.cs`）：
   - `Attack`（普攻）、`Splash`（元素 + 非元素溅射）、`Ultimate`（大招）、`Dot`（感电 DoT + 死亡之蔑延迟伤害）、`Reflect`（反伤）。
   - **超载爆炸在代码里也标记为 `Splash`**（`ApplyOverloadExplosion` 里 `DamageSource.Splash, DamageKind.Magical`）。
4. **伤害扣血点有 4 处**（护盾基座时梳理的，都已在护盾拦截判定之后才真正扣血）：
   - `AttackPiece` 主段（普攻/二段/元素溅射）：来源 = 传入的 `damageSource`（Attack 或 Splash），护盾拦截判定 `TryAbsorbByShield` 返回 false 才扣血。
   - `ApplyIncomingDamage` 统一入口（大招/DoT/反伤/超载爆炸/非元素溅射）：来源 = 参数 `damageType`，护盾拦截判定后扣血。
   - `ApplyExtraDamageSegments` 附加段：固定来源 `DamageSource.Attack`，每段独立过盾。
   - `DeathDancePassive` 延迟伤害：固定来源 `DamageSource.Dot`（不算直接伤害）。
5. **护盾拦截是「被挡不计」的判定点**：`TryAbsorbByShield` 返回 true = 伤害被完全免掉（伤害=0），应不计入累计；返回 false = 实际造成伤害，应计入。
6. **商行只卖装备**：装备有背包（`EquipmentManager.GrantEquipment` 无偿入包），复用即可；道具库存系统未做，本期商行不含道具。

## 改动方向

### 一、累计伤害统计（每方独立，按攻击方视角）

- 按「攻击方」累计：攻击方属于 P1 就累到 P1，属于 P2 就累到 P2，双方各自独立累计。
- 口径（硬约束）：只累计「直接伤害」——来源为 `Attack` / `Splash` / `Ultimate` 的伤害（超载爆炸是 Splash，**计入**）；来源为 `Dot` / `Reflect` 的**不计**。
- 被护盾挡的不计：`TryAbsorbByShield` 返回 true（伤害被免掉）时不计入；返回 false 才计入。
- 统计「实际造成伤害」的数值（护盾未拦截的那份伤害）。统计的挂点覆盖上述 4 个扣血点，不能遗漏（否则某个技能能累计、另一个不能）。

### 二、地下交易事件状态机（每方独立）

P1 和 P2 各自独立的一套地下交易状态，包含：累计伤害值、事件是否激活、商行是否开启、当前触发概率。

流程（每方独立）：
1. 累计伤害达到 60 **且** 该方信誉 < 6 → 该方地下交易事件「激活」。
2. 事件激活后，**每轮结束**（后手方结束行动、一轮完成时）以概率尝试开启商行；基础概率 10%，累计伤害每增加 30 点概率 +20%（概率封顶 100%）。
3. 商行开启后，该方玩家可购买商行物品（只卖装备，复用背包）。
4. **每回合结束**检查该方信誉：信誉 < 6 → 商行保持开启（继续可买）；信誉 ≥ 6 → 清空累计伤害与概率、关闭商行与事件、回到初始状态（重新累计伤害，需再次达到 60 且信誉 < 6 才重新激活）。

### 三、地下交易商行

- 商行有独属物品池（只装备，`EquipmentData` 数组，Inspector 拖入或配置读取；asset 用户后续做）。
- 商行开启后，该方玩家可购买，装备进该方背包（复用 `EquipmentManager.GrantEquipment`）。
- 商行购买**不受信誉涨价影响**（地下交易是低信誉玩家的补偿通道）。

**关键：商行物品的结算方式要多样化**（低信誉玩家 ≈ 欠钱玩家，金币结算他们买不起）。每件商行物品选一种结算方式：

1. **低价金币**：象征性低价（默认 1~3 金币，欠钱玩家也付得起），走透支扣款。
2. **赊账（加深欠债）**：购买不花金币，反而把金币扣到**更负**（加深欠债，金额可配置），复用 `TrySpendGoldWithOverdraft`。

**关键：地下交易装备带「使用代价」**（高风险高回报的黑暗交易）。代价通过代价型被动实现——复用 `IPassiveEffect` 的 `OnTurnStart` 钩子，配在装备 `passives` 里与正面被动共存。代价形式多样，至少实现两种：

1. **持续扣血**：装备后每回合开始扣 X HP（越强扣越多）。
2. **扣 AP**：装备后每回合 AP 上限 -N 或每回合开始扣 AP。

代价参数由装备的 `jsonParams` 配置，asset 用户后续做。设计意图：便宜（低价/赊账）但装备带代价，玩家要权衡「是否值得为强力装备背上每回合的负担」。

### 四、UI 入口（可选但建议）

商行开启时，给该方玩家一个购买入口（例如商店面板新增「地下交易」Tab，仅该方商行开启时可见）。具体形式由你定，但要能感知「商行是否开启」和「可购买物品」。商行物品需展示其**结算方式**（低价金币 / 赊账加深欠债）和**使用代价**（每回合扣血 N / 扣 AP N）。

## 效果目标（验收标准）

1. 双方各自独立累计「直接伤害」（Attack/Splash/Ultimate 计入，Dot/Reflect 不计，被护盾挡不计），数值正确。
2. 某方累计伤害 ≥ 60 且信誉 < 6 → 该方地下交易事件激活。
3. 事件激活后，每轮结束按概率触发商行（基础 10%，累计伤害每 +30 概率 +20%，封顶 100%）。
4. 商行开启后，该方玩家可购买商行装备，进背包，价格不受信誉涨价影响；支持「低价金币」和「赊账（加深欠债）」两种结算方式。
5. 每回合结束检查：信誉 < 6 商行保持开启；信誉 ≥ 6 清空累计伤害+概率、关闭事件、重新累计。
6. 双方的地下交易状态完全独立，互不干扰。
7. 累计伤害为 0 / 事件未激活时，行为与二期完全一致。
8. 代价型被动至少实现「持续扣血」和「扣 AP」两种，配在装备 passives 里通过 `jsonParams` 配置，OnTurnStart 触发。

## 红线（绝不能破坏）

1. **时间单位铁律**：概率触发按「轮」（后手方结束、一轮完成时）；关闭检查按「回合」（每次任一方回合结束）。绝不能混用。
2. **累计伤害口径**：只计入 Attack/Splash/Ultimate，Dot/Reflect 不计；被护盾挡不计。超载爆炸（Splash）计入。
3. **不动伤害管道逻辑**：累计伤害统计是「只读观察」，不得改变任何伤害结算、护盾拦截、减伤、元素附着、反应的现有行为。
4. **每方独立**：P1/P2 的累计伤害、事件、商行、概率互不共享。
5. **商行只卖装备**：复用现有背包；不做道具库存系统。
6. **商行购买不受信誉涨价影响**：价格固定为原价（与商店涨价、拍卖形成对比，是「补偿通道」的定位）。赊账方式的「加深欠债」也**不额外扣信誉**（只是金币变负，之后按二期规则自然触发扣信誉）。
7. **不动一二期已完成的逻辑**：透支、拍卖、利息、信誉系统的现有行为保持原样，只读查询。
8. **概率随机入口收敛到单一方法**（在线 PVP 预留：将来网络化只需替换随机入口为同步 seed）。
9. **代价型被动是新增、独立的效果类**：不得改动现有任何被动效果类；代价只影响「装备它的那个棋子」，不影响全局。

## 明确不做

- 道具库存系统（持有列表 + 背包 UI + 使用入口）。
- 信誉 = 0 的任何额外惩罚。
- 城邦判定（同一二期口径：机制常驻跑通，城邦联动后续统一接）。
- 代价形式不必一次做全：本期至少做「持续扣血」和「扣 AP」两种，其余（耐久损坏、移动范围减等）后续扩展。
