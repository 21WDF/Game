# 附加伤害独立成段（类型参数 + 反甲/减伤精确区分）

## 需求
把附加伤害从「混进基础物理伤害一起结算」改成「独立的一段伤害」，让它拥有自己的伤害类型，从而精确约束三件事：

- 物理附加：吃防御减伤、触发反甲、吃物理减伤被动
- 魔法附加：不吃防御减伤、不触发反甲、吃魔法减伤被动
- 真实附加：不吃防御减伤、不触发反甲、吃真实减伤被动

## 改动方向

1. **ExtraDamagePassive**：加伤害类型参数（默认「真实 True」），暴露只读属性供结算读取。

2. **PassiveFactory**：附加伤害参数类加 `kind` 字段（string 承接，避免 JsonUtility 枚举只认数值的坑），新增一个解析函数把字符串转成伤害类型（对标 ParseReductionFilter，不区分大小写），case 里把类型传给构造。

3. **PieceManager 普攻结算**：附加伤害不再累加进基础伤害公式；改为在基础伤害扣血结算完成之后，把附加伤害按类型分成「物理 / 魔法 / 真实」三份，各自作为独立伤害扣血结算：
   - 物理附加：先吃目标防御减伤，再吃物理类型的减伤被动
   - 魔法 / 真实附加：吃各自对应类型的减伤被动（不吃防御减伤）
   - 每一份独立扣血、独立走免死、独立触发「受到伤害」通知（携带正确的伤害类型）

## 约束（务必遵守）

- 默认类型 True：现有没配 kind 的附加伤害，行为与改动前一致（不吃防御减伤）。
- 附加伤害段只做「防御减伤 + 减伤被动 + 扣血 + 免死 + 受伤害通知」，其余一律不触发：被攻击通知、造成伤害通知（吸血）、元素反应/附着、冻结、感电、超载爆炸、击退、金币、能量、跳字。
- 基础伤害已致死目标时，不再结算附加伤害段。
- 附加伤害段致死时，击杀者仍是攻击者（末尾统一销毁，OnKill 等照常）。
- 首伤=1、死亡之蔑延迟拆分只作用于基础伤害段，附加伤害不参与。
- 不动元素反应、首伤=1、延迟拆分、免死、伤害通知链的既有逻辑（它们仍作用于基础段）。
- 只改 ExtraDamagePassive、PassiveFactory、PieceManager 三个文件，其余文件不碰。
