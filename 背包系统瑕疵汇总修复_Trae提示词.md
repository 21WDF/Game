# 背包系统瑕疵汇总修复_Trae提示词.md

> 铁律：只写「改动方向 + 效果目标 + 红线」，不写实现细节。

## 〇、背景（一句话）

背包系统多轮迭代累积的历史瑕疵 + 本轮新发现，集中修一批。分「必修（功能性 bug）」和「顺带修（注释/防御/效率/清理）」两组。

## 一、必修（功能性 bug，4 项）

### 1. tooltip 空槽退出不关（回归 bug，本轮引入）

- **现状**：`UI_BackpackIconSlot` 的指针移出回调里，只有「格子有物品」时才通知「离开滚动区域」，空槽移出**不通知**，导致 tooltip 的「在滚动区域内」状态卡住、永不消失。
- **目标**：鼠标离开**任何格子（含空槽）**都应触发「可能离开滚动区域」的关闭判定；「离开滚动区域 **且** 不在 tooltip 上」即关闭。空槽的「无响应」只应作用于单击/右键，不应影响 tooltip 关闭通知。

### 2. 三连击误触发双击（历史遗留）

- **现状**：`UI_BackpackIconSlot` 用「点击次数 ≥ 2」判双击，快速三连击时第 3 次点击也走双击分支，道具会被「使用后立即取消退回背包」。
- **目标**：只有「恰好第 2 次点击」触发双击；第 3 次及以后回到单击语义，不产生第二次使用。

### 3. 未选中颜色字段失效（历史遗留）

- **现状**：`UI_BackpackIconSlot.Awake` 里无条件用「预制体 Image 初始色」覆盖「未选中色」字段，Inspector 里配的未选中色名存实亡。
- **目标**：取消选中时还原的是该槽位预制体 Image 的**原始颜色**（空槽背景色正确还原），选中色仍可在 Inspector 配置；「未选中色」字段不再被运行时无条件覆盖。

### 4. 拖拽幽灵定位的 Overlay 假设（历史遗留）

- **现状**：拖拽跟随幽灵把屏幕坐标直接当世界坐标用，仅在 Screen Space Overlay 画布下正确，Screen Space Camera 画布会偏移。
- **目标**：幽灵跟随鼠标在任意 Canvas 渲染模式下都准确。

## 二、顺带修（注释 / 防御 / 效率 / 清理，5 项）

### 5. `GridItemData.requiresSelectedPiece` 注释过时

- 注释还写「UI 据此决定购买按钮是否置灰」，已过时（现为侧栏「使用」校验 + 拖拽负载判定）。改准确。

### 6. `CanApplyDragTarget` 补存活防御

- 注释声称「含存活防御」但未落实：未排除已死亡棋子。补上「非死亡」防御，与注释一致。

### 7. 容量查询复用缓存

- `BuyToInventory` / `BuyEquipment` 的容量校验里重复 `Resources.Load<GameConfig>`；应复用已有的配置缓存（无缓存处补缓存）。功能不变，仅避免重复加载。

### 8. 容量兜底口径统一

- GameConfig 缺失时的兜底：侧栏用「总槽数折半」、购买入口用「硬编码 5」。统一两者兜底口径，避免改总槽数后不一致（仅 GameConfig 缺失的极端场景，正常不触发）。

### 9. 空槽 tooltip 死代码清理

- 空槽已改为不响应右键后，填充时空槽仍被赋「空槽」标题/描述，永不显示。清理冗余赋值与残留注释。

## 三、红线（绝不能破坏）

1. **装备/道具/伤害/元素/护盾/城邦/回合流转的核心逻辑零改动**——本次只动 UI 层、注释、缓存、防御。
2. 拖拽松手判定与高亮收集仍共用**统一可施加判定**，当前口径「己方棋子」不变；拖拽失败/取消无副作用。
3. 侧栏仍**不得调用 `RegisterPanelOpen/Close`**，显隐仍走 CanvasGroup。
4. tooltip 仍是纯显示浮层，不参与面板计数、不锁棋盘。
5. 左键单击选中、左键双击使用道具、右键 tooltip、拖拽装备/需指定道具——用户可感知的交互语义不得改变（只修异常分支：三连击、空槽退出、幽灵偏移、取消选中颜色）。
6. 容量校验仍只约束商店购买；拍卖 / 地下交易等其他获得装备入口不受拦截。

## 四、明确不做

- 拖拽排序、拖回侧栏卸下、「可对敌方」本体实现、溢出、存档、跨局持久化。
- 不动 tooltip 定位（滚动视图右缘 + 垂直平齐）与「双条件关闭」的既定正确逻辑，只修空槽退出漏通知这一处。
