# 战术叠加层三项修复 — 二次审查报告

## 评分：9.3/10

三项修复全部到位。实现干净、事件驱动解耦合理、共享资源优化正确。

---

## 逐项审查

### 修复一：PathPreview 归属从 InputHandler 移入 ChessBoardView

**评分：9/10**

```
调用链：
  InputHandler.UpdatePathPreview()
    → ChessBoardController.ShowPathPreview()
      → ChessBoardView.ShowPathPreview()
        → PathPreview.ShowPath()

旧：InputHandler 直接持有 LineRenderer（输入层管渲染）
新：View 层懒加载持有，Controller 转发，InputHandler 只发指令
```

**做对的**：MVC 分层归位。`ShowPathPreview/HidePathPreview` 经 Controller 转发，View 懒加载创建——首次需要路径预览时才 Instantiate，避免了场景初始化时的空 LineRenderer 开销。

**微小不纯**：`InputHandler.UpdatePathPreview()` 仍然包含路径计算逻辑（读 `BattleController.Model.SelectedPiece` → `FindPath(piece.Coord, hoverTile.Coord)`）。严格 MVC 下路径计算属于 Controller 层（ChessBoardController.FindPath 已经在 Controller 里了），剩下的只是 InputHandler 组装参数。考虑到 InputHandler 已持有 `_currentHoverTile` 和 BattleController 引用，这个残留是务实的——不算债务，但记录在案：如果将来 InputHandler 膨胀到 500 行，这是第一个该切出来的方法。

---

### 修复二：TileOverlay 六边形 Mesh 替换正方形 Quad

**评分：10/10**

```csharp
// 静态共享 Mesh——61 个格子，只有一个 Mesh 实例
private static Mesh _sharedHexMesh;

// 7 顶点扇面：中心 + 6 外顶点（0°/60°/120°/180°/240°/300°）
// 6 个三角形，(0, next, current) CW 绕序 → 法线 +Y
```

**做对的**：
- 静态共享 Mesh 是正确的——61 个格子的 Overlay 共享同一份顶点/三角形数据，GPU 内存只占一份
- 7 顶点扇面是六边形的最简 Mesh 表示法——不需要 12 个三角形（中心→边分割），6 个三角就够了
- 独立子物体 `OverlayHex`（y+0.01）防止与地形层的 z-fighting
- CW 绕序 → `RecalculateNormals()` → 法线 +Y → 正确的 Unlit Shader 光线方向

**唯一的注意事项**（不是 bug）：Overlay 六边形的半径固定为 1（顶点距中心 1 单位），但 GameConfig 中 `hexSize=1.25` 是你的 3D 模型尺寸。当前 Overlay 比模型小 20%，在 Alpha=0.35 的低透明度下几乎不可见差异。如果你之后要精确对齐，在 `GetSharedHexMesh()` 里把 `Mathf.Cos(angle)` 和 `Mathf.Sin(angle)` 乘以 `GameConfig.Instance.hexSize` 即可。**建议现在不改**——当前视觉效果已经够用，加 GameConfig 依赖反而让 TileOverlay 不再自包含。

---

### 修复三：面板关闭时自动刷新悬停

**评分：10/10**

```
UI_ShopPanel.Toggle() → RegisterPanelClose()
  → _openPanelCount-- → count==0?
    → OnAnyPanelOpenChanged(false)
      → HandleAnyPanelOpenChanged(false)
        → UpdateHover()

UI_EquipmentPanel.Toggle() → 同上（独立计数，互不干扰）
```

**做对的**：
- 事件驱动——UI_ShopPanel 和 UI_EquipmentPanel **不知道 UpdateHover 的存在**。它们只调用 `RegisterPanelClose()`，后续链路由 InputHandler 内部完成
- 计数器模式——两个面板同时打开时关闭一个不会触发 UpdateHover，只有全部关闭（count==0）才触发。这和 GridItemManager 的瞄准模式兼容——道具瞄准时不调 RegisterPanelOpen（侧边栏模式），count 始终为 0，不会误触发
- UpdateHover 保持 private——没有为这个需求暴露不必要的公开接口

---

## 两项优化建议（P2，不紧急）

### 建议 1：Overlay 颜色收进配置

当前颜色硬编码在 BattleView 和 InputHandler 中：

```csharp
// BattleView.cs：Move=蓝(0.2,0.4,1.0) α=0.35
// InputHandler.cs：Hover=浅紫(0.7,0.5,1.0) α=0.25
```

这 4 组颜色（Move/Attack/AttackEnemy/Hover）分布在不同文件里，调一次要改两个文件。建议加到 GameConfig：

```csharp
// GameConfig.cs 追加
[Header("战术叠加层颜色")]
public Color overlayMoveColor = new(0.2f, 0.4f, 1f);
public Color overlayAttackColor = new(1f, 0.2f, 0.2f);
public Color overlayHoverColor = new(0.7f, 0.5f, 1f);
```

不改也行——这些颜色调一次后基本不会再动。

### 建议 2：路径颜色从 PathPreview 硬编码抽出来

`PathPreview.ShowPath()` 中路径线的黄色 `α=0.6 宽度 0.15` 目前硬编码在 `PathPreview.cs`。建议改为 `[SerializeField]` 字段或从 GameConfig 读——方便后续配合不同主题（冬季棋盘可能需要白色路径线）。

---

## 结论

**三项修复全部通过，没有引入新问题。** 项目从"能跑"进入"分层正确 + 扩展安全"的状态。下一步的重点应该是 playtest——框架层面的优化到此基本收官，剩下的都是数值和内容的迭代。