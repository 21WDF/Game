# 机制按城邦过滤 — Trae 提示词

## 现状（框架事实）

城邦确定后，专属机制目前是**无条件常驻**的（骨架期「机制常驻跑通、城邦联动后接」的遗留），没有按「当前城邦」过滤：

- **元素染色**：护盾的「染色 / 封印」逻辑由一个**全局布尔开关**控制（默认开启），没有判断当前是不是元素城邦。
- **贸易机制**：拍卖（出价、倒计时成交、刷新）、利息/信誉（结算、商店涨价、拍卖信誉门槛）、地下交易（累计伤害统计、概率触发、商行），全部**无条件生效**，没有判断当前是不是贸易城邦。
- 商店里的「拍卖」页签目前一直显示、「地下交易」页签仅由商行开启状态控制，都不受城邦约束。
- 当前城邦状态已有一个统一查询入口（城邦管理器持有，选择流程完成后即写入）。

## 改动方向

让每套专属机制**只在「当前城邦 == 它所属的城邦」时才生效**，其余城邦下退回到「没有该机制」的基线。过滤判断应**内聚在每套机制自身**（机制入口处查当前城邦、不是则直接不生效），不把城邦判断散落到回合流转等调用点。

1. **元素染色**：染色/封印只在「当前城邦 == 元素城邦」时生效；其他城邦下不染色、不封印（护盾退回普通层数护盾）。**保留原全局开关作为 master 总闸**（默认开启）——染色条件 = 「master 总闸开启 且 当前城邦 == 元素城邦」，不删除该开关（避免资产序列化字段失效），它只降级为总闸。
2. **贸易机制**：拍卖、利息/信誉、地下交易全部只在「当前城邦 == 贸易城邦」时生效，包括：
   - 回合/轮结算（利息、信誉扣减恢复、拍卖倒计时、地下交易概率与关闭检查）——非贸易城邦下整套路径不触发；
   - **累计伤害统计**（伤害管道里的高频记录）——非贸易城邦下不累计；
   - **查询接口**：商店涨价查询非贸易城邦下返回**原价**、拍卖门槛查询非贸易城邦下返回**无门槛**。
3. **商店页签**：「拍卖」「地下交易」两个页签只在贸易城邦下出现/可用，其他城邦下隐藏或不可进入（地下交易页签在此之上仍受「商行是否开启」约束）。

## 效果目标（验收标准）

1. 选到贸易城邦 → 拍卖、利息/信誉、地下交易生效；元素染色不生效。
2. 选到元素城邦 → 染色/封印生效；拍卖、利息/信誉、地下交易、商店涨价都不生效。
3. 选到其余 4 个城邦（欢愉/季风/邪疑/战争）→ 上述机制都不生效（只有商店城邦装备池随城邦变化）。
4. 非贸易城邦下：商店显示原价、无拍卖页签、无地下交易页签、无利息/信誉结算、无累计伤害统计。
5. 机制不生效时，行为与「完全没有该机制」完全一致，不留任何半激活状态或无效日志。

## 红线（绝不能破坏）

1. **护盾基座零改动**：层数护盾、拦截规则、吸血/金币拦截、护盾 UI 全部保持原样，本次只改「染色（元素标签）」这一项的生效条件。
2. **染色的正交铁律不变**：护盾仍只拦伤害数值、不拦元素附着/反应，这个语义不能因加了城邦判断而改变。
3. **机制不生效 = 干净基线**：不是「机制在跑但结果被忽略」，而是整套机制路径都不触发（含高频的累计伤害记录也不做）。
4. **时间单位铁律不变**：利息/信誉恢复按「轮」、欠钱计次/拍卖倒计时/地下交易关闭检查按「回合」，过滤不得改变这些口径。
5. 城邦选择的「求交集 + 随机」纯函数、随机入口收敛单一方法，以及刚做好的选择 UI 流程，零改动。
6. 商店城邦档装备的过滤规则（归属 None 通用 / 归属当前城邦显示）不受本次影响。
7. **不删 `shieldElementDyeingEnabled` 字段**：它降级为 master 总闸，避免 GameConfig 资产序列化字段失效、破坏现有染色测试闭环。
