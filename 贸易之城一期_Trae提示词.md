# 贸易之城一期（透支 + 拍卖本体）· Trae 提示词

## 现状（框架事实，帮你理解问题本质）

项目是 Unity 6 本地双人热座 Hex 战棋，MVC + ScriptableObject。贸易之城是城邦系统（Pillar 3）的第一个具体城邦，机制分三期，本期只做「透支 + 拍卖本体」，利息/信誉/地下交易是二期三期的事。

与本期直接相关的现状：

1. **金币扣款目前不允许透支**：`GoldModel.TrySpendGold` 是「当前金币 < 金额就返回 false」。贸易之城需要玩家能透支（欠钱）购买，所以要新增「允许透支到下限」的扣款能力，下限可配置（默认 -50）。
2. **商店已有 Tab 结构**：`ShopTab` 枚举目前 5 个值（初级/中级/高级/城邦/道具），`UI_ShopPanel` 的 Tab 按钮按下标绑定（`for i < 5`）。拍卖要做成商店里的新 Tab。
3. **时间单位「回合」≠「轮」**（重要红线）：
   - 回合 = 单方行动一次（`CurrentTurn`），后手方结束只代表后手那一个回合结束。
   - 轮 = 双方各行动一次（`CurrentRound`），后手方结束才代表本轮结束。
   - **拍卖的成交倒计时和刷新都用「回合」（CurrentTurn）**，不能用轮——因为用轮会让后手报价者白赚半轮优势。
4. **城邦专属装备已有归属字段**：`EquipmentData.cityState`（`CityStateKind` 枚举），贸易之城专属装备挂 `tier = CityState` 且 `cityState = Trade`。商店城邦 Tab 已按当前城邦过滤（`CityStateManager.ShouldShowEquipment`）。
5. **拍卖物品是新数据概念**：拍卖行物品需要「指向一件可获得的装备或道具 + 最低报价 + 最低加价幅度」，现有 `EquipmentData`/`GridItemData` 没有这些拍卖字段，需要引入新的数据承载方式（具体结构由你定）。

## 改动方向

### 一、透支扣款能力

让金币扣款支持「透支到下限」（默认下限 -50，可配置）。所有消费（买装备/买道具/拍卖成交）都能透支，扣款后金币可以为负（欠钱状态）。透支下限需要能从全局配置读取（方便 playtest 调整），而不是硬编码。

### 二、拍卖行数据与状态

引入拍卖行的核心数据与状态管理，至少包含：

- **拍卖物品**：一件物品有「指向的装备（`EquipmentData`）」「最低报价」「最低加价幅度」，以及运行时状态「当前最高出价」「当前出价者」「成交倒计时剩余回合数」。一期只拍卖装备，不含道具。
- **出价**：仅当前回合玩家可对物品出价；每次出价必须 ≥ 当前价 + 最低加价幅度；玩家可同时参与多个物品的拍卖。
- **成交**：出价后，若连续 6 个回合内无其他玩家加价，物品归属当前出价者，扣其出价金额（可透支），物品移出拍卖行。
- **刷新**：每 5 个回合向拍卖行补充新物品；已被拍卖的物品不会重复添加（需记录已售物品集合）。
- **倒计时递减时机**：按「回合」递减（每次任意一方行动结束，所有有出价者的物品倒计时 -1），倒计时归零即成交。

### 三、拍卖行入口（商店新 Tab）

商店面板新增「拍卖」Tab，展示拍卖行当前物品列表，支持当前回合玩家出价。出价交互复用商店面板的列表项结构。

### 四、拍卖物品范围（一期只装备）

拍卖行一期**只拍卖装备**，不拍卖道具。理由：装备成交后可直接进玩家背包（复用现有 `EquipmentManager` 的背包与装备流程），零新系统；而道具是「购买即用、无库存」，要「进背包」必须先做道具库存系统（持有列表 + 背包 UI + 使用入口），这是二期独立工作。

因此：
- 拍卖物品的数据结构，指向「装备」（`EquipmentData`）+ 底价 + 加价幅度。
- 成交结算时：把该装备交付给成交者（进背包），复用现有装备入包逻辑。
- 本期不做道具库存，拍卖不涉及道具。道具库存系统做完后（二期），道具拍卖才进背包。

拍卖物品的 asset 配置由用户后续自己做，本期先建好「代码能承载这些物品 + 从配置读取」的结构，并允许用空池/占位跑通流程。

## 效果目标（验收标准）

1. 金币扣款能透支到下限（默认 -50），扣完金币可为负值，且下限可配置。
2. 拍卖行能展示装备，每件装备有底价和加价幅度。
3. 当前回合玩家能对装备出价，出价必须满足「≥ 当前价 + 加价幅度」。
4. 出价后连续 6 个回合无人加价 → 成交，扣出价金额（可透支），装备进入成交者背包，拍卖项移除。
5. 每 5 个回合刷新新物品，已售物品不重复出现。
6. 拍卖倒计时严格按「回合」（CurrentTurn）递减，不是按轮。
7. 玩家可同时参与多个物品拍卖（互不干扰）。
8. 商店拍卖 Tab 能正常打开、展示、出价。

## 红线（绝不能破坏）

1. **时间单位铁律**：成交倒计时 6 回合、刷新 5 回合，都按「回合」（CurrentTurn）计，绝不能按「轮」（CurrentRound）计。这是先手/后手公平性的关键。
2. **不做利息、信誉、地下交易**：这三块是二期三期，本期不碰。尤其不要顺手加「负金币扣利息」或「信誉值」的字段和逻辑。
3. **不动现有购买流程的正常逻辑**：装备/道具的普通购买（不透支场景）行为与改造前一致；透支能力是「放宽」而不是「改变」正常扣款。
4. **不动伤害管道、元素附着、元素反应、护盾染色**。
5. **不动城邦框架**：`CityStateManager`、`ShouldShowEquipment`、城邦选择流程保持原样，拍卖是在「贸易之城被选中」这一前提下才生效的机制（但本期可先不严格判断城邦，先把机制跑通）。
6. **拍卖物品的 asset 内容由用户后续做**：本期只建结构，不要求产出具体拍卖物品 asset。
7. **一期只拍卖装备**：成交后装备进背包（复用现有背包/装备流程）；不涉及道具，不做道具库存系统（持有列表 + 背包 UI + 使用入口是二期独立工作）。

## 明确不做（二期及以后）

- 利息（正/负）、信誉系统（10/8/5/3/0 五档 + 持续易伤）、地下交易。
- 道具库存系统（持有列表 + 背包 UI + 使用入口），做完后道具拍卖才进背包。
- 拍卖物品的实际 asset 配置（用户自己做）。
- 拍卖的「仅贸易之城生效」城邦判定（本期先把机制本体跑通，城邦联动后续再接）。
