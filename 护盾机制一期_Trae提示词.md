# 护盾机制一期（基座）Trae 提示词

> 本提示词只描述「改动方向 + 效果目标 + 红线」，不写实现细节（方法名、算法、伪代码）。
> 请先通读全文，理解护盾要达成的行为语义，再自行决定实现方式。

---

## 一、现状（框架事实，帮你理解问题本质）

1. 本项目伤害有**两条独立路径**：
   - 普攻路径（`AttackPiece`）：普攻、二段、溅射走这条。
   - 统一伤害入口（`ApplyIncomingDamage`）：大招、DoT、反伤、超载爆炸、非元素溅射走这条。
   护盾必须**同时覆盖这两条路径**，任何一条漏了都会出现「普攻破盾但大招不破盾」之类的不一致。

2. 伤害有**两个正交维度**（务必理解，别混淆）：
   - **伤害类型（DamageKind）**：物理 / 魔法 / 真实。
   - **元素（ElementType）**：无 / 火 / 水 / 雷 / 冰。
   物理伤害可以带元素（如「物理+雷」），魔法伤害可以不带元素（如反伤）。护盾的「元素免疫」看的是**伤害携带的元素**，不是伤害类型。

3. 现有的伤害介入顺序（两条路径都遵循）：
   `伤害计算 → 减伤 → 易伤 → 首伤=1 → 延迟拆分 → 扣血 → 免死`
   护盾要插在**「减伤」之前**（护盾拦截的是原始伤害，不是减伤后的值）。

4. **元素附着 / 元素反应 / 冻结 / 感电 / 减防，与「伤害数值」是两码事**。护盾只拦「伤害数值」这一件事，绝不能拦元素附着、反应、冻结、感电——这些照常发生。

5. 两条路径里「本次伤害的元素」来源不一致：统一入口有显式的元素参数；普攻路径的元素来自攻击方的先天元素。护盾判断时需要拿到这个元素，注意别只在一处取对、另一处取错。

6. 装备被动走「类名 + JSON 参数 + 工厂」模式（`PassiveFactory`），新增被动只需按既有模式接入。JSON 里的**枚举字段要用字符串承接 + 手动解析**（直接写枚举名会静默回退默认值，这是本项目反复踩过的坑）。

---

## 二、改动方向

给棋子加一个**护盾系统（基座）**，行为语义如下：

### 护盾是什么
- 护盾 = **层数 + 一个元素标签**。
- 层数上限 3，每层代表「免一次伤害」。
- 元素标签取值：无 / 火 / 水 / 雷 / 冰。「无」= 普通盾，其余 = 元素盾。

### 拦截规则（伤害命中护盾时）
| 情况 | 结果 |
|---|---|
| 无护盾 | 不拦截，照常结算 |
| 普通盾（无元素） | 免掉这次伤害，扣 1 层 |
| 元素盾 · 伤害元素与盾元素**相同** | 免疫（免伤且**不扣层**） |
| 元素盾 · 伤害元素与盾元素**不同** | 免掉这次伤害，扣 1 层 |
| 真实（True）伤害 | 无视上面的元素免疫，但**仍扣 1 层** |

### 护盾拦截的边界（务必遵守）
- 护盾**只拦截「伤害数值」**。被拦截的这次伤害：元素附着、元素反应、冻结、感电、减防**全部照常发生**，只有「扣血」被免掉。
- 护盾免伤 = **没有减血**，所以不应触发「受到伤害」类通知，也不应打断「连续未受伤」计时。
- 护盾扣到 0 层 = 护盾消失。
- 重复获得护盾 = **刷新到上限**（重置为满层，不叠加）。

### 测试用装备被动（验证机制用）
做一件测试装备被动「圣盾」，效果是：给持有者若干层护盾，护盾元素可配置（默认无元素，可配成火/水/雷/冰用来测元素免疫）。具体层数、元素用 JSON 参数配置。

---

## 三、效果目标（验收标准）

实现完成后，下面每一条都成立才算通过：

1. 一个带普通盾的棋子被攻击时，不掉血、盾层数减 1，元素反应/附着照常。
2. 一个带火盾的棋子被**火元素**攻击时，不掉血、**盾层数不减**；被**水/雷/冰/无元素**攻击时，不掉血、盾层数减 1。
3. 一个带火盾的棋子被**真实伤害**攻击时，盾层数减 1（真实伤害不受元素免疫）。
4. 普攻、大招、溅射、DoT、反伤——**任何一条路径**打到护盾，行为都符合上表，不出现「某条路径绕过护盾」。
5. 护盾扣到 0 层后，后续伤害正常扣血。
6. 被护盾免掉的伤害，**不**打断「连续未受伤」计时，**不**触发「受到伤害」类被动。
7. 测试装备被动「圣盾」能通过 JSON 配置层数和元素，且生效。

---

## 四、红线（绝不能破坏的东西）

1. **不许改动元素附着、元素反应、冻结、感电、减防的任何现有逻辑**——护盾和它们是完全正交的两件事。
2. **不许改动现有伤害介入顺序**（减伤 → 易伤 → 首伤=1 → 延迟拆分 → 免死）——护盾只在最前面「拦截」，不参与后面任何一步。
3. **不许改动伤害类型三分类（物理/魔法/真实）的现有筛选逻辑**（减伤、反甲、跳字都依赖它）。
4. **不许破坏现有被动框架**——所有已有被动（减伤、反甲、再生、免死等）行为不变。
5. **不许动跳字系统之外的东西**——护盾的反馈如需跳字，只新增提示，不改现有伤害跳字逻辑。
6. 本阶段**不做**「元素城邦染色」（被元素附着后护盾变成对应元素）——那是二期，只把「护盾带元素标签」这个结构预留好即可。

---

## 五、本阶段范围外（不要做）

- 元素城邦的护盾染色机制（二期）。
- 护盾的数值平衡（层数上限 3、测试被动数值，都是占位，playtest 再调）。
- 任何音效、UI 美化。
