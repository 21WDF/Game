# 00 · 待确认事项汇总

> **这份文档的用途**：汇总所有分册里**需要你决策、补充或核实**的事项。策划案正文只写"确定的内容"，不确定的一律收拢到这里——**看这一份就够，不用翻各分册**。
>
> 处理方式：你直接在每条下面写结论，或者口头告诉我，我来改分册。
>
> 更新日期：2026-09-14 · 覆盖分册：总案 / 01 棋盘与地图 / 02 范围与空间

---

## A. 需要你决策的设计问题

### A1. 单局时长目标 ⭐ 最关键

| 项 | 内容 |
|---|---|
| 来源 | 总策划案 §2.3 |
| 现状 | 我按经验填 **20~40 分钟**，标记 `[PLACEHOLDER]` |
| 为什么关键 | 这个数**决定另外三个数**：回合数上限、每方棋子数（3/5/7）能否成立、局外金币的解锁节奏是否合理 |
| 若实际偏长 | 若实测 >40 分钟 → 需减少回合数 **或** 提高伤害效率（让对局更快结束）**或** 降低默认棋子数 |
| 你的结论 | |

### A2. 城邦机制的实现优先级

| 项 | 内容 |
|---|---|
| 来源 | 总策划案 §9.2 |
| 现状 | 6 个城邦中，贸易 / 季风 / 元素 / 战争 已落地；**欢愉（卡牌手牌）/ 邪疑（卧底）/ 战争部分档位** 未实现 |
| 需要你定 | 这三个的优先级顺序（哪个先做） |
| 你的结论 | |

### A3. 移动方式的战术差异是否需要加强

| 项 | 内容 |
|---|---|
| 来源 | 02 §10 |
| 现状 | 实现了 5 种移动方式（自由 / 直线 / 飞行 / 直线穿越 / 奇偶距离），但**它们是否真的带来不同的走位决策，还是只是数值变体**，需 playtest 判断 |
| 你的结论 | |

---

## B. 需要你补充的信息

### B1. 文档署名

| 项 | 内容 |
|---|---|
| 来源 | 总案 + 各分册的「文档信息」表 |
| 现状 | 版本记录表的"修改人"留空 |
| 需要你补 | 填谁的名字（或公司/团队名） |
| 你的结论 | |

### B2. 局外金币数值确认

| 项 | 内容 |
|---|---|
| 来源 | 总案 §6 |
| 现状 | 每局 **+100** / 胜利额外 **+200** / 棋子单价 **500** → 全解锁 5 个约 **12.5 局** |
| 需要你确认 | 是否接受这个节奏（若不接受，告诉我目标"多少局集齐"我再反推单价） |
| 你的结论 | |

### B3. 初始 4 个棋子

| 项 | 内容 |
|---|---|
| 来源 | 总案 §6 / 玩家档案系统 |
| 现状 | 默认取注册表**非召唤**棋子按 id 升序前 4：**菲林斯(1) / 杜林(10) / 哥伦比亚(11) / 克洛琳德(12)** |
| 需要你确认 | 是否就是这 4 个？如果想换（比如想让初始阵容更"新手友好"）→ 告诉我换成哪 4 个 |
| 你的结论 | |

### B4. 一个我没搞懂的参考格式

| 项 | 内容 |
|---|---|
| 来源 | 参考文档《新手策划案参考模版.docx》 |
| 现状 | 里面有张表头是「通用 / 雷 / 火 / 风 / 水 / 土」的分组表——**和本项目的"火/水/雷/冰"四元素对不上** |
| 我的判断 | 判断它是该模版自带的示例内容，**没有往项目里套** |
| 需要你确认 | 这是不是你想参考的格式？如果是，说明它是做什么用的 |
| 你的结论 | |

### B5. 一次意外的文件变更

| 项 | 内容 |
|---|---|
| 来源 | 上一轮 git 提交 |
| 现状 | 提交里带了 **4 个材质文件的改动**：`菲林斯/颜 1.mat`、`安柏/脸.mat`、`杜林/杜林-颜.mat`、`哥伦比亚/少女_闭眼-颜.mat` |
| 说明 | 不是 AI 改的（实现方明确说未触碰 .asset）→ 可能是**你调过**，或 **Unity 自动重存** |
| 需要你确认 | 是否有意为之？若是误改，回滚命令：<br>`git checkout HEAD~1 -- "Assets/Models/菲林斯/Materials/颜 1.mat"` |
| 你的结论 | |

---

## C. 需要我继续补的（文档侧，你不用管）

| # | 内容 | 状态 |
|---|---|---|
| ~~C1~~ | ~~02 §5.4：Provider 参数表~~ | ✅ **已完成**（2026-09-14）：**参数类仅 3 个**（MinMax / Cone / AreaSplash），其余 8 个 Provider **无构造参数**；同时修正了接口描述——攻击 Provider 是**两阶段语义**（`target` 为 null = 高亮返回可选目标位；非 null = 执行返回实际作用格），并记录了环切线的几何判定必须用 `HexCoord.Distance` |
| C2 | ~~**04 回合与资源** 分册~~ | ✅ **已完成**（2026-09-14）：回合结构（Turn/Round）· AP（+2/上限4）· 金币（+1/+0.3/开局50）· 背包（装备5+道具5）· 透支双入口 |
| C3 | **第 2~6 批分册**（战斗层 4 份 / 内容层 3 份 / 城邦 1 份 / 表现层 4 份 / 外围层 3 份） | ⏳ 待写 |
| C4 | **全部 markdown 定稿后统一转 Word** | ⏳ 待做 |
| C5 | ⚠️ **校正 `全局参数配置表.xlsx` 的 `ElementTile` sheet** —— 该 sheet 是我**从设计意图推断**填的（火 2 伤害 / 水 减攻 1 / 冰 减移 1），**当时没读资产**；而 `ElementTileConfig` 的代码默认值**全是 0**。需按 `ElementTileConfig.asset` 的实际值校正 | ⏳ **我的待办** |

---

## D. 写文档时发现的实现缺口 ⭐ 这部分最值得看

> 这些不是"文档没写清"，而是**梳理过程中照出来的实现空洞**——其中几条可能是真的漏做了。

### D1. 棋盘增删格**没有视觉反馈**？

| 项 | 内容 |
|---|---|
| 来源 | 01 §8 / §10 |
| 现象 | 翻遍实现没找到"格子出现/消失"的动画或特效。扩展石用下去，格子是"啪"地出现还是有生长动画？ |
| 建议 | 若确实没有 → 值得补一个短促的出现/消失表现（否则改造棋盘这个核心动作**缺乏反馈**） |
| 你的判断 | |

### D2. Provider 类名/参数填错时会**静默失败**

| 项 | 内容 |
|---|---|
| 来源 | 02 §7 |
| 现象 | `providerClassName` 与 `providerJsonParams` 都是**字符串配置**。若工厂对"类名找不到""JSON 解析失败"静默返回 null，表现是**这个棋子既不能动也不能打**——排查成本极高 |
| 建议 | 在 `RangeProviderFactory` 里对这两种情况**显式打印错误**，不要静默 |
| 你的判断 | |

### D3. 删除石的最低格数下限是否安全

| 项 | 内容 |
|---|---|
| 来源 | 01 §10 |
| 现象 | `minTilesAfter = 7` 能防止把棋盘删空，但**删到只剩 7 格时游戏是否还能正常进行**（部署区够不够放下棋子、寻路是否会退化）未验证 |
| 建议 | 实测一次极限情况，或把下限提高 |
| 你的判断 | |

### D4. 不同范围形状的高亮是否视觉可区分

| 项 | 内容 |
|---|---|
| 来源 | 02 §8 |
| 现象 | 直线、扇形、环切线、最小最大距离环——这些形状在棋盘上**能否让玩家一眼看出差别**？如果高亮只是同一套蓝色的不同排布，玩家可能感知不到形状语义 |
| 建议 | 实机看一眼：换不同棋子时，范围形状是否形成明显不同的"图形记忆" |
| 你的判断 | |

### D5. 金币透支机制**未按城邦过滤**（已知设计缺口）

| 项 | 内容 |
|---|---|
| 来源 | 04 §3.3 / §10 |
| 现象 | `GoldModel.OverdraftFloor`（透支下限）目前**全城邦生效**——但它本应是**贸易城邦的专属机制**。且 `GoldManager.Start()` 是**一次性注入**该值，时点**早于城邦确定**，要按城邦过滤需改成动态查询 |
| 影响 | 非贸易城邦下，玩家也能透支花钱——**超出设计意图**（等于所有城邦都白送了贸易城邦的核心机制） |
| 建议 | 改为运行时动态查询（由 `GoldManager` 主动问 `CityStateManager` 当前是否贸易城邦）。属**行为变更**，需你确认是否现在改 |
| 你的判断 | |

### D6. 开局 AP 与「移动的 AP 消耗规则」未核实

| 项 | 内容 |
|---|---|
| 来源 | 04 §10 |
| 现象 | ① **开局 AP 数值**未核实（推测 0，第一回合开始 +2 得 2）；② **移动的 AP 消耗规则**（每格 1 AP？还是一次移动固定消耗？）未核实 |
| 影响 | 第二条直接影响"一个回合能走多远、能不能边打边跑"——是**节奏的核心参数** |
| 建议 | 我需要读《移动》相关源码确认（属文档侧，我可以自己补） |
| 你的判断 | |

### D7. ⚠️ 减法公式的**防御收益是递增的**（结构性隐患）

| 项 | 内容 |
|---|---|
| 来源 | 05 §4.3 |
| 现象 | 伤害公式是 `max(1, attack - defense)` 的**减法**。这意味着防御的边际收益**递增**： |
| | 面对攻击 20 时——防御 5 → 受 15（减伤 25%）· 防御 10 → 受 10（50%）· 防御 15 → 受 5（75%）· **防御 19 → 受 1（95%）** |
| 影响 | **堆防御可能过强**，配合"保底 1"能形成接近免疫的效果。乘法公式（`damage × (1 - def/(def+K))`）的防御收益是**递减**的，不会这样 |
| 建议 | playtest 时**重点观察"高防棋子是否过强"**。若确实过强，两条路：① 限制防御的装备加成上限（改动小）② 换成乘法公式（结构性改动，需谨慎） |
| 你的判断 | |

### D8. 减伤链的完整顺序未核实

| 项 | 内容 |
|---|---|
| 来源 | 05 §10 |
| 现象 | 从片段看到 `GetTotalDamageReduction`（L808）与 `ApplyVulnerability`（L821），但**护盾 → 减伤 → 易伤 → 首伤=1 → 延迟拆分 → 扣血 → 免死**这条链的**完整顺序**未读全文确认 |
| 影响 | 顺序错了会导致数值异常（例如易伤被减伤吃掉、护盾拦截时机不对） |
| 附注 | `PieceManager.cs` 里有**三处** `max(1, damage - reduction)`（L280 / L478 / L776），分属哪条路径（普攻/大招/DoT）也待确认——若口径不一致是隐患 |
| 建议 | 属文档侧，我可读全文补全 |
| 你的判断 | |

### D9. ⚠️ 元素克制的**消耗不对称**（疑似 bug）

| 项 | 内容 |
|---|---|
| 来源 | 06 §3.5 / §10 |
| 现象 | `ResolveElementInteraction` 的两个克制分支写法**不对称**： |
| | · 「底方克触发方」→ `baseConsumed = max(consume / 2, 1)` ← **有保底** |
| | · 「触发方克底方」→ `trigConsumed = consume / 2` ← **无保底** |
| 后果 | 当 `consume = 1` 时，触发方消耗 = `1 / 2 = 0`（整数除法向下取整）→ **触发元素一点都没消耗，却触发了反应**。等于"白嫖一次反应" |
| 我的判断 | 从**对称性**看更像**遗漏**而非刻意——两个分支是同一件事的镜像，只给一边加保底不合逻辑 |
| 建议 | 你确认是否有意为之。若无意，给触发方分支补上 `max(consume/2, 1)`（一行改动） |
| 你的判断 | |

### D10. 大招：映射待盘 + 频率待实测

| 项 | 内容 |
|---|---|
| 来源 | 07 §5.5 / §10 |
| 现象 | ① **棋子 ↔ 大招的实际映射未核实**——现有 11 个大招类，但总案列了"7 个棋子大招未实现"，**覆盖关系不明**（本文档 §5.5 的"推测用途"列是据类名猜的）<br>② **哪些棋子配了 `baseEnergyRegen`（每回合回能）** 未盘——回能被动会**加速大招节奏**<br>③ **大招频率**：按"每回合移动 2 格 + 攻击 1 次"演算 = 每回合 25 能量 → **4 回合（约 2 分钟）满能一次** |
| 影响 | ③ 是大招节奏的核心参数。**若实测觉得大招太频繁**（每次都能放，失去"攒"的意义）→ 提高 `energyRequired` 或降低 `energyPerAttack` |
| 建议 | ①② 属文档侧，我可读 `PieceData` 补全映射表；③ 需实机确认 |
| 你的判断 | |

### D11. 菲林斯式「部分能量消耗」会**让大招频率翻倍**

| 项 | 内容 |
|---|---|
| 来源 | 07 §4.3 |
| 现象 | `IPartialEnergyUltimate` 允许"满能释放但只消耗一部分"（如菲林斯强化态保留剩余能量）。若某次只消耗 50%，则**下次只需再攒 50 就能再放** |
| 影响 | 这**不只是让大招更强，而是让下一次大招来得更快**——评估该棋子强度时，必须把这个"频率提升"算进去，否则会低估 |
| 建议 | 盘一遍用此接口的棋子（目前看是 `ThunderSweepUltimate` 雷扫 + 菲林斯相关），确认各自的 `empoweredEnergyCost` 数值 |
| 你的判断 | |

### D12. 死亡与胜负的两个未验证边界 ⚠️（其中一条涉及刷币风险）

| 项 | 内容 |
|---|---|
| 来源 | 08 §10 |
| 现象 | ① **双方同时全灭**：一次 AoE 同时打死双方最后一名棋子时，`CheckGameOver` 判谁赢？还是有平局？（规则空白）<br>② **对局结束瞬间仍在结算队列里的伤害**：`OnGameOver` 触发后，是否还会继续扣血 / 产出金币？ |
| 影响 | **② 叠加局外金币系统后风险更高**——如果"已结束"之后仍能产出金币，就成了一条**刷币路径**（玩家可以有意识地制造"结束时还在结算"的局面） |
| 建议 | **优先确认 ②**；① 是规则空白，需你定（判先手赢？还是平局） |
| 你的判断 | |

### D13. ⚠️ 两处棋子数值异常（疑似调试残留）

| 项 | 内容 |
|---|---|
| 来源 | 09 §4.2（数据取自资产实际值） |
| 现象 | ① **菲林斯：攻击 30 + 防御 10** —— 其余 9 个棋子攻击在 10~20 之间、**防御全部为 5**。菲林斯在**同 HP(100)** 的前提下，攻击高 50%、防御高 100%，**没有任何取舍**<br>② **杜林：`energyRequired = 60`** —— 其余所有棋子都是 100 → 杜林大招比所有人**快 40%**（约 2.4 回合满能 vs 4 回合） |
| 影响 | ① **明显超模**——结合 D7（减法公式下防御收益递增），菲林斯等于**同时拥有最强矛和最硬的盾**；② 若是有意设计（火焰枪快速连发是杜林特色）应写进文档，若是残留则破坏"大招节奏统一" |
| 建议 | 你确认：① 攻击调回 20 档，或把防御降回 5；② 杜林能量 60 是否有意 |
| 你的判断 | |

### D14. 9 个棋子的 `builtInPassives` 全部为空（内容层最大空缺）

| 项 | 内容 |
|---|---|
| 来源 | 09 §5.1 / §10 |
| 现象 | 全部 9 个可用棋子的**内建被动列表都是空的**——即棋子目前只有"数值 + 元素 + 范围 + 大招"，没有专属被动 |
| 影响 | 棋子差异化的维度少了一个。总案已把它列入待办（"9 棋子 builtInPassives 均空，模板已规划战士反击/法师减伤等"） |
| 建议 | 这是**内容层的重点待办**；模板与框架都已就绪（复用装备被动模板），只差配置 |
| 你的判断 | |

### D15. 被动框架的两处易错语义 + 一批待核参数

| 项 | 内容 |
|---|---|
| 来源 | 10 §3.5 / §10 |
| 现象 | ① **`OnAttacked` 与 `OnDamageReceived` 语义不同**：前者"**被攻击就触发**"（**即使伤害被护盾完全拦截**），后者"**仅真减血才触发**"。用错会让"被攻击加防"类被动（石像鬼板甲）在高防/护盾时**失效**<br>② **`OnDamageDealt` 仅走 `AttackPiece` 路径** —— 大招 / DoT 造成的伤害**不触发**它（枯萎宝珠类被动据此设计）<br>③ **19 个被动的具体参数与数值未核实**（文档 §5.3 的作用是**据类名推断**的）；24 件装备的 tier 分布也未从资产读 |
| 建议 | ①② 是使用者的语义约定（已写入文档，避免踩坑）；③ 属文档侧，我可读实现补全 |
| 你的判断 | |

### D16. 角色被动（`builtInPassives`）的实现框架已就绪但为空

| 项 | 内容 |
|---|---|
| 来源 | 10 §9 / 09 §9 |
| 现象 | 与 D14 同一件事的另一面：**装备被动有 19 个实现、11 个钩子、完整工厂**；而**棋子的角色被动列表全空**。框架复用装备被动模板（`IPassiveEffect` + `PassiveFactory`），**只差配置** |
| 影响 | 棋子差异化少一个维度（目前只有数值 + 元素 + 范围 + 大招） |
| 建议 | 属内容侧。**只需在 `PieceData.builtInPassives` 里配置**（不需要写新框架）——若需要新效果类型才写新类 |
| 你的判断 | |

### D17. 商店的三处待确认 + ⭐一个关键的平衡判断

| 项 | 内容 |
|---|---|
| 来源 | 11 §10 / §4.2 |
| 现象 | ① **全项目搜不到 `RefreshStock` 类方法** —— 商店是**固定货架**（本就不需刷新），还是**刷新功能未做**？（Roguelike 类通常需要刷新来增加决策空间）<br>② **背包满（5 格）时能否继续购买**？若没拦截会出现"买了放不下"<br>③ **拍卖行的完整规则**（竞价流程 / 结算时机 / 流拍处理）未读全文——文档 §3.4 是基于字段与名词的**推断** |
| ⭐ **关键判断** | **金币的真正瓶颈是背包格子，不是金币本身**：一局粗估约 440 金币，装备单价 50~100（理论能买 4~8 件），**但背包只有 5 格**。<br>→ 若 playtest 发现"**钱花不完但格子满了**"，那是**金币 sink 不足**（需要新的金币出口），**提高装备价格解决不了**；<br>→ 反之若"钱总不够"，才是 `goldPerDamage` 偏高或装备太贵 |
| 建议 | ①②③ 需你确认；playtest 时务必区分"钱不够"与"没地方放"这两种**性质不同**的失衡 |
| 你的判断 | |

### D18. 城邦骨架接口形同虚设 + 调试城邦会被覆盖

| 项 | 内容 |
|---|---|
| 来源 | 12 §5.5 / §10 |
| 现象 | ① **`ICityStateMechanism` 骨架只有 `NullCityStateMechanism`（空实现）且无注册路径** —— 四个已实现的城邦机制**实际都走各自 Manager 的内部判定**，这个骨架接口从未启用<br>② **调试城邦会被覆盖**：`DebugManager.SetupDebugMatch` 在 **0.1s** 设置调试城邦，而 `GameFlowController.BeginPreMatch` 在 **0.3s** 应用大厅结算结果 → **从大厅进入时调试指定被覆盖**（直接 Play 局内不受影响，因为 `CityStateResolved == false`） |
| 影响 | ① 二期接入城邦机制时**要先决定走骨架还是继续走 Manager 内部**（现在是两套并存的状态）；② 想调试特定城邦（如测战争之城主将逻辑）时会被大厅结果冲掉 |
| 建议 | ① 二期设计时一并定；② 修法很小——`BeginPreMatch` 写城邦前先判断"当前城邦是否已非 `None`"，是则不覆盖 |
| 你的判断 | |

### D19. 欢愉与邪疑两个城邦**未实现**（城邦层最大空缺）

| 项 | 内容 |
|---|---|
| 来源 | 12 §3.1 / §9 |
| 现象 | `CityState/` 下只有 `Trade` / `Monsoon` / `Element` / `War` 四个城邦子目录——**欢愉（卡牌/手牌）与邪疑（卧底/归属转换）完全没有实现**。选中它们时本局只有"商店城邦装备池变化"，**无专属机制** |
| 影响 | 6 个城邦里 2 个是"空壳"——玩家选中后会感到落差（以为有特殊玩法，实际没有） |
| 复杂度评估 | 欢愉 = **二档**（全新手牌系统）；邪疑 = **三档**（改棋子归属与胜负判定，**规则歧义最多，须单独评审**） |
| 建议 | 优先做欢愉（自成体系、不影响既有规则）；邪疑需先做设计评审再动 |
| 你的判断 | |

### D20. 棋子表现的两处待验证

| 项 | 内容 |
|---|---|
| 来源 | 13 §10 |
| 现象 | ① **动作宿主方案只验证过菲林斯**的 prefab 结构。方案声称对"Renderer 在根 / 在子节点"两种结构都通用，但**其余 9 个棋子的结构未逐个确认**（曾因结构假设错误导致"动作完全失效"）<br>② **`PieceAuraMaterial` 是否已创建** —— 未创建时**元素光环与大招扩散会静默跳过**（仅一条 Warning），很容易被误认为"这功能没做" |
| 建议 | ① 实机时抽几个不同棋子打一下，确认动作都正常（若某个棋子不动作，就是结构问题）；② 确认 `Assets/Game/Resources/PieceAuraMaterial.mat` 存在 |
| 你的判断 | |

### D21. 表现层缺三样素材（音效 / 粒子 / 动画）

| 项 | 内容 |
|---|---|
| 来源 | 13 §8 / §3.1 |
| 现象 | ① **全项目零音频代码**（仅 4 个空接入位：攻击/受击/死亡/召唤）② **全项目零粒子代码**（零素材方案已覆盖基础反馈，但做不出"火花/烟尘"级别表现）③ **10 个角色 fbx 均无战斗动画剪辑**（仅 2 个角色有 Idle/Walk/Run） |
| 影响 | 表现在"信息传递"层面已达标（能看懂谁打谁），但在"感受放大"层面受限 |
| 建议 | **三者都需要素材，属独立的资源类工作**——建议单独排期，不要与逻辑开发混在一起。**音效性价比最高**（一声提示的信息量常胜过一堆粒子） |
| 你的判断 | |

### D22. 跳字的两个小缺口

| 项 | 内容 |
|---|---|
| 来源 | 14 §10 |
| 现象 | ① **4 个非伤害样式色仍写在代码里**（`FloatStyles` 静态类：反应紫 / 回血绿 / 护甲蓝 / 暴击金），而 `GameConfig` 里已经有 16 个动画参数 → **想调这 4 个颜色必须改代码**（源码注释自己也说"后续可收进 GameConfig"）<br>② **跳字重叠**：AOE 多目标、或同一目标多段伤害时，多个跳字可能**重叠遮挡** |
| 影响 | ① 违反"平衡参数集中在 `GameConfig`"的项目约定（调参要改代码 = 容易被忽略）；② AOE 场景下跳字可读性下降 |
| 建议 | ① 把这 4 个色收进 `GameConfig`（改动很小）；② 评估是否需要错位 / 合并机制 |
| 你的判断 | |

### D23. `FloatStyles` 与 `GameConfig` 的职责重叠

| 项 | 内容 |
|---|---|
| 来源 | 14 §5.3 |
| 现象 | `FloatStyle` 的注释写"本阶段不暴露到 Inspector，由 `FloatStyles` 静态预设库提供实例，**后续可收进 `GameConfig`**"；但 `GameConfig` 里**已经**有 16 个跳字动画参数 → **两处都能定义跳字参数**，实际分工不清 |
| 影响 | 后来者不知道该去哪改（可能改了一个发现不生效） |
| 建议 | 明确分工：**数值参数全归 `GameConfig`，`FloatStyles` 只保留"非伤害样式"的预设**（或干脆一并收进配置） |
| 你的判断 | |

### D24. ⚠️⚠️ 局外金币的「通胀风险」（经济系统最重要的问题）

| 项 | 内容 |
|---|---|
| 来源 | 18 §4.2 |
| 现象 | **金币只有一个 sink（购买棋子），且总量有限**：5 个待解锁 × 500 = **2500 金币**。**买完 9 个棋子后，金币再无任何用途** —— 但每局仍在产出（+100 / +300）→ **只增不减，变成无意义的数字** |
| 性质 | 典型的"**source 找不到对应 sink**"。按设计原则：任何 source 没有 sink，通胀只是时间问题 |
| ⭐ 四个可选解法 | **A. 增加 sink**（重掷城邦 / 开局买装备 / 棋子皮肤）<br>**B. 提高单价或增加棋子**（治标，最终仍会买完）<br>**C. 局外金币可转局内初始金币**（**我倾向这个** —— 让"攒金币"在**每一局**都有意义，打通两个经济系统；但需重新评估局内金币平衡，且与《商店》D17"瓶颈是格子"的判断相互关联）<br>**D. 接受上限**（明确"集齐即毕业"，定位为**一次性成长线** —— 最简单，但要写清这是设计意图） |
| 建议 | **优先决策** —— 它决定局外经济的整个定位，也影响将来要不要继续做长期成长 |
| 你的判断 | |

---

## 📊 进度快照（2026-09-14）

| 批次 | 内容 | 状态 |
|---|---|---|
| — | 项目总策划案 | ✅ |
| 第 1 批 核心规则层 | 01 棋盘与地图（含寻路）· 02 范围与空间 · 04 回合与资源 | ✅ |
| 第 2 批 战斗层 | 05 伤害计算 · 06 元素反应 · 07 大招与能量 · 08 死亡与胜负 | ✅ |
| 第 3 批 内容层 | 09 棋子 · 10 被动与装备 · 11 商店 | ✅ |
| 第 4 批 城邦层 | 12 城邦系统 | ✅ |
| 第 5 批 表现层 | 13 棋子表现（动作+视觉反馈合并）· 14 跳字系统 · 15 元素格 | ✅ |
| 第 6 批 外围层 | 16 大厅与对局配置 · 17 玩家档案 · 18 局外金币经济 | ✅ |

**共 17 份文档全部完成**（1 份总案 + 16 份分册）。**D 类实现缺口累计 24 条。**

**剩余待办**：C5 校正 `ElementTile` sheet · C4 统一转 Word

---

## E. 待验证的数值汇总（全部未经 playtest）

| # | 数值 | 当前值 | 来源 | 验证方式 |
|---|---|---|---|---|
| E1 | 单局时长 | 20~40 min `[PLACEHOLDER]` | 总案 | 实测（失败信号：>40min） |
| E2 | 局外金币 | 每局 +100 / 胜 +200 | 总案 | 玩 12 局看解锁节奏是否合理 |
| E3 | 棋子单价 | 500 / 个 | 总案 | 同上 |
| E4 | 初始棋池 | 4 个 | 总案 | 看新玩家是否觉得受限 |
| E5 | 棋盘道具售价 / AP | 扩展石 30 / AP 1（示例） | 01 | playtest |
| E6 | 动作时长 | 攻击 0.25 / 受击 0.2 / 死亡 0.3 s | 总案 | ✅ **已实机验收** |
| E7 | 视觉反馈 | 闪白 0.8 / 光环半径 0.6 | 总案 | 实机手感 |
| E8 | 城邦揭晓提示时长 | 2 s `[PLACEHOLDER]` | 总案 | 实机（快慢是否合适） |

---

## F. 已确定、不需你处理的事项（备查）

| 项 | 结论 | 时间 |
|---|---|---|
| `03 寻路` 是否独立成册 | **并入 01** | 2026-09-14 |
| 策划案与 Word 的先后顺序 | **先写完 md，最后统一转 Word** | 2026-09-14 |
| 配置表位置 | 统一放 `配置表\`（项目内） | 2026-09-13 |
| `杜林_配置生成器.xlsx` | 已废弃，移出项目 | 2026-09-13 |
