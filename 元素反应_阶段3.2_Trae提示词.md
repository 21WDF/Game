# 阶段③.2（四元素之力的 IReactionModifier 部分）· Trae 提示词

## 总览

四元素之力（潮涌/灼火/陨雷/凝冰）的**元素反应修饰**部分：倍率增强、感电延长、冻结延长、超导减防增强。四件装备**共用一个被动类** `ElementalCorePassive`，用 `element` 参数区分元素。

本阶段**只做 `Modify`（IReactionModifier）**；元素量加成（普攻 +1 元素量）和超载爆炸 AOE 留 ③.3。

## 参数

### jsonParams（1 个）
| 字段 | 类型 | 默认 | 说明 |
|---|---|---|---|
| element | string | "Water" | 元素类型：Water / Fire / Thunder / Ice（string 承接 + 手动解析，JsonUtility 枚举只认数值） |

### PassiveConfig 透传（非 jsonParams）
- `uniqueId`：四件装备都配 **`"ElementalCore"`**（同名唯一覆盖——玩家只能带一个元素之力，后装备的覆盖先装备的）。
- `passiveName`：如 `"核心"`。

## 机制（Modify 按 element + 反应类型改字段）

| element | 反应类型 → 修改 |
|---|---|
| **Water**（潮涌） | Vaporize → `DamageMultiplier += 0.2f`；ElectroCharged → `DotTurns += 2`；Frozen → `FreezeTurns += 1` |
| **Fire**（灼火） | Vaporize / Melt → `DamageMultiplier += 0.2f` |
| **Thunder**（陨雷） | Superconduct → `DefenseReduction += 5` 且 `DefenseReductionTurns += 2`；ElectroCharged → `DotTurns += 2` |
| **Ice**（凝冰） | Superconduct → `DefenseReduction += 5` 且 `DefenseReductionTurns += 2`；Melt → `DamageMultiplier += 0.2f`；Frozen → `FreezeTurns += 1` |

要点：
- **倍率用加法**（`+= 0.2f`），与光界之力（`+= percent/100f`）一致。
- `Modify` 只拿到 `ReactionConfig`（含 `Type`），按 `Type` 判断；`CollectReactionModifiers` 从**攻击方**收集，天然限定「携带者作为触发方」才生效。
- `ReactionType.None` 不处理（无反应不改）。
- struct 传参即副本，直接改后返回。

## 改动

1. **新建 `Assets/Game/Equipment/Effects/Elemental/ElementalCorePassive.cs`**：
   - 同时实现 `IPassiveEffect` + `IReactionModifier`。
   - 构造接收 `ElementType element` + `string uniqueId`。
   - 覆写 `UniqueId => _uniqueId`。
   - `Modify` 按上表 `switch (_element)` 修改字段（每元素一个 case）。
   - `OnEquip` / `OnUnequip` 空实现（其余钩子走默认）。

2. **`PassiveFactory.cs`**：
   - 加 `case "ElementalCorePassive"` + 参数类 `ElementalCoreParams`（`[System.Serializable]`，`element` string 默认 "Water"）。
   - 加 `ParseElement(string)` 解析方法：不区分大小写，`"Fire"→Fire`、`"Thunder"→Thunder`、`"Ice"→Ice`，无法识别回退 `Water`（对标 `ParseAuraDirection` / `ParseFreeAPType` 写法）。
   - case 内把 `Create` 收到的 `uniqueId` 透传给构造。

## 约束（务必遵守）

1. **只做 Modify**，不要做元素量加成、超载爆炸（③.3 才做，届时再扩展本类）。
2. 倍率用**加法** `+= 0.2f`，不用乘法。
3. 唯一被动走 C2 框架：`UniqueId` 覆写 + 工厂透传 `uniqueId`，不要另写去重逻辑。
4. `ElementalCorePassive.cs` 放在 **`Elemental/` 子文件夹**（已建好）。
5. 诊断无报错后收尾。

## 备注（③.3 再做）
- 元素量加成：普攻 +1 对应元素量（需在 AttackPiece 元素附着处介入）。
- 超载爆炸 AOE：灼火/陨雷之力，超载时周围 1 格额外伤害。
