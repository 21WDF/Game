# 阶段②被动框架 2.2A v2（被动参数化 + 归属修正）· Trae 提示词

## 背景
2.2A 已实现的 4 个被动需要按新需求扩展参数 / 修正数据归属。只改这 4 个被动类 + PassiveFactory 参数类 + PieceModel 的 EffectiveAttack，其他文件不动。

## 一、LifestealPassive（饮血剑）—— 吸血自定义计算
参数从 `percent` 扩展为：
- `baseHeal`：固定吸血量
- `percent`：伤害百分比吸血

公式：吸血量 = baseHeal + floor(伤害 × percent / 100)，Heal 封顶 MaxHP。
（baseHeal=0 即纯百分比；percent=0 即纯固定；都有即混合。）

## 二、ThornsPassive（荆棘刺甲）—— 反伤参数化 + 范围反伤
参数扩展为：
- `amount`：基础反伤值
- `percent`：按受击伤害比例反伤（0 = 纯固定）
- `affectedByDefense`（bool）：true = 反伤被目标防御减伤；false = 真实伤害
- `reflectRadius`（int）：反伤范围半径，以受击者为中心
- `alwaysReflectAttacker`（bool）：true = 攻击者必反；false = 只反半径内敌人

反伤值 = amount + floor(受击伤害 × percent / 100)。
对每个目标：最终反伤 = affectedByDefense ? max(1, 反伤值 - 目标 EffectiveDefense) : 反伤值。

反伤目标集合（按以下规则确定）：
- reflectRadius = 0：只反攻击者（单体，不受 alwaysReflectAttacker 影响）
- reflectRadius ≥ 1 且 alwaysReflectAttacker = true：攻击者必反 + 受击者周围 radius 内所有敌方（攻击者若也在半径内需去重，只反一次）
- reflectRadius ≥ 1 且 alwaysReflectAttacker = false：只反受击者周围 radius 内所有敌方（远程攻击者距离 > radius 则不反）

范围判断用 HexCoord.Distance（受击者坐标 → 敌人坐标）。

⚠️ 关键约束：反伤致死会触发 DestroyPiece 修改棋子列表，遍历前必须先收集目标到临时列表（快照），再逐个反伤，避免遍历中修改集合。

反伤仍直接 TakeDamage（不走 AttackPiece，防递归）；致死逐个 DestroyPiece(target, owner)。

## 三、RegenPassive（生命铠甲）—— 进化回血
参数从 `amount` 改为：
- `baseAmount`：基础每回合回血
- `upgradedAmount`：进化后每回合回血
- `upgradeThreshold`：连续未受伤回合数阈值

OnTurnStart 逻辑：owner.TurnsSinceDamaged >= upgradeThreshold 回 upgradedAmount，否则回 baseAmount。

## 四、KillAttackPassive（死亡之刃）—— 叠层归属修正
当前叠层写在 PieceModel.KillAttackBonus（棋子身上），导致卸下装备后叠层残留。改为：
- 删除 PieceModel.KillAttackBonus 字段
- KillAttackPassive 内部累积叠层（实例字段），OnKill 累加，暴露 Accumulated 只读属性
- EffectiveAttack 改为聚合所有被动里的 KillAttackPassive.Accumulated（去掉对 KillAttackBonus 的引用）

这样叠层跟随被动实例（= 装备），卸下自动移除、重装保留。

## 五、PassiveFactory 参数类同步
更新对应参数类字段：
- LifestealParams 加 baseHeal
- ThornsParams 加 percent / affectedByDefense / reflectRadius / alwaysReflectAttacker
- RegenParams 改为 baseAmount / upgradedAmount / upgradeThreshold

全部保持 [System.Serializable]；bool 参数用 JSON true/false 解析。

## 约束（必须遵守）
1. 只改：4 个被动类 + PassiveFactory 参数类 + PieceModel（EffectiveAttack 及删 KillAttackBonus），其他文件一律不动
2. AttackPiece 的伤害介入（附加伤害 / 减伤）本批不动
3. 不重构无关代码，不"顺手优化"
4. 完成后自查诊断无报错即可，运行时验证由我方做
