# 季风之城三期B（飓风·幸运方块掉落物）— Trae 提示词

> 铁律：只写「改动方向 + 效果目标 + 红线」，不写实现细节。

## 〇、背景（一句话）

三期气候复杂组第一块实体系统：**飓风**——出现时商店关闭、棋盘随机空格生成「幸运方块」，**双方共享可见、共同抢**；棋子移动经过即拾取，按方块价值档位随机开出装备/道具/金币。这是项目首个「附着在格子上的实体」，其数据/渲染/生命周期机制**三期C 雷电残留会复用**。

## 一、现状（框架事实）

1. **气候框架已就绪**：`MonsoonManager.TickAndRollClimates`（每轮判定 + 持续 1~3 轮递减）、`RollNewClimates`（独立概率 + 独占）、`IsClimateActive(kind)`（内聚过滤首行 `MonsoonActive`）。`ClimateEffectKind` 现有 `None/Rain/Blizzard/Heat`，`ClimateEntry` 已通用化（三期A，无 value）。
2. **背包/金币入口**：装备无偿入背包 `EquipmentManager.GrantEquipment(side, EquipmentData)`（public）；**道具侧 `GridItemManager.AddToInventory` 是 private，无「无偿获得道具」公共入口——本期需暴露一个（对标 GrantEquipment）**；金币 `GoldManager.AddGold(side, amount)`。
3. **装备/道具字段**：`EquipmentData.tier`（Basic/Intermediate/Advanced/CityState）、`EquipmentData.price`；`GridItemData.price`。商店列表 = `EquipmentManager.GetShopItems()` / `GridItemManager.GetShopGridItems()`。
4. **移动路径遍历有先例**：`PieceManager.MovePieceAlongPath(piece, path)` 拿到完整 `path`（含起终点），移动开始时已遍历 path 触发路径伤害（`VoltPathPassive`）。方块「经过即拾取」复用此 path 遍历点。
5. **格子叠加层渲染**：`ChessBoardController.GetTile(coord).overlay.Show(color, alpha)` 战术层；`AllCoords` 遍历；`PieceLayoutModel.GetPieceAt(coord)` 查占据（「随机空格」用）。
6. **商店开关**：`InputHandler` 按 B 键触发 `OnShopToggle` → `UI_ShopPanel.Toggle` 开/关。飓风「商店不可开」在此拦截（判断内聚 MonsoonManager）。
7. **回合/轮语义**：一轮 = P1 回合 + P2 回合；`MonsoonManager.OnTurnStarted(side)` 每回合开始调用，二期气候判定已用「轮号守卫」保证每轮只判定一次。
8. 常驻红线：气候「是否生效」内聚在 `MonsoonManager`，不散落到移动/商店/回合调用点。

## 二、改动方向

### 1. 枚举 + 气候条目 + 专属配置区块

- `ClimateEffectKind` 追加 `Hurricane`（飓风）。
- `climates` 列表追加飓风条目（复用 `ClimateEntry` 通用字段；概率默认 0.05、无季节限制、非独占）。
- MonsoonConfig 新增「飓风参数」区块：
  - **每轮掉落概率**（默认 0.5，可配）——飓风激活期间，每轮「掉不掉方块」的概率；
  - **掉落数量**（默认 2，可配）——每批掉落几个方块（一轮两批）；
  - **幸运方块价值档位列表**（`LootBoxTier`，默认 4 档，可增删）。
- `LootBoxTier`（每格都是可配字段、代码零硬编码）：

| 字段 | 超高(红) | 高(金) | 中(蓝) | 低(灰) |
|------|---------|--------|--------|--------|
| 方块贴图 / 颜色 | 红 | 金 | 蓝 | 灰 |
| 概率权重 | 0.05 | 0.15 | 0.30 | 0.50 |
| 普通装备 tier | Advanced | Intermediate | Basic | 空 |
| 城邦装备价格范围（tier=CityState） | 空 | ≥50 | <50 | 空 |
| 道具价格范围 | 空 | ≥50 | <50 | 空 |
| 金币范围 | 100~150 | 20~60 | 10~20 | 1~6 |

> 价格分界「50」本身是字段值（金色档 min=50、蓝色档 max=49），概率权重、tier、价格/金币范围全是字段，可随时调。

### 2. 掉落（平衡保底：成对、同品质、同数量）

- 掉落按**轮**决策（轮号守卫，每轮只判定一次）：
  - 每轮开始判定「这一轮掉不掉方块」（按每轮掉落概率）；
  - 若掉：按价值档位**概率权重随机一个档位**（这一轮所有方块同一档位），数量 = 掉落数量；
  - **P1 回合开始、P2 回合开始各掉一批**——两批品质、数量完全一致；
  - 若不掉：这一轮两个回合都不掉。
- 即：**要么一轮两回合都不掉，要么两回合各掉「同品质、同数量」的一批**（默认每批 2 个），保证先手/后手对称公平。
- 每批方块落在棋盘**随机空格**；方块是**对局级实体，双方都可见、都能抢**（非单方专属）。
- 随机入口收敛到 MonsoonManager 单一方法（与其他气候判定同源；将来联机换 seed）。

### 3. 幸运方块实体（首个「附着在格子的实体」，机制供三期C 复用）

- 数据层：MonsoonManager 内聚记录「坐标 → 方块（档位）」，非季风城邦 / 飓风未激活时无任何方块。
- 渲染层：方块所在格用该档位颜色做战术层高亮（颜色取档位字段）。
- 玩家拾取前**只看得到价值档位（贴图/颜色），看不到具体内容**。
- 生命周期：方块保留在棋盘，直到**被拾取**或**飓风结束统一清除**（高亮还原）。
- 「格子实体」的数据记录 + 渲染 + 生命周期尽量做成可复用基础（三期C 雷电残留复用），不过度抽象。

### 4. 拾取 + 开出内容

- 触发：棋子移动**经过**方块格即拾取（路径所有节点含终点；**落点不必是该格**，中途经过也算），判断内聚 MonsoonManager。
- 开出：按该方块的档位内容规则随机——先在该档位「非空的内容来源」里随机一种（普通装备 / 城邦装备 / 道具 / 金币），再在对应来源里随机具体对象：
  - 装备 → `EquipmentManager.GrantEquipment` 进装备背包；
  - 道具 → 走 GridItemManager 新增的「无偿获得道具」入口进道具背包；
  - 金币 → `GoldManager.AddGold`。
- 拾取后方块消失、侧栏/金币自动刷新（走既有事件）。

### 5. 商店关闭

- 飓风激活期间：商店面板不可打开（B 键无效）。判断内聚在 MonsoonManager（提供一个「商店是否被飓风关闭」入口），`UI_ShopPanel.Toggle` 只调该入口、不判断城邦。

### 6. 内聚过滤（关键）

- 掉落 / 方块 / 拾取 / 开出 / 商店关闭的「是否生效」判断全部内聚在 `MonsoonManager`，调用点（移动流程 / 商店开关）只调一个入口方法。

## 三、效果目标（验收标准）

1. 选到季风城邦 → 飓风按概率出现、持续 1~3 轮随机、可叠加、有日志。
2. 飓风期间：B 键打不开商店；棋盘随机空格出现幸运方块，**双方都可见、都能抢**。
3. **平衡保底**：一轮内两个回合要么都不掉、要么各掉「同品质、同数量」一批（默认每批 2 个）；一轮两批品质、数量完全一致，先手/后手无差异。
4. 棋子移动**经过**方块格（落点不必是该格）→ 自动拾取，按档位开出内容：装备进装备背包、道具进道具背包（侧栏刷新）、金币直接加；方块消失。
5. 方块保留到被拾取或飓风结束；飓风结束未拾取方块全部消失、高亮还原、商店恢复可开。
6. 方块档位（贴图/颜色/权重/tier/价格范围/金币范围）、每轮掉落概率、掉落数量——全部从配置读取，代码零硬编码，Inspector 可改。
7. 其他城邦 → 无方块、商店正常开，飓风完全无效果。
8. 二期暴雨/暴雪/高温、四季昼夜、伤害管道、元素、护盾、贸易、城邦选择/过滤、背包系统——行为不变。

## 四、红线（绝不能破坏）

1. **内聚过滤**：飓风「是否生效」判断内聚在 MonsoonManager，不散落到移动/商店/回合调用点。
2. **复用现有落点**：方块拾取走既有背包/金币入口；路径经过走既有 path 遍历点；渲染走既有格子高亮；商店关闭走既有开关入口。**背包/金币/移动逻辑只调用、不改**。
3. **时间单位铁律**：掉落决策、飓风持续递减按「轮」计（轮号守卫），不按「回合」；掉落**执行**在 P1/P2 回合开始时各一批。
4. **数据驱动**：掉落概率、掉落数量、档位规则（贴图/颜色/权重/tier/价格分界/范围）全从配置读取，代码零硬编码。
5. **平衡保底铁律**：一轮内两批必须同品质、同数量（或都不掉），不得出现「一方回合掉、另一方不掉」或「两批品质/数量不一致」。
6. **不破坏既有功能**：四季昼夜、暴雨/暴雪/高温、伤害管道、元素反应、护盾、贸易、城邦选择/过滤、背包系统零改动。
7. 拾取/开出失败（背包满、金币异常、棋子死亡）不得产生副作用（不凭空加、不重复扣、不残留脏数据）。

## 五、明确不做

- 雷电残留、雷劈永久加成（三期C）。
- 幸运方块的内容预览/透视（拾取前只显示价值档位）。
- 方块手动拾取/拖拽交互（移动经过自动拾取即可）。
- 方块跨城邦通用（只属季风城邦）。
- 永久属性加成、AP 上限/回能属性。
