# 季风之城一期（四季 + 昼夜，数据驱动配置）— Trae 提示词

## 现状（框架事实）

- 季风之城是「当前城邦」之一，其专属机制尚未实现。其余已完成城邦的专属机制都遵循「内聚过滤」模式：机制自身判断「当前城邦 == 所属城邦」才生效。
- 项目已有成熟的「数据驱动」惯例：反应系统用 ScriptableObject 数据驱动（Inspector 加行不改代码）、被动框架用「效果类 + 字符串配置」。本次季风机制应遵循同一惯例，做**集中可配置**，而非硬编码。
- 现有可复用落点：棋子回血、棋子扣血（死亡判定已有）、金币增减、有效攻击力聚合、移动范围聚合（需保证下限）、轮次计数（双方各行动一次 = 1 轮）。

## 改动方向

新增季风之城的专属机制（一期只做「四季 + 昼夜」，气候留待二期），采用**集中数据驱动配置**，并遵循「内聚过滤」（机制只在「当前城邦 == 季风城邦」时生效）：

1. **集中配置资产**：新建一个「季风城邦配置」ScriptableObject，集中承载四季、昼夜（以及未来气候）的配置条目，运行时由季风城邦管理器读取。改数值/换效果走资产配置，不改代码。
2. **效果类型抽象**：四季/昼夜的效果统一抽象为「效果类型 + 数值」的配置项（效果类型用一个可扩展的枚举表达，一期先含：回血、攻击、金币、扣血、移动 这五类），每个季节/昼夜槽位 = 一个效果类型 + 一个数值。
3. **四季**：**固定 4 个槽位**（春夏秋冬），按「轮」推进，每 6 轮切换一季、循环。每个槽位的效果（类型 + 数值）从配置资产读取，默认值对应：春=回血 +1、夏=攻击 +2、秋=金币 +10、冬=扣血 -1（可致死）。
4. **昼夜**：**固定 2 个槽位**（昼/夜），与季节对齐，每季前 3 轮为昼、后 3 轮为夜。昼/夜的移动修正值从配置读取（默认昼 +1、夜 -1，且夜的下限 1 格）。
5. **状态存放**：季节/昼夜状态作为「对局级」数据，内聚在季风城邦专属管理器（对标其他城邦机制），切换与效果结算都走该管理器入口，不散落到回合流转/PieceModel。

## 效果目标（验收标准）

1. 选到季风城邦 → 四季按 6 轮/季循环、昼夜按前 3 昼后 3 夜循环，效果从配置资产读取并生效。
2. 春（回血+1）：该方回合开始全体回血；夏（攻击+2）：有效攻击力聚合生效；秋（金币+10）：该方回合结束 +10；冬（扣血-1）：该方回合结束全体 -1 血（可致死）。
3. 昼：行动 +1；夜：行动 -1 且下限 1 格。
4. **改配置资产里的数值 → 运行时生效**（无需改代码即可调四季/昼夜数值或换效果类型）。
5. 其他城邦 → 四季/昼夜机制不生效，棋子无任何季节/昼夜加成。
6. 季节/昼夜切换有清晰日志，便于验证节奏。

## 红线（绝不能破坏）

1. **内聚过滤**：季节/昼夜的「是否生效」判断内聚在季风城邦管理器内部，不散落到 TurnManager、PieceModel 等调用点。
2. **复用现有落点，零新系统**：回血/扣血/金币/有效攻击力/移动范围都走既有结算点与聚合；本期不新建资源或实体系统。
3. **数据驱动，不硬编码数值**：四季/昼夜的数值与效果类型从配置资产读取，代码里不写死「回血 1、攻击 2」这类常量（配置资产本身可提供默认值）。
4. **可致死语义**：冬的扣血走正常扣血结算，死亡判定照常（不额外设下限保护）。
5. **昼夜移动下限**：夜的行动距离 -1 后至少可行动 1 格（下限 clamp）。
6. **时间单位**：季节/昼夜切换严格按「轮」计，不按「回合」。
7. **气候机制不做**：本期只搭「四季 + 昼夜 + 配置框架」，不实现任何气候（飓风/暴雨/暴雪/雷暴/高温留待二期），但配置资产的结构应预留扩展空间（如可加气候条目列表）。
8. 伤害类型、元素系统、护盾、贸易机制、城邦选择/过滤等已完成功能零改动。
