# 城邦选择遮罩残留修复 — Trae 提示词

## 问题本质（现状）

城邦选择面板的「关闭」逻辑目前只关闭了面板主体（`panelRoot`），**没有关闭揭晓遮罩（`revealRoot`）**。而揭晓时遮罩被打开，于是点「继续」进入选棋子后，遮罩仍然残留、全屏挡在选棋子界面之上，导致选棋子界面可见但点不到。

## 改动方向

关闭城邦选择面板时，**同时关闭揭晓遮罩**，让遮罩与面板一起隐藏。

## 效果目标（验收标准）

1. 揭晓界面点「继续」后，遮罩随面板一起消失，选棋子界面能正常点击。
2. 面板关闭逻辑（面板开闭计数）行为不变。

## 红线（绝不能破坏）

1. 面板开闭计数逻辑（`RegisterPanelOpen`/`RegisterPanelClose` 的配对）零改动。
2. 其余流程（多选、提交、揭晓、进入选棋子）零改动。
