# 直线穿越移动路径修复 · Trae 提示词

## 问题

`StraightPassProvider`（直线穿越移动）在运行时路径错误：选中棋子后，可移动范围的高亮是正确的（穿过棋子的空格都被高亮了），但点击落点后，**路径预览线和棋子的实际移动走了 BFS 绕路**，没有沿直线穿越。

## 根源

移动的「可达范围」和「路径生成」目前是两套独立逻辑：
- 可达范围按各移动 Provider 的规则计算（直线 / 穿越 / 飞行 / 跳跃各有规则）
- 路径生成统一走 BFS 最短路径，把棋子当阻挡

两套规则不一致，「穿过棋子」这类移动的高亮和实际路径就对不上。这个问题不只影响直线穿越——飞行（穿过棋子）、跳跃（瞬移）类移动也会遇到同样的问题。

## 修复目标

让路径生成（预览线 + 实际移动）**跟随棋子的移动规则**，而不是统一 BFS。各移动规则的路径效果应为：
- 直线移动：路径就是一条直线
- 直线穿越：路径沿直线穿过棋子
- 飞行：路径可飞越棋子，落点必须是空格
- 跳跃：直接到达落点，无中间路径
- 普通移动：保持现状（BFS 最短路径）

其中「穿过 / 飞越棋子」的移动，棋子实际表现上不能逐格占据经过的敌方格子，应直接到达终点。

## 涉及文件

- `Assets/Game/Piece/Range/` 下的移动范围 Provider（接口及实现类）
- `Assets/Game/Battle/Controller/BattleController.cs`（移动执行与路径预览）
- 可参考 `Assets/Game/ChessBoard/Model/ChessBoardModel.cs` 的 `FindPath`（当前统一寻路入口）

## 不要破坏

1. 可达范围的计算逻辑（高亮是对的，不要动）
2. 普通移动（FreeMove）的行为和路径
3. 途经点（waypoint）的多段路径选择机制
4. 现有的逐格移动动画（`MovePieceAlongPath` / `AnimateMove`）

## 验收要点

- 路径预览线和实际移动必须走**同一条路径**，避免「预览直线、移动绕路」再次出现
- 直线穿越棋子能正确穿过、落点在空格
- 普通移动（FreeMove）行为与修复前完全一致
