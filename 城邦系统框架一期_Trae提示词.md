# 城邦系统框架一期 · Trae 提示词

## 现状（框架事实，帮你理解问题本质）

项目是 Unity 6 本地双人热座 Hex 战棋，MVC + ScriptableObject。城邦系统（Pillar 3）之前悬空，但架构已经埋好了几个扩展点，你只需要激活它们：

1. **商店已有城邦档**：`EquipmentData.tier` 枚举已有 `CityState = 3`；`ShopTab` 枚举已有 `CityState = 3`。
2. **商店已留过滤钩子**：`UI_ShopPanel.ShouldShowCityState(EquipmentData)` 目前硬编码 `return true`，注释明写「城邦动态展示扩展点」。
3. **战前流程**：`GameFlowController` 的流程是 `P1Select → P1Deploy → P2Select → P2Deploy → Playing`，城邦选择要插在最前面。
4. **染色开关**：`GameConfig.shieldElementDyeingEnabled` 是独立布尔，注释写「未来城邦系统接管」。本期不动它。

## 改动方向

### 一、城邦枚举与装备归属

新增一个城邦类型枚举，共 **6 个城邦**：贸易、欢愉、季风、邪疑、战争、元素。

给 `EquipmentData` 增加一个「归属城邦」的配置字段：`None`（通用装备，不归属任何城邦）或 6 个城邦之一（城邦专属装备）。

### 二、城邦管理器（核心）

新建一个城邦管理器单例（对标现有 `GoldManager`/`TurnManager` 的写法），职责只有三块：

1. **持有当前城邦**：记录本局生效的城邦，并在变化时发事件通知（对标现有 `OnXxxChanged` 事件风格）。
2. **城邦选择流程的纯逻辑**：双方各提交 4 个心仪城邦 → 求出双方重合部分 → 从重合部分随机定 1 个作为本局城邦。这个「求交集 + 随机」必须是纯函数（输入双方选择，输出结果城邦），不依赖任何 UI。
3. **商店过滤查询**：提供一个查询方法，判断某件装备在当前城邦下是否应该展示——规则是「装备 tier 是城邦档 且 装备归属城邦 == 当前城邦」。

### 三、城邦专属机制接口（只建骨架，不实现）

定义一个城邦专属机制的接口，预留三个钩子：游戏开始时、每个回合开始时、每个回合结束时。本期只定义接口和「空实现/无机制」的默认情况，不写任何具体城邦机制（拍卖/卡牌/四季/卧底/将帅都是二期的事）。

### 四、战前流程插入城邦选择阶段

在 `GameFlowController` 的战前流程最前面插入「城邦选择」阶段。由于本期不做选择 UI，先用一个**临时的自动选择占位**（例如直接随机一个城邦，或写死一个默认城邦）让流程能跑通到开战。真正的「双方各选 4 个」交互 UI 是二期的事，但城邦管理器的「求交集+随机」纯逻辑本期就要建好，UI 二期只调用它。

### 五、商店过滤接线

把 `UI_ShopPanel.ShouldShowCityState` 从「硬编码 return true」改成调用城邦管理器的过滤查询。过滤规则见上文「二.3」：城邦档装备只在「归属城邦 == 当前城邦」时展示。

## 效果目标（验收标准）

1. 能确定「本局城邦」：城邦管理器在战前流程被初始化，本局城邦有值且可查询。
2. 双方各提交 4 个心仪城邦时，能正确算出重合部分并从中随机定 1 个（纯函数，可脱离 UI 调用）。
3. 城邦档装备只在「归属城邦 == 当前城邦」时出现在商店，其他城邦的专属装备不显示。
4. 通用装备（归属 = None）行为不变，不受城邦影响。
5. 战前流程能从「城邦选择（临时占位）」顺畅过渡到 `P1Select`，最终开战。
6. 城邦专属机制接口已定义，本期无任何具体机制实现，游戏行为与之前完全一致（除了城邦选择和城邦档装备过滤）。

## 红线（绝不能破坏）

1. **不动染色开关**：`GameConfig.shieldElementDyeingEnabled` 保持独立布尔、默认 true，本期不碰它，也不碰护盾染色的任何逻辑（PieceModel 的 BlocksElementAttachment / TryDyeShieldElement 零改动）。
2. **不动伤害管道、元素附着、元素反应**：这三个系统刚完成闭环，任何改动都要避免波及。
3. **不动现有装备/道具/商店的购买流程**：只改「城邦档装备的显示过滤」，不改购买、背包、装备/卸下、金币结算。
4. **通用装备零影响**：归属 = None 的装备，展示和购买行为与改造前完全一致。
5. **不动选棋子/部署流程**：城邦选择阶段只插在 `P1Select` 之前，P1Select 之后的流程顺序和逻辑保持不变。
6. **在线 PVP 预留（重要）**：城邦选择流程的「求交集+随机」判定必须是纯函数、不放 UI；随机入口收敛到城邦管理器的单一方法（将来网络化时只需替换这个入口为同步 seed）；本局城邦状态存在城邦管理器，不挂在任何 UI 组件上。

## 明确不做（二期及以后）

- 不做城邦选择 UI（本期用临时自动占位）。
- 不做任何具体城邦机制（拍卖/卡牌/四季/卧底/将帅）。
- 不做城邦专属装备的实际 asset 内容（配置由用户自己做）。
- 不把染色开关改成城邦驱动（本期保留独立开关，二期元素城邦机制接入时再接）。
