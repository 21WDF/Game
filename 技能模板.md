# 技能模板 设计规范（大招）

> 体系：`IUltimateEffect` + `UltimateConfig`（接口 + 工厂 + ScriptableObject）
> 目录：`Assets/Game/Piece/Effects/`

---

## 一、体系定位

技能模板 = 大招（Ultimate），是棋子满能量后释放的一次性强力效果。当前接口：

```csharp
public interface IUltimateEffect
{
    void Execute(PieceModel caster, PieceModel target, GameContext ctx);
}
```

（注：`GameContext` 参数为未来 AOE/双阵营/时间回溯等复杂机制预留，当前可不用。）

---

## 二、现有模板（2 种）

| 模板 | 语义 | 状态 |
|------|------|:---:|
| **FireSlash**（烈焰斩） | 单体爆发伤害 | ✅ 已有 |
| **IceBarrier**（冰障） | 自身加防 buff | ✅ 已有 |

---

## 三、待新增模板（5 类）

| 模板 | 语义 | 关键参数 | 现状 |
|------|------|---------|:---:|
| **治疗 Heal** | 回复目标/范围友军 HP | healAmount / healPercent + range | ➕ 新增 |
| **召唤 Summon** | 空格生成傀儡 | puppetPieceId + count | ➕ 新增 |
| **控制 Control** | 眩晕（跳回合）/ 沉默（禁技能） | controlType + duration | ➕ 新增 |
| **AOE 伤害** | 以目标为中心范围伤害 | radius + 伤害公式 | ➕ 模板化 |
| **位移 Displacement** | 击退 / 拉近 / 传送 | displacementType + distance | ➕ 模板化 |

---

## 四、各模板语义细化

### 1. 治疗 Heal
- 目标：单体治疗（指向己方棋子）或范围治疗（以目标为中心 radius 内友军）
- 参数：`healAmount`（固定值）或 `healPercent`（最大 HP 百分比）+ `radius`（0=单体）
- 不造成伤害，不触发元素反应，不产金币

### 2. 召唤 Summon
- **实现决策：傀儡 = 普通棋子**，引用已有 `PieceData`，拥有者设为召唤方、**不占己方 7 棋子上限**
- 复用 `PieceManager.SpawnPieceById`，不需要新的实体系统
- 参数：`puppetPieceId`（召唤物棋子模板）+ `count`
- 落点：施法者相邻空格（或可配置范围）

### 3. 控制 Control
- **实现决策：眩晕 + 沉默，冻结已有，共 3 种控制状态**
- `眩晕 Stun`：`IsStunned`，跳过回合（不能移动/攻击）
- `沉默 Silence`：`IsSilenced`，不能放大招，但能普攻
- `冻结 Frozen`：`IsFrozen`（已有）
- 参数：`controlType`（Stun/Silence）+ `duration`（持续回合）

### 4. AOE 伤害
- 与 AreaAttackProvider 溅射区分：溅射是普攻的附属效果，AOE 大招是独立触发
- 参数：`radius` + 伤害公式（复用溅射的 baseDamage + attackPercent 模式）

### 5. 位移 Displacement
- **击退 Knockback**（已有 KnockbackResolver）、**传送 Teleport**（已有 TeleportPiece）需模板化
- 参数：`displacementType`（Knockback / Pull / Teleport）+ `distance`

---

## 五、新增状态字段（PieceModel）

| 字段 | 类型 | 说明 |
|------|------|------|
| `IsStunned` | bool | 眩晕（跳回合） |
| `IsSilenced` | bool | 沉默（禁大招） |
| `IsFrozen` | bool | 冻结（已有） |

回合开始时在 TurnManager 中统一递减/重置这些状态。

---

## 六、架构对接

```
新增 5 个 IUltimateEffect 实现类
PieceManager.CreateUltimate 工厂加 5 个 case
PieceModel 加 IsStunned / IsSilenced 字段
TurnManager 加控制状态回合递减
```

零推翻：现有 FireSlash / IceBarrier、工厂框架、能量系统全部不动。

---

## 七、9 棋子落地

| 棋子 | 大招 |
|------|------|
| 战士 Warrior | 烈焰斩（伤害，已有） |
| 法师 Mage | 冰障（增益，已有） |
| 弓手 Archer | 箭雨（AOE） |
| 骑士 Knight | 冲锋击退（位移） |
| 刺客 Assassin | 背刺（伤害 + 位移复合） |
| 牧师 Priest | 治疗（Heal） |
| 重装兵 Heavy | 眩晕锤击（控制） |
| 术士 Warlock | 召唤（Summon） |
| 游侠 Ranger | 传送（位移） |

---

## 八、待确认项

1. 刺客"背刺"暂定"伤害+位移"复合，是否改成更纯粹的单体高伤？
2. 召唤落点：施法者相邻格，还是可配置范围？
3. 控制状态的持续回合数（眩晕/沉默各持续几回合）[PLACEHOLDER]
