# 护盾层数 UI 文本 Trae 提示词

> 只写「改动方向 + 效果目标 + 红线」，不写实现细节。

---

## 一、现状（框架事实）

1. `UI_StatElement` 是棋子数值面板的引用容器（挂在数值预制体根上），只承载字段引用和运行时缓存，无业务逻辑。里面已有攻击/生命/防御/移动/攻击范围文本、能量条、元素图标、装备图标、状态图标。
2. `UI_PieceStatsDisplay` 是数值面板的管理器，**每帧轮询**（LateUpdate）读取 `PieceModel` 各字段，变化时才更新文本（用 lastXxx 缓存比对，避免每帧字符串分配）。
3. 已有一个「有值显示、无值隐藏」的文本范式可对标：`affixedElementGaugeText`（附着元素量数字，无附着时隐藏）。
4. `PieceModel` 已有护盾层数字段 `ShieldStacks`（0 = 无盾）。

## 二、改动方向

给棋子数值面板加一个**护盾层数文本**：

- `UI_StatElement` 加一个护盾文本字段（供 Inspector 拖入），并加对应的运行时缓存 + 在 `ResetCache` 里重置。
- `UI_PieceStatsDisplay` 的每帧更新里加护盾文本的刷新逻辑。

## 三、效果目标（验收标准）

1. 棋子有护盾时（`ShieldStacks > 0`），护盾文本显示当前层数。
2. 棋子无护盾时（`ShieldStacks == 0`），护盾文本隐藏（不显示数字）。
3. 层数变化时（获得/消耗/破碎/刷新），文本立即跟着变。
4. 只有变化时才更新文本，行为对标现有数值字段（避免每帧字符串分配）。

## 四、红线（绝不能破坏）

1. **不动** `UI_StatElement` / `UI_PieceStatsDisplay` 里其他任何字段、缓存、显示逻辑。
2. **不动** `PieceModel`、护盾拦截、伤害结算——这是纯 View 层显示。
3. **不动** `OnShieldChanged` 事件（本次不走事件驱动，走轮询；事件保留给未来其他 UI 用）。
4. 预制体字段拖拽由用户在 Unity 完成，代码侧只提供字段声明和显示逻辑。
