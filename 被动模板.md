# 被动模板 设计规范

> 体系：`IPassiveEffect` + `PassiveConfig`（接口 + 工厂 + ScriptableObject）
> 目录：`Assets/Game/Equipment/Effects/`

---

## 一、体系定位

被动模板是**持续生效的效果**，区别于大招（一次性主动释放）。当前接口：

```csharp
public interface IPassiveEffect
{
    void OnEquip(PieceModel owner);    // 挂载到 owner 时触发
    void OnUnequip(PieceModel owner);  // 从 owner 卸载时触发
}
```

**被动有两种载体**（这是理解被动模板的关键）：

| 载体 | 位置 | 说明 |
|------|------|------|
| 棋子自带被动 | `PieceData.builtInPassives` | 出厂自带，如"战士反击" |
| 装备携带被动 | `EquipmentData.passives` | 装备后生效，如"火焰护符附加火伤" |

两者用同一套 `IPassiveEffect` + 工厂，只是挂载来源不同。

---

## 二、现有模板（1 种，且是占位）

| 模板 | 语义 | 状态 |
|------|------|:---:|
| **ExtraDamage**（附加伤害） | 攻击加伤 | ⚠️ **占位，未接入攻击结算** |

**⚠ 关键问题**：`ExtraDamagePassive.Amount` 已保存，但 `OnEquip/OnUnequip` 为空，攻击结算（PieceManager.AttackPiece）未读取它。**目前任何装备/棋子的附加伤害被动都不真正生效**。这是本次模板建设要一并修复的。

---

## 三、待新增模板（4 类）

| 模板 | 语义 | 关键参数 | 状态 |
|------|------|---------|:---:|
| **反击 Counter** | 被攻击时反击 | counterPercent | ➕ 新增 |
| **吸血 Lifesteal** | 造成伤害回自身 HP | lifestealPercent | ➕ 新增 |
| **减伤 DamageReduction** | 受伤减免 | reductionPercent / flat | ➕ 新增 |
| **光环 Aura** | 周围友军 buff / 敌军 debuff（持续） | radius + 属性 + 数值 + 方向 | ➕ 新增 |

---

## 四、各模板语义细化

### 1. 反击 Counter
- 被攻击时，对攻击者造成一次反击伤害
- 参数：`counterPercent`（反击伤害 = 受到伤害 × 比例，或 = 自身攻击力 × 比例）
- 在 TakeDamage 时触发（注意防无限反击循环——反击不触发反击）

### 2. 吸血 Lifesteal
- 造成伤害时回复自身 HP
- 参数：`lifestealPercent`（回复 = 造成伤害 × 比例）
- 在 AttackPiece 结算后触发

### 3. 减伤 DamageReduction
- 受到的伤害减免
- 参数：`reductionPercent`（百分比）或 `flatReduction`（固定值）
- 在伤害计算时生效

### 4. 光环 Aura
- **实现决策：一个模板双向配置**——`targetFaction = Ally | Enemy`，`statModifier` 指定增减哪项属性
- 友军增益和敌军减益是同一个模板的两种参数，不写两个类
- 参数：`radius` + `targetFaction` + `statModifier`（属性名 + 数值 + 增减方向）
- 持续效果：回合开始时应用，离场/死亡时移除

---

## 五、关键：接口能力缺口

当前 `IPassiveEffect` 只有 `OnEquip/OnUnequip` 生命周期，**无法处理"事件触发型"被动**（反击要在"被攻击时"触发、吸血要在"造成伤害时"触发、光环要在"回合开始时"生效）。

两条路（需你拍板）：

| 方案 | 做法 | 代价 |
|------|------|------|
| **A. 扩展接口** | 给 `IPassiveEffect` 加事件钩子（OnDamageDealt / OnDamageReceived / OnTurnStart 等） | 接口签名变化，但一次性到位 |
| **B. 事件订阅** | 被动内部自己订阅 EventBus（如反击订阅 DamageEvent），OnEquip 订阅 / OnUnequip 退订 | 不碰接口，但依赖 EventBus（当前项目尚无全局 EventBus） |

---

## 六、架构对接

```
修复 ExtraDamagePassive 接入攻击结算
新增 4 个 IPassiveEffect 实现类
EquipmentManager.CreatePassive 工厂加 4 个 case
（如选方案 A）扩展 IPassiveEffect 接口
（如选方案 B）引入 EventBus 或被动自订阅事件
```

---

## 七、9 棋子落地

| 棋子 | 被动 | 载体 |
|------|------|------|
| 战士 Warrior | 反击 | 棋子自带 |
| 法师 Mage | 减伤 | 棋子自带 |
| 弓手 Archer | 附加伤害 | 棋子自带 |
| 骑士 Knight | 附加伤害 | 棋子自带 |
| 刺客 Assassin | 吸血 | 棋子自带 |
| 牧师 Priest | 光环·友军增益 | 棋子自带 |
| 重装兵 Heavy | 减伤 | 棋子自带 |
| 术士 Warlock | 光环·敌军减益 | 棋子自带 |
| 游侠 Ranger | — | 无 |

---

## 八、待确认项

1. **接口能力缺口选 A 还是 B**？（这是被动模板能否支持"事件触发型"被动的关键）
2. 反击伤害按"受到伤害比例"还是"自身攻击力比例"算？
3. 光环的持续/移除时机（回合开始应用 vs 实时跟随位置）？
4. 减伤出现 2 次（法师 20% / 重装 40%）、附加伤害 2 次（弓手/骑士）——接受还是调整？
