# 阶段② C2（唯一被动框架）· Trae 提示词

## 目标

搭建通用的「唯一被动」框架。约束规则：

- 唯一被动按「**唯一被动 id（uniqueId）**」判断，不是装备名、不是装备序号。
- 同一棋子身上，`uniqueId` 相同的唯一被动只生效**最后装备的那件**，先装备的失效（先进先出）。
- 不同棋子各持相同 `uniqueId` 的被动可**同时生效**（如光环重叠处叠加）。
- **不同装备可共享同一 `uniqueId`**（此时它们互相冲突）——这是关键，约束键是唯一被动 id，不是装备身份。

本阶段是**纯框架**，暂无唯一被动使用者（军团圣盾在 C3 才实现），因此去重逻辑对现有全部被动不触发、无可见行为变化；正确性将在 C3 用军团圣盾验证。

## 字段（加在 PassiveConfig 级别，非装备级别）

`EquipmentData.PassiveConfig` 加 3 个字段：

| 字段 | 类型 | 默认 | 说明 |
|---|---|---|---|
| passiveName | string | "" | 被动名字（纯展示，后续 UI 装备介绍开头显示） |
| isUnique | bool | false | 是否唯一被动 |
| uniqueId | string | "" | 唯一被动 id（isUnique=true 时必填；不同装备填相同 uniqueId 即互相冲突） |

> `isUnique=false` 时只填 `passiveName`（展示名），`uniqueId` 留空；`isUnique=true` 时 `passiveName` + `uniqueId` 都要填。

## 改动清单

1. **`EquipmentData.PassiveConfig`**：加 `passiveName` / `isUnique` / `uniqueId` 三字段（带 Tooltip 说明语义）。

2. **`IPassiveEffect`**：加默认接口属性 `string UniqueId => null;`（与现有默认接口方法同一机制；现有被动类零改动，返回 null = 非唯一）。

3. **`PassiveFactory.Create`**：签名加第三个参数 `string uniqueId = null`。本阶段**现有 case 均不传此参数**（保持默认 null）；唯一被动类（LegionAegisPassive）在 C3 才实现并消费它。

4. **`EquipmentManager.InstantiatePassives`**：调用 Create 时传入 `cfg.uniqueId`（即 `Create(cfg.className, cfg.jsonParams, cfg.uniqueId)`）。

5. **`EquipmentModel`**：加字段 `public int EquipOrder;`（装备时间戳，0=未装备；**仅用于判断「先进先出」的先后**，不是唯一性标识）。

6. **`EquipmentManager`**：
   - 加私有字段 `private int _equipOrderCounter;`
   - `EquipToPiece` 装新装备时：`equipment.EquipOrder = ++_equipOrderCounter;`
   - `UnequipInternal` 卸下时：`equipped.EquipOrder = 0;`

7. **`PieceModel.GetAllPassives()`**：对**被动级**做唯一去重：
   - 第一遍遍历 `EquippedItems` 的 `ActivePassives`，对 `UniqueId` 非空的被动，记录「每个 uniqueId → 最大 EquipOrder」（该 uniqueId 最后装备的装备）。
   - 第二遍遍历，普通被动（`UniqueId` 为 null）全部加入；唯一被动仅当「所在装备的 `EquipOrder` == 该 uniqueId 的最大 EquipOrder」时加入，否则跳过（被压制的唯一被动）。
   - `BuiltInPassives` 不变照旧加入。

## 约束（务必遵守）

1. **唯一性分组键 = `uniqueId`**（唯一被动 id），不是装备名、不是 EquipOrder。`EquipOrder` 只用于「先进先出」的先后取舍。
2. 去重逻辑只影响 `GetAllPassives()` 的遍历结果，**不改 `EquippedItems` 数组本身**（被压制的装备仍在槽位里，可正常卸下）。
3. `passiveName` 本次只落字段，UI 展示后续再做。
4. 本阶段不要创建任何新的被动类、不要改伤害/死亡介入逻辑（那些是 C1/C3 的事）。
5. 诊断无报错后收尾。
