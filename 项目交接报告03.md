# Chaotic Chess（混沌棋局）项目交接报告 03

> 本报告供接手的下一个 AI（或用户）快速上手。请先通读全文，再开始动手。
> 前序报告：`项目交接报告01.md`（Phase1/2、装备、元素、伤害结算链）、`项目交接报告02.md`（9棋子内容、范围Provider、大招框架、用户提示词要求）。
> 本报告 = **第 3 任负责人（2026-08-20 ~ 08-29）的增量 + 当前残留状态**。前两任已覆盖的内容不重复。

---

## ⚠️ 〇、P0 头号风险（接手第一件事）

**git 已 init 但 8 天成果未提交。** 仓库只有 2 个早期 commit：
```
fa4f9f9 删除旧8棋子空壳
39ef785 首次提交（锁定 22 天成果）
```
之后第 3 任推进的**全部工作（约 143 个文件）仍裸露在工作区**——`Assets/Game/CityState/`（整个城邦系统目录）、护盾、贸易之城资产、背包系统、MonsoonConfig.asset、季风三期、大量 Trae 提示词，全是 `M`/`D`/`??` 未提交状态。

**任何 `git checkout .` / `git clean -fd` / 误操作都会丢失 8 天成果。接手后第一件事 = `git add -A && git commit` 把当前状态锁定。**

---

## 一、项目概览（更新）

| 项 | 内容 |
|----|------|
| 游戏 | 本地双人热座 Hex 战棋（回合制，元素反应为核心玩法） |
| 引擎 | Unity 6 LTS（6000.3.19f1）+ URP + MVC + ScriptableObject |
| 阶段 | **Pillar 3 城邦系统中后期**：城邦框架闭环，6 城邦里 3 个机制已落地（贸易/季风/元素），3 个待实现（欢愉/邪疑/战争） |
| 版本控制 | git 已 init，但**当前成果未提交（P0）** |

**核心循环**：选棋子 → 选城邦（双方各选 4 → 取交集 → 随机定 1）→ 部署 → 移动/攻击/装备/道具/大招 → 城邦专属机制 + 元素反应 + 装备被动 → 金币经济 → 回合结算 → 全灭判负。

---

## 二、第 3 任工作成果（相比报告 02 的增量）

| 系统 | 状态 | 要点 |
|------|------|------|
| git 版本控制 | ✅ 已 init | 解决 02 的 P0（但后续成果未提交，见 P0） |
| 删除旧 8 棋子 | ✅ | Archer~Warlock 空壳删除，选人池精简为 9 新棋子 + 兔兔伯爵 |
| 护盾系统 | ✅ 闭环 | 层数护盾（上限3）+ 元素标签 + 拦截规则 + 吸血/金币拦截 + UI + 元素染色 |
| 城邦系统框架 | ✅ 闭环 | `CityStateKind`（6城邦）+ `CityStateManager` + `ICityStateMechanic` + 选择流程 + 商店过滤 |
| 元素系统城邦过滤 | ✅ | 非元素城邦关闭元素附着/反应/跳字染色 |
| 贸易之城 | ✅ 闭环 | 透支 + 拍卖（6回合成交/5回合刷新）+ 利息（10金/回合）+ 信誉（涨价/禁拍）+ 地下交易（累计伤害触发） |
| 季风之城 | ✅ 闭环 | 一期四季昼夜 + 二期气候简单组（暴雨/暴雪/高温）+ 三期气候复杂组（飓风幸运方块/雷暴雷电残留+永久加成） |
| 背包系统 | ✅ 闭环 | 道具库存 + 固定槽位侧栏 + 右键tooltip + 双列 + 拖拽装备/道具到棋子 |

---

## 三、城邦系统现状（Pillar 3 核心，接手必读）

### 3.1 框架（已完成）
- **6 城邦枚举** `CityStateKind`：Trade（贸易）/ Joy（欢愉）/ Monsoon（季风）/ Occult（邪疑）/ War（战争）/ Element（元素）。
- **选择规则**：双方各选 k 个（k = floor(城邦数/2)+1 = 4）→ 取交集 → 随机定 1（鸽笼原理保证交集非空）。
- **机制按城邦过滤**：只有当前城邦的专属机制生效，其余城邦机制不生效。
- **每城邦 = 专属装备池（tier=CityState）+ 专属机制**（统一走 `ICityStateMechanic` 钩子）。
- **在线 PVP 预留**：选择判定是纯函数、随机入口收敛到 Manager 单一方法（将来换网络 seed）；城邦状态是「对局级」数据放 Manager 非 UI。

### 3.2 各城邦机制实现状态

| 城邦 | 状态 | 机制 |
|------|------|------|
| 贸易 Trade | ✅ 闭环 | 透支 / 拍卖 / 利息 / 信誉 / 地下交易（三大机制） |
| 季风 Monsoon | ✅ 闭环 | 四季昼夜 + 气候三组（暴雨/暴雪/高温/飓风/雷暴） |
| 元素 Element | ✅ 闭环 | 元素附着/反应/跳字 + 护盾染色 |
| 欢愉 Joy | ⬜ 未实现 | 卡牌/手牌系统（**复杂度二档**，全新手牌系统） |
| 邪疑 Occult | ⬜ 未实现 | 卧底/棋子归属（**复杂度三档**，改归属） |
| 战争 War | ⬜ 未实现 | 将帅/主将死亡即败（**复杂度三档**，改胜负） |

**复杂度分档**（用户拍板）：一档（贸易+季风，复用现有系统）→ 二档（欢愉，全新手牌）→ 三档（邪疑+战争，改归属/胜负，规则歧义最多，须单独评审）。

---

## 四、背包系统（本次会话最后闭环的一块，接手必读）

- **数据层**：`GridItemManager`（道具库存按玩家分组 List）+ `GrantItem`（无偿入口）/ `BuyToInventory`（购买）/ `UseFromInventory`（使用）。
- **UI**：`UI_BackpackSidebar`（常驻侧栏，CanvasGroup 显隐）+ `UI_BackpackIconSlot`（固定槽位格）+ `UI_ItemTooltip`（右键 tooltip）。
- **交互**：单击选中高亮 / 双击使用道具 / 右键 tooltip / 拖拽装备到己方棋子（EquipToPiece）、拖拽 requiresSelectedPiece 道具到己方棋子（选中+使用）。
- **关键约定**：侧栏**绝不调用** `InputHandler.RegisterPanelOpen/Close`（常驻不锁棋盘）；显隐走 CanvasGroup 不用 SetActive；容量在 GameConfig（装备/道具各 5）。

---

## 五、关键新增设计决策（长期有效）

1. **护盾**：层数护盾（上限3），元素盾对同元素伤害免疫（真实伤害无视免疫仍扣层）；染色仅元素城邦生效（GameConfig.shieldElementDyeingEnabled 为过渡开关，未来城邦接管）。
2. **气候时间单位**：季节/昼夜/气候判定/持续递减/残留衰减**严格按「轮」计**（轮号守卫，双方各行动一次 = 1 轮），不按「回合」。
3. **飓风幸运方块平衡保底**：一轮内两回合要么都不掉、要么各掉「同品质同数量」一批（默认每批2），保证先手/后手对称公平；方块双方共享、经过即拾取。
4. **雷暴永久加成**：劈中且存活 → 概率获得对局内永久随机属性加成，接入 `EffectiveAttack/EffectiveDefense/MaxHP/MoveRange/AttackRange` 五聚合点，**纯加法求和项**（现有计算零改动）。
5. **气候配置结构**：`ClimateEntry` 只存通用字段（名称/概率/季节限制/效果类型/独占），各气候专属数值参数拆独立区块（避免「简单气候背无用参数」）。
6. **容量只约束商店购买**：拍卖/地下交易等其他获得装备入口不受背包容量拦截。

---

## 六、残留问题 / 待办（更新 02，按优先级）

### P0
1. **git 未提交**（见〇节）——8 天成果裸露，立即 `git add -A && git commit`。

### 高优先级
2. **欢愉 / 邪疑 / 战争 3 城邦机制未实现**（见 3.2）——Pillar 3 还剩一半。
3. **棋子视觉占位**——9 棋子 prefab 仍可能是 Cube 占位（待用户确认是否已拖入预制体）。

### 中优先级
4. **无音效**——全项目无 AudioManager。
5. **数值全为占位**——棋子/被动/装备数值从未 playtest 调平。
6. **GetAllPassives 每次分配新 List**——高频查询 GC 压力。

### 低优先级 / 已知边界（本次会话发现，未修）
7. **季风三期 3 个边界**：①「到达雷电残留格并停留」同轮扣 2 次（到达+停留），强度8时同轮扣16，可能偏重；②幸运方块与雷电残留同格共存时高亮冲突（都用 tile.overlay 战术层，后 Show 覆盖先 Show）；③雷劈「尝试N次跳过重复格」实际劈的格子数可能略少于 N。
8. 背包侧栏头注释残留「slotCount 配置槽位数量」（字段已删）。

### 02 遗留（待确认是否仍存在）
9. 溅射跳字重复、物理默认色白非红。

### Playtest 失败信号（调平参考）
- 单局 >40 分钟 → 节奏太慢；金币囤积 >120 → 通胀；某棋子/装备组合胜率 >70% → 平衡崩了。

---

## 七、技术陷阱（新增，接手必读）

1. **Trae 声称「无编译错误」不可信**——曾出现「字段上移时留了旧定义块 → 重复字段定义 CS0102」，Trae 仍报「无编译错误」。凡 Trae 报编译通过但涉及字段增删/上移/重构，务必逐行核对是否留了旧定义。
2. **SO 资产加字段不自动回填**——MonsoonConfig.asset / GameConfig.asset 等，代码加了新字段后，旧序列化资产会按「默认值」反序列化，但**旧字段值不会自动更新**。涉及数值修正时要提醒用户在 Inspector 手动确认（如三期B 幸运方块档位的 maxCityPrice/includeNormalEquipment）。
3. **GameConfig 无单例**——消费方统一用 `Resources.Load<GameConfig>("GameConfig")`，不要用 `GameConfig.Instance`。
4. **格子实体共用 tile.overlay 战术层**——幸运方块、雷电残留、移动/攻击高亮都用同一战术层，同格共存时后者覆盖前者（视觉冲突，不影响逻辑）。
5. 沿用报告 01「技术陷阱」10 条 + 报告 02「用户提示词要求」（见下）。

---

## 八、下一步方向（建议）

1. **先 `git add -A && git commit`**（P0，锁定 8 天成果）。
2. 继续 Pillar 3：欢愉（二档）→ 邪疑（三档）→ 战争（三档），按复杂度档逐一推进，三档城邦须单独评审规则歧义。
3. playtest 数值平衡（建议先打 20-30 局单机）。
4. 补音效、棋子视觉（拖 prefab）。

---

## 九、辅助文档索引

| 文件 | 用途 |
|------|------|
| `.workbuddy/memory/MEMORY.md` | 项目长期记忆（设计决策、用户约定） |
| `.workbuddy/memory/YYYY-MM-DD.md` | 每日工作日志（08-20 ~ 08-28） |
| 项目根目录 `*_Trae提示词.md` | 各阶段 Trae 提示词（城邦/贸易/季风/背包等约 30+ 个） |
| `Assets/Game/CityState/` | 城邦系统（Trade/ Monsoon/ Auction/ Underground/ 等子目录） |
| `Assets/Game/Client/UI_BackpackSidebar.cs` 等 | 背包系统 UI |
| `季风之城三期C_雷暴雷电残留_Trae提示词.md` | 季风三期最后一块（含永久加成接入细节） |

---

## 十、用户提示词要求（沿用报告 02，务必遵守）

> 完整版见 `项目交接报告02.md` 末尾，此处列核心三条：

1. **工作分工（铁律）**：用户出方向/拍板/做 asset 配置；Trae 只写代码；本助手出方向提示词 + 最后核对。
2. **Trae 提示词只写三件事**：改动方向 + 效果目标 + 红线。**绝不写实现细节**（不加方法名建议、不写算法步骤、不给伪代码）。可写「现状/框架事实」和「红线/坑」。
3. **核对（按复杂度取舍，不是每次改完都核对）**：**简单/低风险改动**（纯 View、机械增删、单一效果类、配置默认值）**只看 Trae 汇报 + 抽查关键点即可，不必逐行读码**；**复杂改动**（涉及 BattleController 状态机、伤害管道、回合流转、被动框架接口、多文件逻辑联动、字段增删/重构）才逐行读源码 + 追踪调用链 + 交叉验证。核对结论给「文件 → 判定 → 验证点」清单，并报告「非 bug 观察点」和「需用户知晓的口径」。

**其他约定**：中文命名；分析报告放 `.workbuddy/artifacts/` 或项目根目录；Trae 提示词放项目根目录；Excel 配置表保留（数值平衡用）；跳字颜色涨红跌绿、货币 ¥。
