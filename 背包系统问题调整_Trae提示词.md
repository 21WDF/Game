# 背包系统问题调整_Trae提示词.md

> 铁律：只写「改动方向 + 效果目标 + 红线」，不写实现细节。

## 〇、背景（一句话）

背包侧栏闭环后集中修 5 个交互/规则问题：tooltip 定位与关闭、满槽禁买、空槽高亮、拖拽目标格高亮。不新增系统，是既有功能的校正与补全。

## 一、现状（框架事实）

1. **tooltip 定位**：`UI_ItemTooltip` 现在把面板定位到「物品格子的右边缘」，由物品格把自身 RectTransform 传入 `Show(title, desc, anchor)`。
2. **tooltip 关闭**：现在是「移出物品 → 延迟关 / 移进 tooltip → 保持 / 移出 tooltip → 关」三态，挂在物品格与 tooltip 自身的指针进出事件上。
3. **容量**：背包固定槽位 `slotCount=10`、双列（左装备右道具）→ 装备列 5 / 道具列 5。但「容量」只在侧栏 UI 层由 slotCount 推导，购买入口 `EquipmentManager.BuyEquipment`、`GridItemManager.BuyToInventory` 只查金币、**不查容量**。
4. **空槽高亮**：侧栏的单击选中回调对「空槽」也切换高亮，没有区分有无物品。
5. **拖拽**：装备 / 需指定棋子道具可拖到己方棋子（松手用 `RaycastTile()` + `GetPieceAt(coord)` 判定）。当前拖拽**没有任何目标格反馈**。
6. **棋盘高亮体系**：格子叠加层 `TileOverlay` 分「战术层（Show/Hide）+ 悬停层（ShowHover/HideHover）」，两层独立；悬停色已由 `InputHandler` 用 `GameConfig.overlayHoverColor` 统一处理（鼠标经过任意格子自动显示）。拖拽目标格只需补「战术层常亮」，悬停层无需新做。
7. **配置**：`GameConfig` 已收拢大量调参字段，加载方式 `Resources.Load<GameConfig>("GameConfig")`。
8. 常驻红线（不可破坏）：侧栏不调 `RegisterPanelOpen/Close`、显隐走 CanvasGroup、随活动玩家切换、战前隐藏/开战显示/结束隐藏。

## 二、改动方向

### 1. tooltip 显示位置（问题1）

- 显示位置从「贴物品格子右侧」改为「贴 **Scroll View 滚动视图的右侧边缘**」，并且**垂直方向与对应物品格子对齐（平齐）**。
- 需要让 tooltip 拿到「滚动视图」的定位基准，同时保留「当前物品格」用于垂直对齐。

### 2. tooltip 关闭条件（问题2）

- 关闭条件从「鼠标移出物品」改为：**鼠标离开滚动视图区域，且不处于 tooltip 面板上** 时才关闭。
- 即「离开滚动视图」与「不在 tooltip 上」两个条件都成立才消失；鼠标仍在滚动视图内、或仍在 tooltip 上，都不关。

### 3. 满槽禁止购买（问题3）

- 把「装备容量」「道具容量」两个数值收进 `GameConfig`（默认各 5）。
- 侧栏的槽位数量与列容量改由这两个字段决定（不再依赖孤立的 slotCount 常量，保证 UI 与逻辑同源）。
- 购买入口：购买**装备**前校验装备容量、购买**道具**前校验道具容量；对应列已满时拒绝购买（不扣金币、不入背包，给出可读提示）。
- 拍卖 / 地下交易等其他获得装备的入口不受此容量拦截（本次只约束商店购买）。

### 4. 空槽不高亮（问题4）

- 空槽（无物品）单击时**不响应**：不切换高亮、不改变选中态。
- 仅「有物品」的槽位才响应单击选中。

### 5. 拖拽目标格高亮（问题5）

- 拖拽开始：把「可施加该负载的棋子」所在格子做**常亮提示**（战术层），颜色收进 `GameConfig`（默认浅金色）。
- 拖拽结束 / 取消：清除这些常亮。
- 鼠标经过这些格子时，走**现有的默认悬停高亮**即可（悬停层已就绪，无需新做）。
- **可施加目标判定要收敛为统一口径**（高亮收集与松手判定共用同一判定），当前口径 =「己方棋子」；要为将来「可对敌方施加的物品」预留扩展——判定不能把「己方」硬编码散落在多处，应能按负载类型/数据字段扩展出「可对敌方」。

## 三、效果目标（验收标准）

1. 右键某装备 → tooltip 出现在「滚动视图右边缘外侧」，且与该装备格垂直平齐。
2. 鼠标在滚动视图内移动、或移进 tooltip 面板 → tooltip 保持；离开滚动视图 **且** 不在 tooltip 上 → tooltip 消失。
3. 装备列满 5 件后，商店再买装备 → 拒绝（金币不动、背包不变）；道具列满 5 件后，再买道具 → 拒绝。
4. 单击空槽 → 无任何高亮/选中变化；单击有物品槽 → 正常选中高亮。
5. 拖装备/需指定道具时，所有「可施加棋子」所在格常亮（浅金）；松手到己方棋子执行、到敌方/空格取消；拖拽结束常亮清除。
6. 拖拽目标格被鼠标经过时，显示默认悬停色（与现有棋盘悬停一致）。
7. 单击选中、双击使用道具、右键 tooltip、悬停保持、拖拽装备/道具——语义与改动前一致。

## 四、红线（绝不能破坏）

1. **`EquipToPiece` / `UseFromInventory` / `BuyEquipment` / `BuyToInventory` 的核心逻辑（扣款、入包、装备、瞄准）不得改动**——只在购买入口补「容量校验」，拖拽只调用既有接口。
2. 侧栏仍**不得调用 `RegisterPanelOpen/Close`**；显隐仍走 CanvasGroup。
3. 拖拽只能作用到「可施加」棋子（当前=己方），不得拖到敌方棋子 / 空格产生副作用。
4. 拖拽失败 / 取消不得产生副作用（不凭空装备、不凭空消耗道具）。
5. tooltip 仍是纯显示浮层，不参与面板计数、不锁棋盘。
6. 商店、拍卖、地下交易、伤害管道、元素、护盾、城邦过滤、回合流转——零改动。

## 五、明确不做

- 拖拽排序 / 整理槽位。
- 装备 / 道具从棋子身上拖回侧栏（卸下）。
- 「可对敌方施加」的本体实现（本次只留扩展口径，不实现敌方目标）。
- 溢出处理、存档、跨局持久化。
