# 季风之城三期C（雷暴·雷电残留 + 雷劈永久加成）— Trae 提示词

> 铁律：只写「改动方向 + 效果目标 + 红线」，不写实现细节。

## 〇、背景（一句话）

三期气候复杂组最后一块：**雷暴**——出现时随机格子雷劈（劈中棋子扣真实伤害 + 概率永久加成），劈过的格子留「雷电残留」（强度每轮衰减的棋盘陷阱，位于/经过受当前强度真实伤害）。这是第二个「格子实体」（复用三期B 幸运方块的实体模式）+ 一处核心模型改动（永久属性加成接入 PieceModel 聚合点）。

## 一、现状（框架事实）

1. **气候框架已就绪**：`MonsoonManager.TickAndRollClimates`（每轮判定 + 持续递减）、`RollNewClimates`、`IsClimateActive(kind)`（内聚过滤）。`ClimateEffectKind` 现有 `None/Rain/Blizzard/Heat/Hurricane`，`ClimateEntry` 已通用化（三期A）。
2. **格子实体先例（三期B）**：幸运方块已实现「坐标 → 实体 → 渲染（`tile.overlay.Show`）→ 生命周期」的完整模式（`_luckyBoxes` 列表 + `LuckyBox` 类 + `ShowBoxOverlay` + `GetBoxAt` + `ClearAllBoxes`）。**雷电残留复用同套模式**，区别仅在「残留带强度值 + 每轮衰减」。
3. **真实伤害**：`PieceManager.ApplyIncomingDamage(target, damage, source, DamageSource.Dot, DamageKind.True, ElementType.None)`（季风冬扣血同款；source=null 环境伤害、不附着元素、不统计地下交易）。
4. **移动路径遍历点**：`PieceManager.MovePieceAlongPath` 已在路径遍历区调用 `MonsoonManager.OnPieceMovedAlongPath(model, path)`（三期B 方块拾取；雷电残留「经过」可复用或并行接入同一 path 遍历点）。
5. **属性聚合点（永久加成接入处）**：`PieceModel.EffectiveAttack` / `EffectiveDefense` / `MaxHP` / `MoveRange` / `AttackRange` 五处查询式聚合（现有：基础值 + 装备加成 + 被动 + 临时 buff + 光环 + 季风加成）。
6. **回合/轮语义**：`MonsoonManager.OnTurnStarted`（每回合开始）/ `OnTurnEnded`（每回合结束）/ `OnRoundEnded`（一轮完成，P2 结束）均已接线。
7. 常驻红线：气候「是否生效」内聚在 `MonsoonManager`，不散落到移动/攻击/回合调用点。

## 二、改动方向

### 1. 枚举 + 气候条目 + 专属配置区块

- `ClimateEffectKind` 追加 `Lightning`（雷暴）。
- `climates` 列表追加雷暴条目（复用 `ClimateEntry` 通用字段；概率默认 0.05、无季节限制、非独占）。
- MonsoonConfig 新增「雷暴参数」区块（全部可配字段，代码零硬编码）：
  - 雷劈格子数下限 / 上限（默认 2 / 4）；
  - 劈中棋子真实伤害（默认 20）；
  - 残留初始强度（默认 8）；
  - 残留每轮衰减（默认 2，强度每轮减此值，归零消失）；
  - 永久加成概率（默认 0.4）；
  - 永久加成属性池（列表：属性类型 + 数值，默认 HP +2 / 攻击 +2 / 防御 +2 / 移动 +1 / 射程 +1；每次雷劈从池里随机抽一条）。

### 2. 雷电残留实体（复用三期B 格子实体模式）

- 数据层：MonsoonManager 内聚记录「坐标 → 残留（当前强度）」，非季风城邦 / 雷暴未激活时无任何残留。
- 渲染层：残留所在格用残留专用颜色做战术层高亮（颜色进 GameConfig，可调；与方块/移动/攻击高亮可区分）。
- 生命周期：残留强度**每轮 -衰减量**（轮号守卫，归零即消失、高亮还原）。残留是独立实体，按自身强度衰减，不受雷暴本身持续轮数影响。
- 复用三期B 的「格子实体」数据记录 + 渲染 + 生命周期机制（不另起炉灶）。

### 3. 雷劈（出现时一次性）

- 雷暴**出现时**：随机若干格子（数量可配）雷劈。
- 每个被劈格子：
  - 留下雷电残留（初始强度）；
  - 若该格站着棋子 → 棋子扣劈中真实伤害（`ApplyIncomingDamage`，环境伤害、不附着元素）；且该棋子**若仍存活**，有概率（可配）获得一个**对局内永久**属性加成——从属性池随机一条属性 +N（可配；多次雷劈可叠加累积）。
- 随机入口收敛到 MonsoonManager 单一方法（与其他气候判定同源；联机换 seed 只改这里）。

### 4. 残留伤害（经过 / 停留，均按当前强度）

- **经过/到达**：棋子移动路径**经过或终点落在**残留格 → 立即受「当前强度」的真实伤害（复用三期B 的 path 遍历点，判断内聚 MonsoonManager）。
- **停留**：棋子停在残留格 → **每轮结束时**（一轮一次）再受「当前强度」真实伤害（配合强度每轮递减，形成「停留 8→6→4→2 递减」）。

### 5. 永久属性加成（核心模型改动，谨慎）

- 给 `PieceModel` 增加「对局内永久属性加成」的承载（对局内永久、随棋子存续生效、无回合递减；多次雷劈可叠加累积）。
- 接入 `EffectiveAttack` / `EffectiveDefense` / `MaxHP` / `MoveRange` / `AttackRange` 五处聚合点——**只新增一个求和项，不改变现有计算逻辑**。

### 6. 内聚过滤（关键）

- 残留 / 雷劈 / 残留伤害 / 永久加成的「是否生效」判断全部内聚在 `MonsoonManager`，调用点（移动流程 / 属性聚合）只调一个入口方法。

## 三、效果目标（验收标准）

1. 选到季风城邦 → 雷暴按概率出现、持续 1~3 轮随机、可叠加、有日志。
2. 雷暴出现时：随机格子雷劈；被劈格留雷电残留（初始强度 8）；劈中棋子扣 20 真实伤害（无视防御/护盾、不附着元素）；劈中且存活的棋子有概率获得随机一个属性的对局内永久加成（棋子面板可见）。
3. **雷电残留**：强度每轮 -2（8→6→4→2→0），归零消失；棋子移动经过/到达该格立即受当前强度真实伤害；停在残留格的棋子每轮结束再受当前强度真实伤害。
4. **永久加成**：接入 `EffectiveAttack/EffectiveDefense/MaxHP/MoveRange/AttackRange`，对局内永久、多次叠加，卸装备/换棋子不影响；现有属性计算逻辑不变。
5. 雷劈格子数、劈中伤害、残留强度/衰减、永久加成概率/属性池/数值——全部从配置读取，代码零硬编码，Inspector 可改。
6. 其他城邦 → 无雷劈、无残留、无永久加成，雷暴完全无效果。
7. 二期暴雨/暴雪/高温、三期B 飓风方块、四季昼夜、伤害管道、元素、护盾、贸易、城邦选择/过滤、背包系统——行为不变。

## 四、红线（绝不能破坏）

1. **内聚过滤**：雷暴「是否生效」判断内聚在 MonsoonManager，不散落到移动/攻击/回合调用点。
2. **复用现有落点，不另起炉灶**：残留走真实伤害 + `ApplyIncomingDamage`；路径经过走既有 path 遍历点；实体复用三期B 格子实体模式；永久加成接入既有属性聚合点。
3. **时间单位铁律**：残留衰减、停留伤害结算按「轮」计（轮号守卫），不按「回合」。
4. **数据驱动**：概率、数量、数值、属性池、衰减量全从 MonsoonConfig/GameConfig 读取，代码零硬编码。
5. **永久加成不破坏属性计算**：只新增求和项接入 `Effective*`，现有基础值/装备/被动/buff/光环/季风加成的计算逻辑零改动（这是核心模型，务必回归验证攻击/防御/血量计算无异常）。
6. **不破坏既有功能**：四季昼夜、暴雨/暴雪/高温、飓风方块、伤害管道、元素反应、护盾、贸易、城邦选择/过滤、背包系统零改动。
7. 雷劈/残留伤害失败或棋子死亡边界不得产生副作用（不重复扣血、不残留脏数据、不凭空加属性）。

## 五、明确不做

- 雷电残留的连锁传导/扩散。
- 永久属性加成的跨局持久化（仅对局内永久）。
- AP 上限 / 回能类属性的永久加成（本期属性池默认 HP/攻击/防御/移动/射程）。
- 雷暴每轮重复雷劈（本次只「出现时劈一次」；若要每轮劈后续再加）。
