# 阶段②被动框架 2.2B（第二批）· Trae 提示词（含「被攻击」钩子）

## 任务
实现「被攻击」钩子 OnAttacked + TempBuffs 基础设施 + 5 个被动：石像鬼板甲、枯萎宝珠、英灵幡、探索者护臂、诸葛连弩。

## 一、OnAttacked 钩子（新增，与 OnDamageReceived 解耦）
- `IPassiveEffect` 加钩子（默认空实现，现有被动零改动）：
  `void OnAttacked(PieceModel owner, PieceModel attacker, DamageSource source = DamageSource.Attack) { }`
  语义：owner **被攻击时**触发（无论是否造成减血）；attacker 为攻击方。与 OnDamageReceived（仅减血触发）区分。

- 触发点：
  1. `AttackPiece`（普攻/元素溅射）：在 `TakeDamage` 后、`OnDamageReceived` 前，触发 `target` 全部被动的 `OnAttacked(target, attacker, damageSource)`
  2. `ApplyIncomingDamage`（大招/DoT/非元素溅射/反伤）：在方法开头（`target` 已死判断之后）触发 `target` 全部被动的 `OnAttacked(target, source, damageType)`，**即使 damage≤0 也触发**；之后 `damage<=0` 仍 return（不触发 OnDamageReceived）

- 说明：反甲 ThornsPassive 只覆写 OnDamageReceived（不覆写 OnAttacked），故反伤伤害触发的 OnAttacked 不会反出反甲（防递归天然成立）。

## 二、TempBuffs 基础设施（枯萎宝珠依赖）
- PieceModel 加 `GetTempBuffTotal(string statType)`：遍历 `TempBuffs` 累加 `statType` 匹配项的 `amount`（取值 `"Attack"`/`"Defense"`）
- `EffectiveAttack` 末尾追加 `+ GetTempBuffTotal("Attack")`；`EffectiveDefense` 末尾追加 `+ GetTempBuffTotal("Defense")`
- TurnManager 加 `TickTempBuffs`（`OnTurnStarted` 里调用，遍历双方棋子）：每个 TempBuff `turnsRemaining > 0` 的 `-1`，`<=0` 移除；`<0` 视为永久不递减

## 三、OnDamageDealt 加 target 参数（枯萎宝珠依赖）
- 签名改 `OnDamageDealt(PieceModel owner, int damage, PieceModel target = null)`
- AttackPiece 触发处传 target；LifestealPassive 覆写忽略 target（仅签名匹配）

## 四、5 个被动类（放 Assets/Game/Equipment/Effects/）

### 1. StoneSkinPassive（石像鬼板甲）
- 参数：`defenseBonus`（每次叠加防御，默认 5）、`timerTurns`（计时器回合，默认 4）、`triggerOnNoDamage`（bool，默认 true = 无论是否减血都触发）
- 实例字段：`_stacks`（叠层数）、`_timer`（剩余回合）、`_lastAttacker`（上次触发攻击来源）
- 叠加逻辑 `TryStack(owner, attacker)`：`attacker != null && attacker != _lastAttacker` 时 `_stacks++`、`_timer = timerTurns`、`_lastAttacker = attacker`（同一敌人连打不触发）
- `OnAttacked`：`if (!_triggerOnNoDamage) return;` 否则 `TryStack(owner, attacker)`
- `OnDamageReceived`：`if (_triggerOnNoDamage) return;` 否则 `TryStack(owner, owner.LastDamageSource)`
- `OnTurnStart`：`_lastAttacker != null` 时 `_timer--`，`<=0` 则 `_stacks=0`、`_lastAttacker=null`（计时口径同生命铠甲，每次一方回合触发）
- `OnEquip`/`OnUnequip`：全部清零
- 暴露只读 `DefenseBonus => _stacks * defenseBonus`
- PieceModel 加 `GetTotalStoneSkinDefense()` 聚合所有 StoneSkinPassive 的 `DefenseBonus`，`EffectiveDefense` 引用（对标 GetTotalKillAttackBonus 写法，逐项遍历不分配列表）

### 2. WitherPassive（枯萎宝珠）
- 参数：`defenseReduction`（减防值，默认 3）、`duration`（持续回合，默认 2）
- `OnDamageDealt(owner, damage, target)`：`target` 非空时往 `target.TempBuffs` 加 `{ statType="Defense", amount=-defenseReduction, turnsRemaining=duration }`（可叠加多条）

### 3. SpiritBannerPassive（英灵幡）
- 参数：`energyGain`（回蓝量，默认 10）
- `OnAllyDeath(owner, ally)`：`owner.Energy?.Gain(energyGain)`（Energy 可能为 null，判空）

### 4. FreeMoveAPPassive（探索者护臂）
- 参数：`cooldownTurns`（冷却回合，默认 4）
- 实例字段 `_cooldown`；`OnTurnStart` 递减（最小 0）；`OnEquip`/`OnUnequip` 清零
- 暴露 `bool CanUseFreeAP => _cooldown <= 0` 与 `void MarkUsed() => _cooldown = cooldownTurns`

### 5. FreeAttackAPPassive（诸葛连弩）
- 与 FreeMoveAPPassive 结构一致，仅用于攻击

## 五、BattleController AP 减免
- `HandleMove`：移动前遍历 `piece` 被动找 `FreeMoveAPPassive` 且 `CanUseFreeAP`，有则**跳过 HasAP 检查和 ConsumeAP** 并 `MarkUsed()`；否则走原 AP 消耗
- `HandleAttack`：同理找 `FreeAttackAPPassive`（注意攻击"每回合一次"限制不受影响）

## 六、PassiveFactory
switch 补 5 个 case + 5 个参数类（全部 `[System.Serializable]`；FreeMoveAPPassive/FreeAttackAPPassive 的 jsonParams 可为 `{}`）。

## 约束（必须遵守）
1. 只改：IPassiveEffect / PieceModel / TurnManager / PieceManager / BattleController / PassiveFactory + 5 个新被动类，其他一律不动
2. 伤害结算、元素反应、金币、能量主流程不动
3. 不重构无关代码
4. 完成后自查诊断无报错即可，运行时验证由我方做
