# 城邦选择面板计数修复 — Trae 提示词

## 问题本质（现状）

城邦选择面板在「P1 选完 → 切到 P2 选」时，面板本身保持打开、只是切换了内容，但每次进入选择都重复登记了一次「面板打开」，最终关闭时只登记一次「面板关闭」，导致全局的面板开闭计数**残留 +1**。

这个计数 > 0 时会屏蔽棋盘点击、悬停和大招键。所以城邦选完后，选棋子、部署、对战全程棋盘都点不了，游戏卡死。

（对比：选棋子面板是「打开→关闭→打开→关闭」成对调用，所以没有这个问题；城邦选择面板是「打开→打开→关闭」，中间缺了一次关闭。）

## 改动方向

让「面板打开」的登记只在「面板从关闭状态切到打开」时发生一次；面板已打开时的内容切换（如 P1 切 P2）不重复登记。

## 效果目标（验收标准）

1. P1 选完切 P2 时，面板开闭计数不增加（不重复累加）。
2. 揭晓确认关闭面板后，计数归零，棋盘点击/悬停/大招键恢复正常，能正常进入选棋子、部署、对战。
3. 面板首次打开时仍正常登记（正确屏蔽棋盘）。
4. 城邦选择、提交、结算、揭晓、流程顺序全部行为不变。

## 红线（绝不能破坏）

1. **其他面板的开闭计数机制零改动**（商店/装备/选棋子/游戏结束面板的 Open/Close 配对不动）。
2. 城邦选择/提交/结算逻辑零改动（只改「面板开闭登记」这一处）。
3. 多选交互、选满才能提交、揭晓视图、流程顺序（城邦→P1 选棋子→部署→对战）不变。
