# 莫娜（水）· 领域控制 + 易伤 —— 棋子内容实现

## 棋子规格

- **移动**：不被阻拦（飞行移动，现有 FlyMoveProvider 能力，配置即可，无需新代码）
- **普攻**：通用单目标（现有能力）
- **被动（领域）**：普攻命中后，在**目标位置**留下半径 2 格的领域；每回合开始时对领域内的敌方棋子造成物理伤害；后续普攻会**更改领域的位置**（旧领域消失，新领域落在新目标位置）
- **大招（易伤）**：对**目标位置及周围 2 格**内的敌方棋子施加「易伤」——该棋子受到的伤害提高 50%，持续 4 回合

## 现状说明（已核对，供你定位）

- 领域是一个**全新的「区域实体」概念**——它不是棋子（不能占据格子、不能被选中/攻击/移动），只是「一个位置 + 一个半径 + 每回合生效」。项目里目前没有这个概念，最近的参照是安柏的傀儡，但傀儡是「真实棋子」，领域是「非棋子的区域」，两者本质不同。
- 「每回合开始结算」这个时机，项目里已有现成机制（回合开始时的持续状态递减/DoT 结算），领域可以挂在这个节奏上。
- 易伤（「受到伤害提高」）是一个**新 debuff**。现有伤害结算里只有「减伤」类被动（降低受到的伤害），没有「增伤/易伤」类效果，需要一个新的结算接入点让「受到的伤害 ×1.5」生效。

## 改动方向

1. **领域被动**：普攻命中后建立领域（位置 = 普攻目标格，半径 2）；每回合开始对领域内敌方造成物理伤害；再次普攻时领域移动到新目标位置（同一棋子同时只存在一个领域）。
2. **易伤大招**：对目标位置周围 2 格内的敌方施加易伤 debuff，持续 4 回合，期间这些棋子受到的所有伤害提高 50%。

## 红线（不要破坏）

- 领域**不是棋子**——不能被选中、不能被攻击、不占据格子、不参与胜负判定。不要把它做成第二个傀儡。
- 易伤只影响「之后受到的伤害」，不能干扰莫娜本次大招自己的结算顺序。
- 领域每回合的持续伤害，其元素附着遵循项目现有的「直接伤害默认带元素」规则（莫娜是水元素）——但注意它是**每回合持续 tick** 的伤害，请自行判断它与元素附着/反应交互的合理方式（是附着水元素还是按「非主动直接伤害」处理）。
- 不破坏：现有伤害管道、元素反应表、被动框架、回合流转。

## 验收要点

- 莫娜普攻后，目标位置出现领域，下个回合开始领域内敌方掉血（物理伤害）
- 莫娜再次普攻其他位置，旧领域消失、新领域落在新位置（场上始终只有一个领域）
- 莫娜大招后，目标周围 2 格敌方进入易伤，之后受到的伤害提高 50%，4 回合后恢复
- 领域不占据格子、不可被选中/攻击，不影响胜负判定
