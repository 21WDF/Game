# 直接伤害默认带元素（统一改造）· Trae 提示词

## 目标

落实「角色直接伤害默认带元素」规则：普攻、大招、被动造成的直接伤害，默认附着棋子的先天元素（innateElement）并触发元素反应，除非该伤害效果**明确配置为不带元素**。

伤害类型（物理/魔法/真实）与元素附着是两个独立维度——物理伤害也可以带元素。

## 需要实现

1. 为直接伤害效果类增加「是否带元素」的配置参数（默认带元素；false 时保持旧的「不附着元素」口径）
2. 克洛琳德路径伤害（VoltPathPassive）：物理 + 带雷元素
3. 杜林溅射（FlameNovaPassive）：物理 + 带火元素
4. 菲林斯强化态魔法段（ThunderSweepUltimate 强化态）：魔法 + 带雷元素

## 方向说明

- **物理伤害带元素**：走元素反应口径（含反应倍率/额外伤害 + 元素附着与消耗），参考现有 FireSlash / ThunderSweep 普通态的写法。
- **魔法伤害带元素**：保持魔法基础伤害（不受防御减伤），在其上增加元素附着与反应触发，参考现有魔法段口径 + 元素附着。
- **配置参数**：每个直接伤害效果类增加一个布尔参数（默认带元素），由 jsonParams 解析；false 时保持旧的「纯公式、不附着元素」口径。

## 保持不附着（「特别说明」特例，不要改）

- 反伤（荆棘刺甲）
- 感电 DoT
- 超载爆炸

## 不要破坏（红线）

1. 伤害类型三分类与防御减伤的现有逻辑（物理受防御、魔法/真实不受）
2. 普攻 / 大招已有的「走元素」口径
3. 元素反应表、伤害管道、被动框架
4. 护盾暂不实现（未来再做）

## 验收要点

- 克洛琳德路径伤害附着雷元素、触发超导/感电
- 杜林溅射附着火元素、触发蒸发/融化/超载
- 菲林斯强化态魔法段附着雷元素
- 配置为「不带元素」时，对应伤害保持不附着
- 反伤 / 感电 DoT / 超载爆炸仍不附着元素
