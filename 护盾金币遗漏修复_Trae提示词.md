# 护盾金币遗漏修复 Trae 提示词

> 只写「改动方向 + 效果目标 + 红线」，不写实现细节。
> 这是护盾一期修复的**补漏**：上轮漏了 3 处自定义溅射金币。

---

## 一、现状（问题定位）

护盾挡伤害时，标准金币（`OnDamageDealt` / `OnDamageReceived`）已正确拦截。但有 3 处**自定义比例金币**（用 `AddGold`，比例是 `splashGoldPercent` / `goldPercent`）**没有拦截**——护盾挡掉溅射伤害时，攻击方仍拿到金币。

这 3 处是：

1. **杜林溅射被动**（`FlameNovaPassive`）：金币在伤害结算**之前**就加了，没判断伤害是否被盾挡。
2. **元素溅射分支**（`BattleController`）：走 `AttackPiece(skipGold:true)` 后，又手动按比例补一笔金币，没判断这次攻击是否真造成了伤害。
3. **非元素溅射分支**（`BattleController`）：金币在伤害结算**之前**就加了，没判断。

## 二、改动方向

让这 3 处金币**只在「实际造成了伤害」时结算**——护盾挡掉伤害（目标没掉血）时，攻击方不拿这笔金币。

判断「实际造成了伤害」的依据（二选一，按场景自然选择）：
- 走统一伤害入口的地方，用它的返回值（它已经能告诉你伤害是否被护盾拦截）；
- 直接扣血的地方，用「目标扣血前后血量差」是否大于 0 来判断。

## 三、效果目标（验收标准）

1. 带护盾的目标被杜林溅射、元素溅射、非元素溅射命中（伤害被盾挡）时，攻击方**不拿**这笔自定义金币。
2. 盾碎后的余量、无盾目标，金币**照常**，且**金币数值和修复前完全一致**（比例、基数都不变）。
3. 标准金币的拦截行为不受本次改动影响。

## 四、红线（绝不能破坏）

1. **不改金币的比例和基数**——只在「给不给」上加判断，数值一分不动。
2. **不动标准金币**（`OnDamageDealt` / `OnDamageReceived` 那 10 处已经对的，别碰）。
3. **不动伤害结算本身**（溅射的伤害、元素附着、反应、死亡判定）。
4. **不动护盾拦截规则**、不动吸血拦截。
5. 其余一切与本次无关的行为保持不变。
