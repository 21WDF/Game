# 贸易之城二期（利息 + 信誉）· Trae 提示词

## 现状（框架事实，帮你理解问题本质）

贸易之城是城邦系统的第一个具体城邦，机制分三期。一期（透支 + 拍卖本体）已完成并验证通过，本期做二期「利息 + 信誉」，地下交易是三期。

与本期直接相关的现状：

1. **透支已实现**：一期已加 `TrySpendGoldWithOverdraft`（允许金币扣到透支下限，默认 -50，可配置），买装备/买道具/拍卖成交都走它。金币可以为负（欠钱状态）。
2. **时间单位「回合」≠「轮」**（铁律，见规格文档第〇节）：
   - 回合 = 单方行动一次（`CurrentTurn`），后手方结束只代表后手那一个回合结束。
   - 轮 = 双方各行动一次（`CurrentRound`），后手方结束才代表本轮结束。
   - 利息、信誉恢复按「轮」；欠钱扣信誉按「回合」。
3. **每轮结束的判定点**：`TurnManager.EndTurn` 里 `SwitchActivePlayer()` 切回 P1 时，`CurrentRound` +1，即一轮完成。目前 `EndTurn` 只有「每回合」钩子（城邦 + 拍卖），**没有独立的「每轮」结算钩子**——利息/信誉恢复需要一个按「轮」触发的结算点。
4. **商店购买扣款点**：`EquipmentManager.BuyEquipment`（买装备）、`GridItemManager.BuyAndUse`（买道具），都已是透支版扣款。商店涨价要作用在这两处的售价上。
5. **拍卖出价**：`AuctionManager.PlaceBid`，信誉门槛要加在这里。
6. **信誉状态目前不存在**：项目里没有任何「信誉值」字段，需要新增一个按玩家存储信誉的状态载体。

## 改动方向

### 一、利息机制（每轮结算，仅正利息）

每轮结束时，双方各自结算利息：
- 金币 > 0：每拥有 10 金币，获得 1 金币利息（向下取整）。
- **无负利息**：金币 < 0 时不扣金币利息（欠钱数不会自己滚大，除非继续透支消费）。
- 结算时机按「轮」（后手方结束行动、一轮完成时），不是按「回合」。

### 二、信誉系统

新增按玩家存储的信誉状态，默认满 10 点。

- **扣信誉（周期制）**：欠钱状态下，每持续欠钱满 8 个回合扣一次信誉，扣 `floor(欠钱数/10)` 点（欠钱数 = 负金币绝对值，向下取整）。欠 11 → 扣 1，欠 39 → 扣 3，欠 < 10 → 扣 0。「周期性」= 扣完后继续欠钱、再满 8 回合会再扣一次；中途还清（金币 ≥ 0）则计数重置，再次欠钱重新计数。扣信誉的计数按「回合」计。
- **恢复**：金币 ≥ 0（脱离欠钱）后，每轮恢复 1 点信誉（按「轮」），上限 10。

**惩罚只有两条**：

1. **商店涨价**：商店购买（买装备 + 买道具）的售价上涨，涨幅 = `(10 - 当前信誉) × 10%`。地下交易和拍卖会**不受影响**（拍卖成交价、拍卖底价不涨）。信誉 10 = 原价，信誉 8 = +20%，信誉 5 = +50%，信誉 0 = +100%。商店永远可买，只是随信誉变贵。
2. **信誉 < 6 不能进拍卖行**：信誉低于 6 时无法在拍卖行出价（地下交易和商店不受影响）。信誉 ≥ 6 可正常拍卖。

### 三、UI 展示（可选但建议）

商店/拍卖界面能体现信誉状态和涨价后的价格。具体形式由你定，但至少要让玩家能感知「当前信誉值」和「商店价格已因信誉上涨」。

## 效果目标（验收标准）

1. 每轮结束结算正利息（每10金币+1，向下取整），按「轮」触发；金币 < 0 时不扣金币利息。
2. 信誉默认 10，按玩家独立存储。
3. 欠钱状态下，每持续欠钱满 8 回合扣一次信誉（`floor(欠钱数/10)`，欠 11 扣 1、欠 39 扣 3、欠<10 扣 0），扣完继续欠钱再满 8 回合会再扣；中途还清则计数重置。
4. 金币 ≥ 0 后每轮恢复 1 点信誉，上限 10。
5. 商店购买价格 = 原价 × `(1 + (10-信誉)×10%)`；信誉 10 时原价；拍卖出价和底价不受涨价影响。
6. 信誉 < 6 时无法在拍卖行出价；商店和地下交易不受此限制。
7. 信誉满 10 时，商店原价、拍卖正常，行为与一期完全一致。

## 红线（绝不能破坏）

1. **时间单位铁律**：利息、信誉恢复按「轮」（后手方结束、一轮完成时）；欠钱扣信誉的计数按「回合」（单方行动）。绝不能混用。
2. **涨价只作用商店购买**（买装备 + 买道具），拍卖成交价、拍卖底价、拍卖出价**不涨**。
3. **禁拍卖只作用拍卖行出价**，商店购买和地下交易不受影响。
4. **信誉满 10 时一切行为与一期一致**：商店原价、拍卖正常，零额外效果。惩罚只在信誉下降后逐步显现。
5. **不做地下交易**（三期），不做累计伤害统计。
6. **不动一期已完成的拍卖本体逻辑**：出价校验、成交倒计时、刷新、已售去重等保持原样，只加「信誉 < 6 禁止出价」这一个门槛。
7. **不动伤害管道、元素附着、元素反应、护盾染色、城邦框架**。

## 明确不做（三期）

- 地下交易（累计伤害统计 + 概率触发 + 独属物品池）。
- 负利息（已取消，欠钱不扣金币利息）。
- 信誉 = 0 的任何额外惩罚（已砍掉 50% 伤害，信誉最低只表现为「商店 +100% 涨价 + 禁拍卖」）。
