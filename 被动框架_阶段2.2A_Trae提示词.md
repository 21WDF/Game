# 阶段②被动框架 2.2A（第一批纯数值被动）· Trae 提示词

## 任务
实现 6 个被动，让框架的 4 个事件钩子 + 2 个伤害介入点都跑通一次真实调用，验证事件驱动框架可用后再上 B/C 批。

## 一、新建 5 个被动类（放 Assets/Game/Equipment/Effects/）
每个类实现 IPassiveEffect，构造函数收 JSON 参数，只覆写需要的钩子（其余钩子用接口默认空实现，不要重写）。

| 类名 | 参数(JSON) | 覆写的钩子 | 语义 |
|---|---|---|---|
| LifestealPassive | percent | OnDamageDealt | 攻击造成伤害后，回复 owner「伤害值 × percent%」（向下取整） |
| DamageReductionPassive | amount | （不覆写钩子，见下方伤害介入） | 受到攻击时伤害 -amount |
| RegenPassive | amount | OnTurnStart | 回合开始回 amount 血 |
| ThornsPassive | amount | OnDamageReceived | 对 LastDamageSource 反 amount 伤害 |
| KillAttackPassive | amount | OnKill | owner 攻击力永久 +amount（累积到棋子死亡） |

ExtraDamagePassive 已存在，本批只接入结算，不新建。

## 二、伤害介入（只改 PieceManager.AttackPiece 的普攻路径，别动大招）
在现有 damage 计算之后、`target.TakeDamage(damage)` 之前，把最终伤害改为：
```
最终伤害 = max(1, 基础伤害 + 攻击方附加伤害总和 - 受击方减伤总和)
```
- 附加伤害总和 = 遍历 attacker 的全部被动（GetAllPassives），累加 ExtraDamagePassive.Amount
- 减伤总和 = 遍历 target 的全部被动，累加 DamageReductionPassive.Amount
- 提取附加伤害 / 减伤各写一个 PieceManager 私有方法，遍历写法参照现有 CollectReactionModifiers

改完后，damage 的后续引用（浮动跳字 / 金币 / 日志）自动用新值，不要额外改动。

## 三、击杀加攻的状态字段（改 PieceModel）
- 加一个可累积的攻击加成字段（如 KillAttackBonus），KillAttackPassive.OnKill 里累加
- 把这个字段纳入 EffectiveAttack 的聚合计算

## 四、反伤的边界约束（ThornsPassive）
- 反伤直接对 LastDamageSource 调 TakeDamage，**不要走 AttackPiece**（避免递归触发被动链）
- 若反伤致死攻击者，处理其死亡销毁（DestroyPiece，killer 传 owner）
- owner 即使本次被打死，仍正常反伤（同归于尽成立）

## 五、改 PassiveFactory
switch 里补 5 个新 case；每个新参数类都要标 [System.Serializable]（参照现有 ExtraDamageParams / FireSlashParams 写法，别重蹈 ExtraDamageParams 的坑）。

## 约束（必须遵守）
1. 只改上述文件：5 个新被动类 + PassiveFactory + PieceModel + PieceManager.AttackPiece，其他文件一律不动
2. 大招（FireSlashUltimate / UseUltimate）的伤害路径本批不动
3. 不重构现有代码，不"顺手优化"无关逻辑
4. 完成后自查诊断无报错即可，运行时验证由我方做
