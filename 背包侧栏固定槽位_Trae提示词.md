# 背包侧栏固定槽位_Trae提示词.md

> 铁律：本提示词只写「改动方向 + 效果目标 + 红线」，不写任何实现细节。实现方式由你决定，我只验收结果。

## 〇、背景（一句话）

现有背包侧栏是「装备区动态列表 + 道具区动态列表」（背包有几件就动态生成几个格子）。**这是错的**，要改成云顶之弈（TFT）式的「**固定空槽位**」：左侧一条滑动视图，里面**预置 10 个空槽**，购买装备/道具后**填充**到空槽里。空槽始终摆在那里（有淡底），玩家一眼看到「10 个格，填了几个」。

## 一、现状（框架事实）

1. 侧栏脚本 `UI_BackpackSidebar` 现在有三个容器：`slotsContainer`（选中棋子已装备 3 槽）、`equipmentContainer`（装备动态列表）、`itemsContainer`（道具动态列表）。
2. 图标格子组件 `UI_BackpackIconSlot` 已就绪（`iconImage` + `highlight` 选中高亮 + `labelText` 小字 + 单击单选 / 双击 / 悬停 tooltip），可复用。
3. tooltip 浮层 `UI_ItemTooltip` 已就绪，可复用。
4. 数据源：装备 = `EquipmentManager.GetBackpack(side)`（`List<EquipmentModel>`，按获得顺序追加）；道具 = `GridItemManager.GetInventory(side)`（`List<GridItemData>`，按获得顺序追加）。两者都已有 `icon` 字段。
5. 侧栏常驻约定（不可破坏）：不调用 `InputHandler.RegisterPanelOpen/Close`（常驻面板锁死棋盘的死穴），穿透阻挡靠 `UIPanelRaycastBlocker`；内容随活动玩家切换（订阅 `TurnModel.OnTurnStarted`），战前隐藏、开战后首个回合显示、游戏结束隐藏。

## 二、改动方向

### 1. 固定空槽位（核心，别再做动态列表）

- 侧栏的「装备区 + 道具区」合并成**一套固定 10 个空槽位**。
- **10 个槽位是预置的、始终存在的**（不是背包有几件就生成几个）。没物品的槽显示「空槽」的淡底，有物品的槽显示图标。
- 填充规则：把「装备列表（按获得顺序）+ 道具列表（按获得顺序）」合并，**装备在前、道具在后**，按顺序填入前 N 个空槽（N = 装备数 + 道具数）。第 N+1 到第 10 个槽保持空槽淡底。
- 最多展示 10 件（超出 10 件部分本次暂不展示，后续再处理）。
- 槽位布局（横排/竖排、是否滚动）由 Unity 侧的 ScrollRect + LayoutGroup 决定，代码只需「预置 10 个固定槽位 + 按顺序填充」，不关心排列方向。

### 2. 槽位的三种状态

- **空槽**：淡底占位（无图标）。
- **装备**：显示装备 icon + 悬停 tooltip（名称 + 属性，复用 `UI_ShopPanel.BuildStatsText`）。
- **道具**：显示道具 icon + 悬停 tooltip（名称 + 描述 + AP，复用 `UI_ShopPanel.BuildGridItemDesc`）+ 可选小字名称。

### 3. 交互（延续现有，不改）

- 单击图标 → 单选高亮。
- 悬停 → tooltip。
- 双击道具 → 使用（`GridItemManager.UseFromInventory`）。
- 双击装备 / 空槽 → 无动作（拖拽下一步做）。

### 4. 保留「已装备槽位区」

- `slotsContainer`（选中棋子的 3 个已装备槽位，只展示 + tooltip）保留现状，不在本次改动范围内（除非它阻碍了固定槽位区，那也只需保持它的展示行为不变）。

## 三、效果目标（验收标准）

1. 侧栏（开战后）**始终显示 10 个空槽**（淡底），不随背包件数增删槽位。
2. 购买装备 → 装备图标**填充到第一个空槽**（不是新增一个格子）。
3. 购买道具 → 道具图标填充到下一个空槽；装备在前、道具在后。
4. 空槽淡底可见、有物品的槽显示图标（无 icon 的用占位色）。
5. 单击选中、悬停 tooltip、双击道具使用，行为与改动前一致。
6. 最多显示 10 件；第 10 件之后的新物品暂不展示（不崩溃）。
7. 侧栏仍：不屏蔽棋盘（不调面板计数）、随活动玩家切换、战前隐藏、开战显示、结束隐藏。

## 四、红线（绝不能破坏）

1. **固定空槽是「预置」，不是「动态生成」**——这是本次与上次最根本的区别，别再按背包件数 Instantiate 格子。
2. **侧栏仍不得调用 `RegisterPanelOpen/Close`**——棋盘不能被锁死。
3. **装备/道具的管理逻辑（`BuyEquipment` / `BuyToInventory` / `UseFromInventory` / `EquipToPiece` / `UnequipFromPiece`）零改动**——本次只改「展示层」，不改任何数据/使用/购买逻辑。
4. 商店、拍卖、地下交易、伤害管道、元素、护盾、城邦过滤零改动。
5. 道具 `icon` 字段、`GridItemData` 结构零改动。

## 五、明确不做（下一步及以后）

- 拖拽（装备拖到棋子装备、道具拖到棋子使用）。
- 装备/卸下动作（由拖拽接管）。
- 槽位的玩家手动排序/整理。
- 严格的「装备与道具交叉购买顺序」（本次用「装备前、道具后」的简单规则）。
- 超出 10 件的溢出处理（本次暂不展示）。
