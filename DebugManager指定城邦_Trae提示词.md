# DebugManager 指定城邦 — Trae 提示词

## 现状（框架事实）

- DebugManager 在调试模式开启时，通过「取消 GameFlowController 挂起的流程调用 + 停用该组件」的方式，已经跳过了整个战前流程（城邦选择、选棋子、部署），直接摆 7v7 开战。
- 因此调试模式下**城邦选择 UI 不会弹**，但本局城邦也**没有被设置**（保持 None），导致「机制按城邦过滤」在调试时全部不生效——想测贸易/元素城邦的机制时没有入口指定城邦。
- 城邦管理器已提供「直接设置本局城邦」的公开接口（设置后会触发城邦变化事件，各专属机制据此过滤生效）。

## 改动方向

给 DebugManager 增加一个「指定本局城邦」的调试字段（枚举类型，Inspector 下拉选择，None 表示不指定）。调试启动时，若该字段不是 None，就跳过城邦选择、直接把它设为本局城邦；设为 None 则保持不指定（行为与现状一致）。

## 效果目标（验收标准）

1. 调试模式 + 指定某城邦 → 开局直接就是该城邦，该城邦的专属机制（如贸易的拍卖/利息/地下交易、元素的染色）照常生效。
2. 调试模式 + 字段为 None → 本局城邦保持 None，所有专属机制不生效（与现状一致）。
3. 非调试模式（enableDebugMode=false）→ 完全不影响，仍走正常城邦选择流程。

## 红线（绝不能破坏）

1. **不修改任何现有脚本**：DebugManager 保持「仅通过单例 + 公开 API 操作」的独立调试模块定位，只调用城邦管理器的公开设置接口，不改动城邦选择管线、纯函数、随机入口。
2. 城邦设置的正常流程（选择 UI、求交集、随机）零改动。
3. `enableDebugMode=false` 时 DebugManager 行为与之前完全一致（等同脚本不存在）。
