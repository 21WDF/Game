# Chaotic Chess（混沌棋局）项目交接报告

> 本报告供接手本项目的下一个 AI 快速上手。请先通读全文，再开始动手。文末附「用户提示词要求」，**动任何代码前必须遵守**。

---

## 一、项目概览

| 项 | 内容 |
|----|------|
| 游戏 | 本地双人热座 Hex 战棋（回合制，元素反应为核心玩法） |
| 引擎 | Unity 6 LTS（6000.3.19f1）+ URP |
| 架构 | MVC + ScriptableObject（Model 纯数据无 Unity 依赖，View 纯渲染，Controller 驱动） |
| 阶段 | Phase 2 中后期，**9 棋子内容全部实现完毕**（代码 + asset），核心循环完整可玩，进入配置 + Playtest 阶段 |
| 代码组织 | 全单例跨模块访问、无 namespace（类名冲突风险，见残留问题） |

**核心循环**：选棋子 → 部署 → 移动/攻击/装备/道具/大招 → AP 耗尽换人 → 全灭判胜负。

---

## 二、目录结构（Assets/Game/）

```
Game/
├── Core/            # HexCoord、GameConfig、GameEnums 等基础
├── ChessBoard/      # 棋盘 Model/Controller（含 FindPath 寻路）
├── PieceLayout/     # 占据权威（谁在哪个格）
├── Piece/
│   ├── Model/       # PieceData（SO）、PieceModel（运行时）、EnergyModel、PieceRegistry
│   ├── Controller/  # PieceManager（核心：伤害管道/死亡/大招/召唤）
│   ├── Passives/    # 11 个角色被动（棋子专属）
│   ├── Effects/     # IUltimateEffect 接口 + 11 个大招类
│   ├── Range/
│   │   ├── Move/    # 5 个移动模板 + IMoveRangeProvider 接口
│   │   ├── Attack/  # 6 个攻击模板 + IAttackRangeProvider 接口
│   │   └── RangeProviderFactory.cs   # 统一工厂
│   ├── View/        # PieceView
│   └── Data/        # 18 个棋子 .asset（旧 8 + 新 9 + 兔兔伯爵傀儡）+ PieceRegistry
├── Equipment/
│   ├── Effects/     # IPassiveEffect 接口 + PassiveFactory + 14 装备被动 + Elemental/（元素装备）
│   ├── Model/       # EquipmentData、EquipmentModel
│   └── Controller/  # EquipmentManager
├── Battle/          # BattleController（选中/移动/攻击/大招的交互状态机）
├── Turn/            # TurnManager（回合流转 + 持续状态递减）
├── Systems/         # DamageCalculator、ElementReactionTable
├── Client/          # FloatingText（跳字）、FloatingTextPool、FloatStyles、ElementColorMapper
├── ActionPoint/     # AP 系统
├── Gold/            # 金币系统
└── GridItem/        # 棋盘道具
```

---

## 三、核心系统架构（已完成）

### 1. 伤害管道（DamageCalculator + PieceManager）
- **DamageSource 位掩码**：Attack=1 / Splash=2 / Ultimate=4 / Dot=8 / Reflect=16
- **DamageKind 三分类**：Physical（物理）/ Magical（魔法）/ True（真实）
- 统一伤害入口 `ApplyIncomingDamage(target, damage, source, damageType, damageKind, element)`，含减伤→易伤→首伤=1→死亡之蔑延迟→扣血→免死→跳字的完整结算链
- 附加伤害独立段：物理/魔法/真实三份各自结算（ExtraDamagePassive）

### 2. 元素系统（ElementReactionTable + DamageCalculator）
- 4 元素（火/水/雷/冰）+ 6 反应（蒸发/融化/超载/冻结/感电/超导）
- Gauge 附着消耗算法（克制×2/消耗减半），ReactionDatabaseSO 数据驱动
- 反应副作用：超导减防、感电 DoT、冻结、超载爆炸 AOE、击退

### 3. 被动框架（IPassiveEffect + PassiveFactory）
- **9 个事件钩子**：OnEquip / OnUnequip / OnTurnStart / OnAttacked / OnDamageReceived / OnDamageDealt / OnKill / OnAllyDeath / **OnAllyUltimateCast** / **OnAllyAttackHit**（后两个为雷电将军新增）
- 唯一被动去重（UniqueId 仅作用于装备被动，角色被动不参与）
- 内建被动（builtInPassives）在 SpawnPieceById 时创建 + 触发 OnEquip（2026-08 修复过的关键缺口）

### 4. 移动/攻击范围（Provider 模式）
- 移动模板 5 个：FreeMove（BFS）/ StraightMove（直线）/ FlyMove（飞越 BFS）/ StraightPass（直线穿越）/ ParityMove（偶数距离跳跃）
- 攻击模板 6 个：Circle / Line / Area（溅射）/ MinMax（环形）/ Cone（扇形）/ RingTangent（环切线3格）
- 路径生成已下沉到 Provider（IMoveRangeProvider.FindPath），预览线与实际移动同源
- **范围配置内联进 PieceData**（2026-08 重构，砍掉了 MoveRangeConfig/AttackRangeConfig 两个 SO 资产层）

### 5. 大招框架（IUltimateEffect）
- `UltimateTargetMode`：Enemy（指定敌人）/ Self（自身增益）/ Tile（指定格子，可点空格）
- `UltimateTileShape`：Circle（圆形）/ Ray（6 方向射线，冲刺用）
- 三参 Execute(caster, target, targetCoord) 支持指定格模式，两参 Execute 向后兼容

### 6. 跳字系统（FloatingText + FloatStyles）
- TextMeshPro（TextMeshProUGUI），对象池复用
- **颜色 = 元素**（火/水/雷/冰），**样式 = 伤害类型**（物理=纯色块 / 魔法=渐变 / 真实=深色描边）——两个维度正交
- 反应名提示（「蒸发！×2」）独立紫色跳字

### 7. 其他机制
- 蓄力系统（甘雨，usesChargeSystem 开关替代能量条）
- 召唤物（安柏傀儡，isSummon 标记，真实 PieceModel 但不可选中/不计胜负）
- 复活（哥伦比亚，DestroyPiece 顶部拦截，一局一次）
- 领域实体（莫娜，非棋子的区域，每回合 tick）
- 协同攻击（雷电将军，OnAllyAttackHit 广播 + static 重入守卫防递归）
- 全局回能（雷电将军，OnAllyUltimateCast 广播）
- 易伤 debuff（莫娜，ApplyVulnerability 三处伤害管道接入）
- 减防（爱可菲，独立字段与超导并行）

---

## 四、9 棋子内容清单（全部实现完毕）

| id | 棋子 | 元素 | 定位 | 被动（Passives/） | 大招（Effects/） |
|----|------|------|------|------|------|
| 1 | 菲林斯 | 雷 | 蓄力爆发 | SparkEmpowerPassive（普攻后强化6回合） | ThunderSweepUltimate（普通态横向3格物理 / 强化态周围1格魔法） |
| 10 | 杜林 | 火 | 直线穿透 | FlameNovaPassive（普攻后周围2格溅射） | FlameLanceUltimate（直线6格穿透） |
| 11 | 克洛琳德 | 雷 | 冲锋切入 | VoltPathPassive（移动路径伤害） | ThunderDashUltimate（直线冲刺+回血） |
| 12 | 哥伦比亚 | 水 | 团队辅助 | RevivePassive（己方死亡复活，一局一次） | TideSurgeUltimate（周围5格己方+攻） |
| 13 | 安柏 | 火 | 召唤嘲讽 | DoubleStrikePassive（普攻两段） | BaronBunnyUltimate（投放傀儡） |
| 14 | 兔兔伯爵 | 无 | 傀儡（isSummon） | PuppetPassive（受击3次/6回合/主动引爆） | 无 |
| 15 | 莫娜 | 水 | 领域控制 | DomainPassive（普攻落领域，每回合伤害） | OmenUltimate（易伤：受伤+50%） |
| 16 | 爱可菲 | 冰 | 控场持续 | FrostBreakPassive（减防）+ BlizzardPassive（风暴） | FrostFeastUltimate（伤害+治疗+激活风暴） |
| 17 | 雷电将军 | 雷 | 能量引擎 | StormEyePassive（队友开大回能）+ CoordinatedStrikePassive（协同） | MusouStrikeUltimate（横向3格+激活协同） |
| 18 | 甘雨 | 冰 | 蓄力狙击 | 无（蓄力=usesChargeSystem 系统开关） | FrostArrowUltimate（蓄力三档：1单点/2周围1格/3周围2格） |

旧 8 棋子（id 2~9）：Assassin/Archer/Heavy/Knight/Mage/Priest/Ranger/Warlock，仍为旧 Warrior/Knight 等原型内容，**尚未替换为原神角色规格**（见残留问题）。

---

## 五、关键设计决策（长期有效，勿违背）

1. **全局被动 = 在场即生效**（哥伦比亚复活、雷电将军回能，死亡后失效）
2. **甘雨蓄力完全替代能量**（无能量条，蓄力层数 0-3 驱动大招）
3. **「横向3格」= 环切线3格**（目标格 + 同环左右 2 邻居）→ RingTangentProvider
4. **嘲讽 = 强制锁定**（安柏傀儡在场，敌方本回合只能攻击傀儡）
5. **范围配置内联 PieceData**（砍掉 SO 资产层，避免资产爆炸）
6. **伤害类型 与 元素附着 是正交维度**：
   - 伤害类型：物理（受防御+护盾）/ 魔法（仅护盾）/ 真实（无视防御+护盾）
   - 元素附着：直接伤害默认带元素（附着 innateElement、触发反应），除非特别说明（反伤/感电DoT/超载爆炸不附着）
   - **护盾暂不实现**（只定规则，当前护盾=0）
7. **跳字视觉规范**：颜色=元素（非元素城邦默认物理红/魔法紫/真实白）、样式=伤害类型（物理纯色块/魔法渐变/真实描边）

---

## 六、当前状态

**已完成**：
- 9 棋子 + 兔兔伯爵傀儡的代码全部实现并核对通过（零遗留 bug）
- 伤害管道、元素反应、被动框架、装备、范围 Provider、大招框架、跳字系统全部闭环
- 范围配置内联重构完成（代码 + 12 资产迁移 + 删 31 个 SO 文件）
- 18 个棋子 asset 文件全部存在（含甘雨 id=18）

**未完成（见下节残留问题）**。

---

## 七、残留问题 / 待办清单（按优先级）

### P0（红线，必须处理）
1. **无 git**——项目没有任何版本控制，一次误操作就是全盘重来。**接手后第一件事就是 `git init` + 首次提交**，否则后续任何改动都无回退能力。

### 高优先级
2. **城邦系统未实现**——Design Pillar 3「城邦效果让每一局不像上一局」目前没有载体，是五条设计支柱里唯一悬空的。
3. **棋子视觉占位**——18 个棋子的 `prefab` 字段基本为空（`{fileID: 0}`），玩家看到的仍是 Cube 占位。需在 Editor 拖入含 PieceView 的预制体。
4. **旧 8 棋子未替换**——Assassin~Warlock 仍是旧 Warrior/Knight 原型，未按原神角色规格（9 棋子）替换。当前 9 新棋子已就绪，旧 8 棋子是「遗留内容」。

### 中优先级
5. **无音效**——全项目无 AudioManager，无任何音效。
6. **数值全为占位**——所有棋子 maxHP/attack/defense、被动参数、大招参数都是 `[PLACEHOLDER]` 或 0，从未做过 playtest 调平。
7. **GetAllPassives 每次分配新 List**——高频查询有 GC 压力，后续可优化为缓存。

### 低优先级 / 显示层
8. **溅射跳字重复**——`BattleController.cs` 两处溅射结算（约 L595/L625），在 `AttackPiece`/`ApplyIncomingDamage` 内部已跳字后又手动补一次 `ShowDamage`，同位置重叠显示两个数字。需删除手动补的跳字。
9. **物理默认色是白非红**——用户方案里写「物理红」，但 `GameConfig.floatDamagePhysical` 实际配的是白。是否调红待用户拍板。

### 待用户确认的语义点
10. 菲林斯强化态「6 回合」当前 = 6 全局回合 = 3 轮次 = 3 次行动回合。若用户想要「6 次行动回合」需改 12。
11. 甘雨 attackConfig 用 Circle（近身也能蓄力）还是 MinMax（远程狙击手、近身不能攻击），待用户定。

### Playtest 失败信号（调平时参考）
- 单局 >40 分钟 → 节奏太慢
- 金币囤积 >120 → 经济通胀
- 某棋子/装备组合胜率 >70% → 平衡崩了

---

## 八、配置层待办（用户在 Unity Editor 完成）

1. **拖 prefab**：给所有新棋子 asset（杜林~雷电将军 id 10~18 + 兔兔伯爵 id 14）的 `prefab` 字段拖入含 PieceView 的预制体。
2. **注册 PieceRegistry**：把所有棋子 asset 拖入 `PieceRegistry.pieces` 列表。安柏（id13）自动进选人池，兔兔伯爵（id14，isSummon）被过滤、仅供 `SpawnPieceById(14)` 查找。
3. **数值填写**：按用户平衡目标填 maxHP/attack/defense 和各被动/大招的 jsonParams（参考 `棋子配置生成器.xlsx`）。

---

## 九、辅助工具与文档

| 文件 | 用途 |
|------|------|
| `棋子配置生成器.xlsx` | 9 棋子+傀儡的完整配置表（被动/大招/移动/攻击生成器 + 速查 + 清单），数值平衡用 |
| `gen_pieces_config.py` | 生成上表的脚本，加新棋子往数组加数据重跑 |
| `gen_range_configs.py` | 生成范围配置资产（已因内联重构废弃，仅作历史参考） |
| 项目根目录 `*_Trae提示词.md` | 历史提示词（约 30 个），记录了每轮改动的方向 |
| `.workbuddy/memory/MEMORY.md` | 长期设计决策（本文部分内容来源） |
| `.workbuddy/memory/YYYY-MM-DD.md` | 每日工作日志 |
| `C:\Users\MECHREVO\WorkBuddy\2026-07-27-23-05-25` | 22 天完整开发历史档案 |

---

## 十、交接上手建议

1. 先 `git init` 提交当前状态（P0）。
2. 通读 `MEMORY.md` 和最近的工作日志，理解设计决策。
3. 在 Unity 里跑一局，验证核心循环 + 各棋子机制（日志里有 `[Domain]`/`[Blizzard]`/`[CoordinatedStrike]` 等标签可跟踪）。
4. 优先处理「城邦系统」和「旧 8 棋子替换」这两块内容层工作（框架已全就绪）。
5. 任何代码改动，先看文末「用户提示词要求」。

---

# 用户提示词要求（务必严格遵守）

## 工作分工（铁律）
- **用户负责**：设计方向、拍板设计决策、asset 配置（PieceData 字段填写、className/jsonParams 引用）。
- **AI（Trae）负责**：代码实现。**只写代码，不做 asset 配置**（除非用户明确要求）。
- **本助手负责**：出方向提示词、最后核对代码实现是否正确。

## Trae 提示词风格（最重要的一条）
给 Trae 的提示词**只写三件事**：
1. **改动方向**（要达成什么效果）
2. **效果目标**（验收标准）
3. **红线**（什么不能破坏）

**绝对不写实现细节**：
- ❌ 不加方法名建议（不说「给 XX 加个 GetPath 方法」）
- ❌ 不写算法步骤
- ❌ 不给伪代码

原因：如果我把「怎么做」写进提示词，Trae 就变成打字机，我出错它必然跟着错。分工必须是「我出方向 → Trae 实现 → 我最后核对」，三个环节各有价值。

**可以写**的例外：「现状/框架事实」（帮助 Trae 理解问题本质、避免表面修补，例如「可达范围和路径生成是两套逻辑」这类问题定位），以及「容易踩的坑/红线」。

## 核对规范（按复杂度取舍，不是每次 Trae 改完都核对）
- **简单/低风险改动**（纯几何 Provider、单一效果类、纯 View、机械增删、配置默认值）：**只看 Trae 汇报 + 抽查关键点即可，不必逐行读码**。
- **复杂改动**（涉及 BattleController 状态机、伤害管道、回合流转、被动框架接口、多文件逻辑联动、字段增删/重构）：升级到「逐行读源码 + 追踪调用链 + 交叉验证多个文件」。
- 复杂改动核对不能只看 Trae 的汇报摘要（Trae 的汇报描述的是意图，不是实际代码）。
- 核对结论要给出「文件 → 判定 → 关键验证点」的清单，并报告「非 bug 的观察点」和「需用户知晓的口径说明」。

## 其他约定
- 中文命名习惯（设计文档、SO 资产、棋子名）。
- 分析报告放 `.workbuddy/artifacts/` 或项目根目录，不污染 Assets。
- Trae 提示词放项目根目录，文件名 `xxx_Trae提示词.md`。
- Excel 配置表**保留**（用于后期数值平衡，不是中间产物）。
- 跳字颜色规范：涨红跌绿（中国股市约定，与美股相反）；货币用 ¥（如涉及经济显示）。
