# 补全伤害跳字（DoT / 延迟伤害 / 附加伤害 / 反伤等）

## 需求
现在只有普攻主伤害会跳伤害数字，以下几类伤害都没有跳字，玩家看不到数值，需要补齐：

- 感电 DoT 每回合烧血
- 死亡之蔑的延迟伤害结算
- 附加伤害独立段（物理/魔法/真实）
- 反伤、超载爆炸、非元素溅射（这些走统一伤害入口 ApplyIncomingDamage）

## 改动方向

1. **加两个伤害类型颜色**：给魔法伤害、真实伤害各加一个独立跳字颜色（在 GameConfig 加两个颜色字段并给默认值）；在颜色映射器里加一个「按伤害类型取颜色」的方法——物理 → 物理白、魔法 → 魔法色、真实 → 真实色。

2. **统一伤害入口补跳字**（ApplyIncomingDamage 扣血后）：这样 DoT、反伤、超载爆炸、非元素溅射都会自动跳字，一处改全覆盖。

3. **延迟伤害补跳字**（死亡之蔑的回合结算处）。

4. **附加伤害独立段补跳字**（按各自伤害类型上色）。

## 约束（务必遵守）

- 跳字是纯 View 层反馈：**不改任何伤害数值、结算顺序、死亡判定、免死逻辑**。
- 跳字位置用受击方棋子的世界坐标（其 View 的 transform.position）。
- 沿用现有跳字池和样式接口，不新增跳字动画类型，只按伤害类型传颜色。
- 池未就绪（Instance 为 null / View 为 null）时静默跳过，沿用现有跳字的安全兜底写法。
- 不动普攻主伤害已有的跳字逻辑。
- 只改 GameConfig、颜色映射器、PieceManager、死亡之蔑被动这几个文件，其余不碰。
