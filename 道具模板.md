# 道具模板 设计规范（棋盘道具）

> 体系：`IGridItemEffect` + `GridItemData`（接口 + 工厂 + ScriptableObject）
> 目录：`Assets/Game/GridItem/`

---

## 一、体系定位

道具模板 = **棋盘道具**，是作用于棋盘/棋子的消耗品，购买即用（不同于装备的持续占用槽位）。当前接口：

```csharp
public interface IGridItemEffect
{
    bool CanExecute(HexCoord target, PlayerSide user);  // 瞄准前过滤可点目标
    void Execute(HexCoord target, PlayerSide user);     // 执行效果
}
```

`target` 语义由各实现自定义（扩展石 = 新增格坐标，删除石 = 删除格坐标，传送石 = 传送落点）。

---

## 二、现有模板（3 种）

| 道具 | effectClassName | 价格 | AP | 语义 | 关键参数 |
|------|----------------|:---:|:---:|------|---------|
| 扩展石 | ExpandTileEffect | 20 | 1 | 棋盘外新增 1 格 | 幽灵格方案 |
| 删除石 | RemoveTileEffect | 15 | 1 | 删除 1 个空格 | minTilesAfter=7 |
| 传送石 | TeleportUnitEffect | 30 | 1 | 传送己方棋子 | maxRange=5 |

---

## 三、模板框架（GridItemData 字段）

| 字段 | 类型 | 含义 |
|------|------|------|
| `id` / `displayName` | int / string | 标识与显示名 |
| `price` | int | 商店售价 |
| `apCost` | int | 使用时消耗 AP |
| `effectClassName` | string | 工厂创建用 |
| `effectJsonParams` | string | 构造参数 JSON |
| `requiresSelectedPiece` | bool | 是否需要先选中己方棋子（传送石 true） |
| `minTilesAfter` | int | 防软锁下限（删除石用） |

**这套字段已经具备扩展性**：`requiresSelectedPiece`、`minTilesAfter`、`apCost` 都是可配置的，加新道具只需加一个效果类 + 一个 .asset。

---

## 四、扩展方向（建议，待确认）

现有 3 种全是"改棋盘"类。可沿 `IGridItemEffect` 框架补充"作用于棋子/资源"类道具：

| 候选道具 | 语义 | requiresSelectedPiece | 备注 |
|---------|------|:---:|------|
| 回血药 | 回复选中棋子 HP | true | 复用治疗逻辑 |
| 元素清除 | 清除选中棋子身上的附着元素 | true | 反制元素流 |
| 地形改造 | 给格子附加元素残留 | false | 配合元素城邦 |
| 额外 AP | 回复 1 点 AP | false | 资源类 |

**建议**：先补齐 1-2 个"作用于棋子"的道具（如回血药），验证 `requiresSelectedPiece` 分支在"治疗类"道具上是否顺畅，再决定要不要扩更多。

---

## 五、架构对接

```
新增道具效果类（如 HealItemEffect）
GridItemManager.CreateEffect 工厂加 case
GridItemData 无需加字段（现有 8 字段够用）
```

零推翻：现有 3 种道具、购买即用流程、幽灵格/高亮瞄准机制全部不动。

---

## 六、待确认项

1. 是否需要补充"作用于棋子"类道具（回血药/元素清除等）？还是保持纯棋盘改造？
2. 现有 3 种道具的数值（价格/AP）是否需要调整？
