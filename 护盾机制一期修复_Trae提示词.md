# 护盾机制一期修复（吸血/金币拦截 + UI 事件）Trae 提示词

> 只写「改动方向 + 效果目标 + 红线」，不写实现细节（方法名、算法、伪代码）。
> 这是对已实现的护盾基座的**修复**，请先理解现状再动手。

---

## 一、现状（框架事实，帮你理解）

1. 护盾基座已实现：伤害命中护盾时，`TryAbsorbByShield` 会拦截并返回「是否被吸收」。被拦截时跳过扣血链，但**攻击方的「伤害收益」照常结算**了——这正是本提示词要修的。

2. 伤害收益有两类，且分布完全不同：
   - **吸血**：只在普攻路径（AttackPiece）的 `OnDamageDealt` 钩子触发。这个钩子有 7 个消费者（吸血、二段、减防、强化、领域、溅射等），但**只有吸血真正依赖传入的伤害值**，其余都用攻击力重算或只做状态标记、不看伤害值。
   - **金币**：分散在 12 处调用点——普攻内联 1 处 + 11 个效果类各自在伤害结算后调用 `GoldManager`（OnDamageDealt 攻击方金币 / OnDamageReceived 受击方金币）。

3. 统一伤害入口 `ApplyIncomingDamage` 目前返回 void，调用方（效果类）在调用后无法得知伤害是否被护盾拦截，所以照常用原始伤害给了金币。

---

## 二、改动方向

### 1. 吸血拦截

护盾挡掉伤害时，攻击方**不吸血**——包括「固定吸血量」也不吸。

### 2. 金币拦截

护盾挡掉伤害时，攻击方**不获得金币**、受击方**也不获得受击金币**。

### 3. 护盾层数 UI 事件

护盾层数变化时发出事件通知（对标能量条的事件机制），供 UI 订阅显示当前护盾层数；层数为 0 时 UI 隐藏。

---

## 三、效果目标（验收标准）

1. 带护盾的棋子被攻击（护盾挡住伤害）时：攻击方**不吸血**（固定 + 百分比都不吸）、**不获得金币**、受击方**不获得受击金币**。
2. 护盾被真实伤害破盾、或普通/元素盾正常格挡——**所有被盾挡的场景**，吸血和金币都归零。
3. 护盾**没挡到**的伤害（盾碎后的余量、无盾时），吸血和金币**照常**，数值和原来完全一致。
4. 二段攻击、减防、强化、领域、溅射这些「命中类」效果，护盾挡伤害时**照常触发**（护盾挡的是伤害，不是命中）。
5. 护盾层数每变化一次（获得/消耗/破碎/刷新），事件触发一次，UI 能拿到最新层数；层数归 0 时 UI 隐藏。

---

## 四、红线（绝不能破坏）

1. **不许动「命中类」效果**：二段攻击、减防（枯萎/冰锋）、强化（菲林斯）、领域（莫娜）、溅射（杜林）在护盾挡伤害时照常触发。
2. **不许改金币/吸血的基数与比例**：非拦截场景下，金币和吸血的数值必须和修复前完全一致。
3. **不许动元素附着、反应、冻结、感电、减防**——它们和护盾正交，护盾只拦伤害数值。
4. **不许破坏现有伤害介入顺序**（减伤 → 易伤 → 首伤=1 → 延迟拆分 → 免死）。
5. **不许动护盾拦截规则本身**（普通盾扣层 / 元素盾同元素免疫 / True 无视免疫扣层）。
6. UI 事件只做「通知」，不参与任何结算逻辑。
7. 本阶段**不做**元素城邦染色（二期）。

---

## 五、范围外（不要做）

- 元素城邦染色。
- 金币/吸血的数值平衡调整。
- 任何音效、UI 美化（UI 文本由用户自己在 Unity 添加，本提示词只提供事件）。
