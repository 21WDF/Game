# 阶段② C3（军团圣盾光环 + 进化 + 唯一被动）· Trae 提示词

## 总览

军团圣盾 = **光环（查询型，动态遍历全场）** + **进化（连续未受伤）** + **唯一被动（C2 框架的第一个使用者）**。这是 C 批最后一环。

---

## 一、参数（jsonParams，11 个）

| 字段 | 类型 | 默认 | 说明 |
|---|---|---|---|
| affectDirection | string | "Ally" | 作用方向：Ally=己方 / Enemy=敌方 |
| affectRange | int | 2 | 作用范围（HexCoord 距离 ≤ 该值） |
| bonusStat | string | "Defense" | 加成属性：Attack / Defense |
| bonusAmount | int | 5 | 加成数值（可为负值） |
| evolveTurns | int | 4 | 进化回合：-1=不进化；0=装备立即进化；>0=连续该回合数未受伤后进化 |
| evolvedDirection | string | "Ally" | 进化后作用方向 |
| evolvedRange | int | 3 | 进化后作用范围 |
| evolvedStat | string | "Defense" | 进化后加成属性 |
| evolvedAmount | int | 10 | 进化后加成数值（可为负值） |
| evolveOverrides | bool | true | 新光环是否覆盖旧光环（true=覆盖；false=新旧叠加） |

> `affectDirection` / `bonusStat` / `evolvedDirection` / `evolvedStat` 用 **string 承接 + 手动解析**（JsonUtility 枚举字段只认数值不认名字，同 FreeAPType 的坑）。定义枚举 `AuraDirection { Ally, Enemy }`、`AuraStat { Attack, Defense }`，解析不区分大小写，无法识别时回退默认（Ally / Defense）。

---

## 二、机制

### ① 光环（查询型，动态遍历全场）

- 当任意棋子的 `EffectiveAttack` / `EffectiveDefense` 被查询时，遍历**全场所有棋子**（`TurnManager.Instance.Model` 的 P1+P2 列表），对每个棋子的 `LegionAegisPassive` 判断该查询棋是否在光环内：
  - `携带者.Coord.Distance(目标.Coord) ≤ 作用范围`
  - 阵营匹配：Ally = 同阵营；Enemy = 敌阵营
  - 属性匹配：`bonusStat` == 查询的 statType
  - 命中则累加 `加成数值`
- **携带者自己默认吃自己的 Ally 光环**（距离 0 ≤ 范围，同阵营）；Enemy 光环天然排除自己（自己是己方非敌方）。
- 进化状态决定用哪套光环参数：
  - 未进化：用 `affectDirection/affectRange/bonusStat/bonusAmount`
  - 已进化且 `evolveOverrides=true`：只用 `evolved*` 一套
  - 已进化且 `evolveOverrides=false`：新旧两套都算（分别判断范围/方向/属性，命中都累加）
- **空安全**：`TurnManager.Instance == null || Model == null` 时返回 0。

### ② 进化计时（装备实例）

- 实例字段 `_turnsWithoutDamage` + `_isEvolved`：
  - `OnEquip`：`_owner = owner`；`_turnsWithoutDamage = 0`；`_isEvolved = false`；若 `evolveTurns == 0` 则 `_isEvolved = true`（立即进化）。
  - `OnTurnStart`：若 `_isEvolved || evolveTurns <= 0` 则跳过；否则 `_turnsWithoutDamage++`，`≥ evolveTurns` 时 `_isEvolved = true`（一次性，进化后不再退化）。
  - `OnDamageReceived`：`_turnsWithoutDamage = 0`（受击打断进化计时；进化后归零无影响）。
  - 卸下重装重新计时（OnEquip 清零）。

### ③ 唯一被动（走 C2 框架）

- `LegionAegisPassive` 构造接收 `uniqueId` 并覆写 `UniqueId` 属性。
- `PassiveFactory` 的 case 把 `Create` 收到的 `uniqueId` 透传给构造。
- 装备 `.asset` 的 `PassiveConfig` 配 `isUnique=true` + `uniqueId`（如 `"LegionAegis"`）+ `passiveName`（如 `"圣盾光环"`）。

---

## 三、改动文件

1. **新建 `Assets/Game/Equipment/Effects/LegionAegisPassive.cs`**：
   - 含 `AuraDirection` / `AuraStat` 枚举。
   - 实现 `IPassiveEffect`，覆写 `OnEquip` / `OnTurnStart` / `OnDamageReceived` / `UniqueId`。
   - 暴露方法供属性查询调用（如 `int GetAuraAmount(string statType, HexCoord targetCoord, PlayerSide targetOwner)`，返回对指定目标该属性的光环加成，0 = 不在范围内/方向或属性不匹配）。

2. **`PassiveFactory.cs`**：加 `case "LegionAegisPassive"` + `LegionAegisParams` 参数类（`[System.Serializable]`，string 字段承接枚举，含 `ParseAuraDirection` / `ParseAuraStat` 解析方法）；case 内把 `Create` 收到的 `uniqueId` 透传给 LegionAegisPassive 构造。

3. **`PieceModel.cs`**：
   - 加私有方法 `GetAuraBonus(string statType)`：遍历全场棋子收集 `LegionAegisPassive.GetAuraAmount` 累加（空安全返回 0；尊重唯一被动去重——遍历时用 `GetAllPassives()` 即可复用 C2 去重逻辑）。
   - `EffectiveAttack` 追加 `+ GetAuraBonus("Attack")`；`EffectiveDefense` 追加 `+ GetAuraBonus("Defense")`。

4. **顺手修 C2 遗留缩进瑕疵**（非阻塞，纯整洁）：
   - `PieceModel.GetAllPassives` 从注释第 2 行起、方法声明与整个方法体**多缩进了 4 空格**，统一回退到类成员 4 空格、方法体 8 空格。
   - `IPassiveEffect.OnDamageReceived` 的注释第 2 行与声明**多缩进了 4 空格**，回退对齐。

---

## 四、约束（务必遵守）

1. **不要改伤害/死亡介入逻辑（C1）、不要改唯一被动去重逻辑本身（C2）**——只新增军团圣盾并复用它们。
2. 光环是**查询型**（属性查询时现算），不是"进出范围时加减"——不要引入范围变更事件监听。
3. 进化计时/进化状态**记在被动实例上**（跟装备走，卸下重装重新计时），不要借用棋子全局字段。
4. 参数类**全部 `[System.Serializable]`**；枚举字段用 string 承接 + 手动解析。
5. 光环遍历注意空安全（`TurnManager.Instance` 可能为 null）。
6. 诊断无报错后收尾。
