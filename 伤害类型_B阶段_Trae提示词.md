# 伤害类型系统 · B 阶段（反甲改造 + 减伤类型筛选）· Trae 提示词

## 需求

A 阶段已给所有伤害打上类型标签（Physical / Magical / True）。B 阶段消费这些标签，做两件事：

1. **反甲**：从"来源位掩码筛选"改为"只反物理伤害"。
2. **减伤**：加"筛选类型"参数，并把减伤介入扩展到所有伤害类型。

## 改动方向

### 一、反甲（ThornsPassive）

- 废弃 `reflectTriggers`（伤害来源位掩码），改为**只反物理伤害**（`kind == Physical` 才反伤，其余类型直接返回）。
- 反甲反出的伤害已经是 Magical（A 阶段已打标签），天然不会触发反甲，**防递归自动成立**。
- 删掉 `reflectTriggers` 参数：构造参数、参数类字段、工厂 case 传参一并移除。

### 二、减伤（DamageReductionPassive）

- 加一个**筛选类型参数**（默认「全类型」，可选「只减物理 / 只减魔法 / 只减真实」），用 string 承接 + 手动解析（对标 element / freeType 的写法）。
- 减伤介入从「只在普攻结算」扩展到「所有伤害入口」（普攻 + 统一伤害入口 `ApplyIncomingDamage`），按上面的筛选参数决定是否对该类型生效。
- 减伤介入的顺序保持在「首伤=1」之前（与普攻路径的既有顺序一致）。

### 三、顺手修

- 超载爆炸的日志文案「真实伤害」改为「魔法伤害」（现在爆炸是 Magical）。

## 约束（务必遵守）

1. **减伤被动是「额外减伤」**，和「防御减伤（减法公式）」是两回事——魔法/真实不受防御减伤，但仍受减伤被动（默认全类型，除非筛选排除）。
2. 反甲废弃 reflectTriggers 后，不再有「响应哪些来源」的掩码，统一「只反物理」。
3. 不改伤害计算公式、不改首伤=1 / 延迟拆分 / 免死的顺序、不改 DamageKind 枚举与 A 阶段的打标签。
4. 参数默认值保持向后兼容（减伤默认全类型，行为与改动前对物理伤害一致）。
5. 诊断无报错后收尾。
