# 季风之城二期（气候·简单组）— Trae 提示词

## 现状（框架事实）

- 一期已完成四季 + 昼夜，采用「集中配置 + 内聚过滤」模式：季风城邦专属管理器持有状态，判断「当前城邦 == 季风城邦」才生效，效果数值从配置资产读取。
- 一期已预留气候扩展口（配置资产里有气候条目列表，但结构偏简单，本期需扩展为「气候」专用结构）。
- 气候效果可复用现有落点：
  - 暴雨的「上回合受击」：棋子已有「未受伤害连续回合计数」（受击归零、回合开始 +1），据此判断「上回合是否受击」；
  - 暴雨的「攻击距离 -1」：攻击范围聚合（已有基础值 + 装备加成）；
  - 暴雪的「击退 1 格」：已有击退解析器（超载反应同款，给定攻击者/受击者坐标 + 距离即可）；
  - 高温的「行动后锁定」：棋子已有「本回合已攻击」标记（攻击后置位、回合开始重置），移动/攻击入口可据此拦截。

## 改动方向

在季风城邦内新增「气候」机制（二期只做简单组：暴雨、暴雪、高温；飓风、雷暴留待三期），沿用「内聚过滤 + 数据驱动」：

1. **气候状态**：内聚在季风城邦管理器（当前活跃的气候集合，可同时存在多个），判断「当前城邦 == 季风城邦」才生效。
2. **出现与持续**：每轮开始时随机判定新气候（「每轮」= 双方各行动一次，新一轮开始时判定）；气候出现后持续随机 1~3 轮，持续期间生效，到期移除。
3. **概率与限制**：各气候概率从配置读取（默认 5%），独立判定、可叠加；**专属气候（暴雪=冬季限定、高温=夏季限定）在非对应季节时概率为 0、不参与判定**；**高温独占——高温出现时本轮不再判定其他可叠加气候**。
4. **三个效果（二期）**：
   - 暴雨（通用）：上回合受到攻击的棋子，本回合攻击距离 -1（下限 1 格，至少可攻击周围 1 格）；
   - 暴雪（冬季限定）：棋子攻击后，被攻击棋子被击退 1 格（复用超载击退）；
   - 高温（夏季限定）：棋子进行了一次行动（移动 或 攻击）后，本回合无法再进行任何行动（不能移动也不能攻击）。
5. **配置扩展**：气候条目承载「名称 + 概率 + 限制季节 + 效果类型 + 数值 + 是否可叠加/独占」等字段，从配置资产读取（代码不硬编码概率与效果数值）。

## 效果目标（验收标准）

1. 选到季风城邦 → 每轮开始随机判定气候，日志清晰显示「本轮气候：X / 晴」及持续时间。
2. 暴雨：上回合受击的棋子本回合攻击范围 -1（下限 1）；未受击者不受影响。
3. 暴雪：冬季时攻击后击退被攻击者 1 格（复用超载击退，遇阻/边界自然处理）；非冬季暴雪不出现。
4. 高温：夏季时棋子移动或攻击后本回合锁定（不能再移动/攻击）；非夏季高温不出现。
5. 高温独占：高温出现时，本轮不再同时出现暴雨/暴雪/雷暴等其他气候。
6. 概率、持续时间、限制季节、是否叠加均可从配置资产调整（数据驱动，代码零硬编码）。
7. 其他城邦 → 气候机制不生效，棋子无任何气候影响。
8. 气候切换/到期有清晰日志，便于验证节奏与叠加。

## 红线（绝不能破坏）

1. **内聚过滤**：气候的「是否生效」判断内聚在季风城邦管理器内部，不散落到移动/攻击/伤害管道等调用点。
2. **复用现有落点，不建实体系统**：暴雨走攻击范围聚合、暴雪走击退解析器、高温走行动标记；本期不做飓风（掉落物）、雷暴（雷电残留）等棋盘实体。
3. **时间单位铁律**：气候判定/持续递减按「轮」计，不按「回合」。
4. **数据驱动**：概率、持续轮数、限制季节、效果数值均从配置资产读取，代码不硬编码。
5. **简单组只做三个**：本期只实现暴雨、暴雪、高温；飓风、雷暴、晴（无效果）之外的气候都不做。
6. **叠加语义**：可叠加气候（本期含暴雨、暴雪）可同时生效；高温独占。叠加规则不因实现而改变。
7. 四季、昼夜、伤害类型、元素系统、护盾、贸易机制、城邦选择/过滤等已完成功能零改动。
