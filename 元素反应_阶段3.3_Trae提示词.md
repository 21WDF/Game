# 阶段③.3（元素量加成 + 超载爆炸 AOE）· Trae 提示词

## 总览

装备系统的最后一块：四元素之力的两个新机制，都要动 `AttackPiece` 的元素结算区：

1. **元素量加成**：对应元素棋子普攻的触发元素量从 1 → 2。
2. **超载爆炸 AOE**：灼火/陨雷之力让超载反应对目标周围 1 格敌方额外造成真实伤害。

本阶段给 `ElementalCorePassive` 加 4 个新参数，并在 `PieceManager` 加两个介入点。

---

## 一、新增参数（ElementalCorePassive 从 6 参数扩到 10 参数）

| 字段 | 类型 | 默认 | 说明 |
|---|---|---|---|
| （原有 6 个不变） | | | element / multiplierPercent / electroChargeTurns / freezeTurns / superconductDefense / superconductTurns |
| extraGauge | int | 1 | 对应元素棋子普攻额外元素量（元素量加成） |
| explosionRadius | int | 1 | 超载爆炸范围（HexCoord 距离 ≤ 该值） |
| explosionBase | int | 1 | 爆炸基础伤害（公式 = base + floor(伤害/divisor)） |
| explosionDivisor | int | 5 | 爆炸伤害除数（每 divisor 点伤害 +1） |

> 爆炸伤害公式：`explosionBase + floor(超载伤害 / explosionDivisor)`，默认 `1 + floor(伤害/5)`。

## 二、ElementalCorePassive 暴露的新成员

1. `public ElementType Element => _element;`（元素量加成判定用）
2. `public int ExtraGauge => _extraGauge;`
3. `public bool CanExplode => _element == ElementType.Fire || _element == ElementType.Thunder;`（只有火/雷元素之力超载爆炸）
4. `public int ExplosionRadius => _explosionRadius;`
5. `public int GetExplosionDamage(int damage) => _explosionBase + damage / _explosionDivisor;`

## 三、PieceManager 介入点

### 1. 元素量加成（AttackPiece 元素附着处）

当前（约 243-250 行）：
```
var (resultElem, resultGauge) = ElementReactionTable.ResolveElementInteraction(
    target.AffixedElement, target.AffixedElementGauge, attackerElem, 1);
```
`1` 是普攻触发量。改为：
```
int triggerGauge = 1 + GetExtraElementGauge(attacker, attackerElem);
... ResolveElementInteraction(..., triggerGauge);
```

新增私有聚合方法 `GetExtraElementGauge(PieceModel attacker, ElementType attackerElem)`：
- 遍历 `attacker.GetAllPassives()`，对 `ElementalCorePassive` 且 `Element == attackerElem` 的被动累加 `ExtraGauge`，返回总和（无则 0）。

### 2. 超载爆炸（AttackPiece 超载处理处）

当前超载击退区（约 269-274 行，`reaction.KnockbackDistance > 0 && !target.IsDead`）。在其后（或同区）新增：

```
if (reaction.Type == ReactionType.Overload && !target.IsDead)
    ApplyOverloadExplosion(attacker, target, damage);
```

新增私有方法 `ApplyOverloadExplosion(PieceModel attacker, PieceModel target, int overloadDamage)`：
1. 遍历 `attacker.GetAllPassives()`，找第一个 `CanExplode` 的 `ElementalCorePassive`（没有则 return）。
2. `explosionDamage = exploder.GetExplosionDamage(overloadDamage)`；`≤ 0` 则 return。
3. **快照收集**目标周围受害者：遍历 `TurnManager.Instance.Model.GetPieces(敌方侧)`（`attacker.Owner` 的对立面），排除 `null` / `IsDead` / `target 本身`，`target.Coord.Distance(enemy.Coord) <= exploder.ExplosionRadius` 的加入临时 `List`。
4. 逐个受害者：`ApplyIncomingDamage(victim, explosionDamage, attacker, DamageSource.Splash)`（真实伤害，打断计时，不触发反甲递归）；若 `victim.IsDead` 则 `DestroyPiece(victim, attacker)`（攻击者为击杀者，OnKill 被动生效）。

## 四、改动文件

1. **`ElementalCorePassive.cs`**：构造 + 字段 + 4 个新参数；暴露 `Element` / `ExtraGauge` / `CanExplode` / `ExplosionRadius` / `GetExplosionDamage`。
2. **`PassiveFactory.cs`**：`ElementalCoreParams` 加 4 字段（默认 1/1/1/5）；case 读 10 字段传构造。
3. **`PieceManager.cs`**：加 `GetExtraElementGauge` + `ApplyOverloadExplosion` 两个私有方法；`AttackPiece` 元素附着处改 `triggerGauge`、超载处加爆炸调用。

## 约束（务必遵守）

1. **不要改** Modify 逻辑、元素反应结算、伤害介入顺序、唯一被动框架。
2. 爆炸伤害是**真实伤害**（走 `ApplyIncomingDamage`，不再减伤），`damageType` 用 `DamageSource.Splash`。
3. 爆炸**排除 target 本身**（target 已受超载伤害）；爆炸致死 `DestroyPiece(victim, attacker)`（击杀者 = 攻击者）。
4. 受害者收集**先快照再结算**（爆炸致死会改棋子列表，避免 foreach 遍历中修改）。
5. 元素量加成只对「携带者元素 == 攻击方先天元素」生效（即对应元素之力）。
6. 诊断无报错后收尾。
