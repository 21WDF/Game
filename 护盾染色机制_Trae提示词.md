# 护盾染色机制（元素城邦二期）Trae 提示词

> 只写「改动方向 + 效果目标 + 红线」，不写实现细节（方法名、算法、伪代码）。

---

## 一、现状（框架事实）

1. 护盾基座已就绪：棋子有 `ShieldStacks`（层数）和 `ShieldElement`（元素标签，None=普通盾）两个字段，统一通过一个修改入口更新（盾碎时元素标签会自动清空）。
2. 元素附着逻辑汇聚在一个纯函数（`ResolveElementInteraction`），返回「消耗反应后的最终附着元素」。这个函数被**多个调用点**调用——普攻、大招、多个被动各自内联调用，**不只一处**。染色必须覆盖所有附着路径，漏一处就会出现「某个技能能染色、另一个不能」。
3. 元素反应（伤害倍率）和元素附着是**两步**：先查反应算伤害，再结算附着。染色封印的是「附着」这一步，不是「反应」。
4. `GameConfig` 是全局配置（ScriptableObject），适合放全局开关。

## 二、改动方向

实现「元素城邦护盾染色」，用一个**全局开关**控制（默认开启，未来城邦系统接管此开关）。

染色行为（开关开启时）：

1. **染色触发**：一个「有护盾」且「未染色（护盾元素为 None）」的棋子，身上一旦被元素附着（附着的最终结果不是 None），护盾就永久变成对应元素护盾。
2. **染色封印**：护盾一旦染成某元素，该元素就**无法再附着到该棋子身上**——其他元素照常附着。
3. **反应照常**：染色封印的是「附着」，元素反应（伤害倍率、反应副作用）照常发生，不受染色影响。
4. **染色随护盾消失**：护盾扣到 0 层时，染色随之消失（护盾元素清空）；重新获得护盾 = 无元素盾，重新等首次附着（这一条基座已经天然支持，无需额外做，只要别破坏它）。

## 三、效果目标（验收标准）

开关开启时：

1. 一个有普通盾的棋子被**火元素**攻击（火附着成功）→ 护盾变成**火盾**。
2. 一个火盾棋子再被**火元素**攻击 → 火**不附着**（身上元素不变），但元素反应照常、火伤害被火盾免疫（基座已有）。
3. 一个火盾棋子被**水/雷/冰**攻击 → 水/雷/冰**照常附着**，护盾仍是火盾（不换色）。
4. 所有元素附着路径（普攻、大招、各类被动）染色行为一致，无遗漏。
5. 火盾扣到 0 层 → 护盾元素清空（回到未染色）；重新获得护盾 → 无元素盾，重新等首次附着。

开关关闭时：染色完全不生效，护盾永远是普通盾（无元素标签），行为等同护盾基座。

## 四、红线（绝不能破坏）

1. **不动护盾拦截规则**（普通盾扣层 / 元素盾同元素免疫 / 真实无视免疫扣层）——染色只改「护盾元素」这个标签，不改拦截逻辑。
2. **不动元素反应系统**（倍率、副作用、Gauge 消耗算法）——染色封印的是「附着」，反应照常。
3. **不动伤害管道、吸血/金币拦截、护盾 UI 事件**。
4. **染色随护盾消失**这个基座已有行为不能破坏（盾碎清元素的语义保留）。
5. 开关关闭时，一切行为等同护盾基座（无染色）。

## 五、范围外（不要做）

- 城邦系统本身（当前城邦类型、切换城邦等）——只做一个全局开关占位。
- 染色的 UI 显示（护盾元素图标等）——本期不做，只做逻辑。
- 任何数值平衡（染色相关数值全按现有护盾元素标签，无新增数值）。
