# 元素系统城邦过滤 — Trae 提示词

## 现状（框架事实）

- 元素系统（元素附着 + 元素反应 + 伤害带元素）目前是**无条件常驻**的，没有按「当前城邦」过滤。而按设计，元素系统整体是「元素城邦」的专属机制——非元素城邦下，棋子不应带元素、不应挂元素、不应触发元素反应，只保留「伤害类型」（物理/魔法/真实）这一维度。
- 之前的「机制按城邦过滤」只关了**护盾染色**（元素城邦专属之一），漏了元素系统本体。
- 元素系统的「消费」汇聚在少数几个点：
  - 元素反应（倍率 + 副作用：超导减防/感电DoT/冻结/超载击退）→ 反应查询函数（`ElementReactionTable`，有带修饰器和不带修饰器两个重载）；
  - 元素附着/消耗 → 元素交互结算函数（`ElementReactionTable`）；
  - 伤害跳字的元素颜色 → 普攻路径、统一伤害入口、溅射路径三处；
  - UI 元素图标 → 选棋子界面、棋子面板。
- 「当前城邦」已有统一查询入口（城邦管理器，`当前城邦 == 元素城邦` 判定）。伤害类型与元素是正交维度（物理伤害可带元素、魔法伤害可带元素），关闭元素只关「元素」这一维，不动「伤害类型」的减伤规则。

## 改动方向

让元素系统整体只在「当前城邦 == 元素城邦」时生效，其余城邦下退回到「无元素」基线。过滤判断应内聚在元素系统的核心与展示处：

1. **元素反应**：非元素城邦下，反应查询返回「无反应」（伤害倍率 1、无任何副作用），即蒸发/融化/超载/感电/冻结/超导全部不触发。
2. **元素附着**：非元素城邦下，元素交互结算不再附着元素（目标身上不挂任何元素，也不消耗）。
3. **伤害跳字颜色**：非元素城邦下，伤害数字的元素颜色视为「无元素」，回落到按伤害类型的默认色（物理/魔法/真实各自默认色）。
4. **UI 元素图标**：非元素城邦下，选棋子界面与棋子面板上的元素图标隐藏。

## 效果目标（验收标准）

1. 选到元素城邦 → 元素附着、元素反应、元素伤害、染色全部照常（与过滤前一致）。
2. 选到其他城邦（贸易/欢愉/季风/邪疑/战争）→ 棋子攻击不挂元素、不触发任何反应、伤害跳字用默认色、UI 不显示元素图标；护盾退回普通层数护盾（无染色）。
3. 伤害类型的减伤规则不变：物理仍受防御+护盾、魔法仍仅受护盾、真实仍无视防御护盾。
4. 非元素城邦下，行为与「完全没有元素系统」一致，不留半激活状态或残留元素附着。

## 红线（绝不能破坏）

1. **伤害类型与元素的正交铁律不变**：关闭的只是「元素」维度，伤害类型（物理/魔法/真实）的减伤、护盾关系一字不动。
2. **元素城邦下的元素系统行为零改动**：反应倍率表、Gauge 消耗算法、元素克制关系、染色逻辑，在元素城邦下全部照常。
3. **护盾基座零改动**：层数护盾、拦截规则、吸血/金币拦截不受本次影响（染色过滤已单独做过，本次与它同属「元素城邦专属」且不冲突）。
4. **反应数据源零改动**：反应数据库（ScriptableObject）、反应修饰器链、临时覆盖逻辑不因城邦过滤而改变数据内容。
5. 城邦选择、纯函数、随机入口、以及已完成的「机制按城邦过滤」（贸易/染色）零改动，本次只是补齐「元素系统本体」这一块。
